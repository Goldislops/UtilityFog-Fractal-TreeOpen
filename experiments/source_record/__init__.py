"""``source-record-v1``: a synthetic-only, read-only laboratory for recording
sources and attributed claims across two mechanically separated corpus
registers and one bridge register.

The binding specification is ``CONTRACT.md`` in this directory. This package
deliberately re-exports nothing: the import surface is exactly
``experiments.source_record.schema`` and ``experiments.source_record.validate``.
"""
