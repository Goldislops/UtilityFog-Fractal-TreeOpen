# AGENT_HANDOFF.md — Project Orientation for AI Collaborators

> **For**: Any AI (Claude, Gemini/AURA, GPT/Jack, future Nemo Claw, etc.) joining this project mid-stream. Read this first; it'll save Kevin from having to re-explain everything every time.
>
> **Last revised**: 2026-07-08 (`main = e30b1dd`). State is point-in-time — `git log --oneline -20` is authoritative for repo state; snapshot state requires a numeric/date-aware listing (plain `ls … | tail` mis-sorts lexically, e.g. `gen9…` after `gen1…`) and may differ between local `data/` and the backup.

## ⭐ Latest State (2026-07-08, `main = e30b1dd`) — read this first

**Current-state snapshot. The Phase 19 block below remains the active work-phase narrative; this block supersedes older "next work" pointers. Authoritative state = `git log --oneline -20`.**

- **Repo:** `main = e30b1dd` (PR #297). Since the prior handoff (`d342c14`): the **.gitignore hygiene arc COMPLETED** — **#266** (this file's prior refresh) then Packets **A #267** (Rust/Python build+cache ignores), **B #268** (`data/` runtime-output ignores), **A2 #269** (untracked the 19 stale committed `.pyc`), **Epi-C #270** (regenerable epi-genome export ignored), **C #271** (`crates/vanguard-mcp/Cargo.lock` now TRACKED — binary-crate norm). **The prior block's ~52,000-untracked caveat is RESOLVED: `git status` is fully clean** (curated `data/` files + base genome remain tracked). Then the **2026-07-06/07 coding session** landed **#275/#276/#277** (UTF-8 encoding + CI-comment fixes — see Session Log). The **07-07 PM continuation** landed **#280/#281** (telemetry + vis-CSV UTF-8) and **#282/#283** (deprecated `utcnow()` removal + observatory all-NaN color-scale guards) — the full gate now runs **warning-free**. The **07-07/08 defect-hunt arc** then landed **#284–#297** (handoff refresh + 13 adversarially-verified fixes — see Session Log); the maintained suite grew **786 → 809 passing** with the gated engine defect as the sole failure.

- **Read-only repo orientation EXISTS (2026-07-06):** ops-dir `C1_REPO_ORIENTATION_v1.md` maps engine vs instruments vs app-stack vs docs from the tree itself (6 bounded read-only scans + parent verification; `data/`+NPZ excluded). Future seats: orient there first, with `ca/docs/README.md` for CA execution-surface status.

- **Legacy-package caveat (NEW 2026-07-07):** `UtilityFog_Agent_Package/` is a legacy snapshot with corrupted stub files, **but it is gate-imported**: CI-gated `tests/test_feature_flags.py` and `tests/test_observability.py` sys.path-append it and import `agent.feature_flags` / observability modules ("canonical package" per their in-file comments). **Plain removal would break the pytest gate.** Disposition (remove vs tombstone vs relocate) = open Kev/Jack item; treat it as live-in-part until decided.

- **Known engine defect (recorded, unfixed, GATED):** on CuPy+GPU machines, `step()`'s memory-grid migration crashes on legacy 3/5-channel NumPy grids (`_migrate_memory_grid`, `scripts/continuous_evolution_ca.py:670`; test `test_3_to_8_channel_migration` fails locally, passes on CPU-only CI — the memory grid is migrated at line 962 *before* being coerced to GPU at lines 963–964, so `_migrate_memory_grid` receives a NumPy array while the active backend is CuPy, causing a NumPy/CuPy array mismatch). One-line fix PROPOSED in ops-dir `ENGINE_CUPY_MIGRATION_BUG_DIAGNOSIS_v1.md`; **engine edits need their own explicit go.** Local GPU/Windows baseline (2026-07-08, post-merges): `python -m pytest tests/ -q` = **809 passed / 1 failed / 26 skipped, zero warnings** (skips = uft_ca extension not installed locally; `pip install ./crates/uft_ca` to match CI's full gate).

- **Ignition/Cascade investigation (NEW, June 2026) — read-only, OPS-DIR, PROVISIONAL, NOT in the repo.** A multi-day **read-only** analysis of git-ignored `data/` snapshots (no engine, no repo mutation, all outputs under `C:\Users\kevin\medusa_backup\`) reached a **Jack-reviewed provisional** characterization of the late COMPUTE ignition as **emergent lattice-scale phase behavior** — everywhere-at-once, *not* a resolved center-out wave, **within snapshot resolution**. The non-void front predates the sampled window; an anisotropy caveat (mild radial gradient: center-bias vs cube-boundary depletion) is **parked**. **Nothing is canonical**; artifacts + full caveats live **outside the repo**. *(Governance ≠ evidence; do not promote into repo docs as established. Lock-in→expansion = correlation/hypothesis only. External media = analogy only, never evidence.)*

- **Warm backup (NEW).** `data/` snapshots are mirrored off-OneDrive to a warm backup (UNC `\\192.168.86.50\data\medusa_data_backup\data` + `K:`), with a nightly task. *84-reported:* ~13,627 npz / ~577 GB, newest ≈ gen 1,896,023 (2026-06-26). `data/` is **not** the only copy.

- **Governance — M'WE Constitution v1 (NEW, external).** Project process follows the **M'WE Constitution** (ratified 2026-06-27), an **external** charter at `C:\Users\kevin\medusa_backup\mwe_constitution_v1.md` (kept out of the repo by design). Authorization lanes: *small / sizable-no-code / existing-output read / helper-script / read-only snapshot pass / engine·replay·intervention / repo-mutation·PR / canonical-claim* — each more gated than the last. **Glass Wall:** Medusa-generated artifacts are the only Medusa evidence lane; external media may shape vocabulary/questions/analogies, **never** Medusa evidentiary weight *(unless separately cited as external scholarship — see Constitution Law 8)*.

- **Theory Sandbox — trap arc COMPLETE** (unchanged). Toys #1–#5 as scripts, #6 analytically closed; passive-local rule-mask trap paradigm durably closed. `experiments/theory_sandbox/TRAP_ARC_CAPSTONE.md`. Resting at a clean checkpoint; all future doors parked + unauthorized.

- **Lane A / Swarm Hunter — PARKED** (standing guardrail). No engine consumer acts on observer signals; Swarm Hunter unimplemented. **Task A** (engine consumer of `tuning_pending.json` + Track A CuPy parallelism) = engine surgery, HIGH RISK, **NOT started, UNAUTHORIZED**; `docs/LANE_A_READINESS_REVIEW.md` precedes it (authorizes nothing).

- **Lane B / Nextness Observer — BUILT & CALIBRATED** (unchanged). Passive, read-only; do not conflate with Lane A.

- **Vanguard / AURORA / Granite** (unchanged): code present, live-cluster provisioning **UNVERIFIED**; "Aurora" = an Alienware desktop Ollama host (not a model); "Granite" = IBM Granite 4 via Ollama; smoke test is a documented PLAN, execution UNVERIFIED. Make no assumptions without evidence.

- **Board hygiene:** PR **#17** content salvaged via #265. Remaining Sept-2025 stale PRs (#25, #52–#58) — *84-reported as still open/triage-pending; re-check `gh pr list` before acting.* **Dependency surface (2026-07-07):** Dependabot version-update PRs **#272/#273/#278** open (uft_ca `toml`/`criterion` + the actions group — #278 superseded #274 on 2026-07-07 after the #276 ci.yml merge) — observe-only, merge = Kev's gate; **Dependabot alert #10** open (LOW, `rand` via `crates/vanguard-mcp/Cargo.lock` — visible since Packet C tracked the lockfile; unremediated — vanguard-mcp has no test/CI gate to verify a bump against).

- **Engine:** Medusa bakes at **256³**. *84-reported (not live-verified):* newest backup snapshot = gen **~1,896,023** (2026-06-26). **Do not pause/restart for routine work.**

## ⭐ Current Phase — Phase 19 (Nextness Observer + Second-Pass Calibration)

**Read this block first; the Phase 17/18 material below is still accurate but is now historical context.**

Phase 19 added the **Nextness Observer**: a passive, read-only, offline Layer-2 analyser that translates local CA patches into a 16-token vocabulary (`scripts/nextness_observer.py`), plus a calibration harness (`scripts/nextness_calibration.py`).

- **Lane A is PARKED.** No agent acts on the observer's signal yet. No engine touch, no tuning API, no Swarm Hunter activation. This is the standing guardrail for all Phase 19 work.
- **Lane B** (observer/operator side) is where the work happens.
- **Second-pass calibration arc — COMPLETE & MERGED**: PR #159 (plan) → #160 (predicate aggregator review) → #161 (empirical profiling) → #162 (candidate selection) → #163 (vocabulary/status model) → **#164 (metta_warmth demotion implementation, merged `82db1f3`)**.
- **Key outcome**: warmth is real but too sparse to cluster ("a hermit candle in a stone cathedral"). `metta_warmth` is now status `diagnostic_only` — removed from the cascade, its signal surfaced as `warmth_max` / `warm_cell_count` JSONL diagnostics. New `active_vocabulary_occupancy` metric distinguishes routing vocab (12 tokens) from full historical vocab (16). `phase_boundary` kept but documented as radius-specific.
- **Memory grid is CHANNEL-FIRST** `(channels, X, Y, Z)` — enforced by `load_snapshot`. Index channels as `memory[idx]`, not `memory[..., idx]`. Locked by `test_warmth_diagnostics_read_correct_memory_axis`.
- **CI gate (FULL SUITE — #165 COMPLETE & CLOSED; #180 maturin arc landed)**: `verify-python` runs the **entire maintained suite** via `python -m pytest tests/ -q` on every PR/push, **with the `uft_ca` Rust/pyo3 extension built in CI** (Rust toolchain + `pip install ./crates/uft_ca`, maturin backend). Current state ≈ **832 passed, 8 skipped, 0 failed, 0 collection errors** (see the latest `verify-python` run for live counts). Coverage history: #165 arc (#172→#186) closed the "CI runs nothing" blind spot; skip-conversion added `matplotlib`/`flask`/`pyzmq` (#190) and `openai` (#191); the #180 maturin arc added the Rust extension (#195 packaging, then test-contract reconciliation + CI wiring). CI install: `numpy pytest pytest-asyncio matplotlib flask pyzmq openai` + Rust stable. Remaining ~8 skips are internal sub-skips (e.g. observatory variants, `evolution_engine`). (Run tests locally as hygiene.)
- **Next safe work**: the full coverage saga is **complete** — #165 (#172→#186, "CI runs nothing" → full-suite gate), skip-conversion (#190 pip deps, #191 `openai`), and the **#180 maturin arc** (#195 packaging + #196 reconciliation/CI wiring; **#180 closed**) are all merged. The maintained suite now gates fully in CI — Python **and** the `uft_ca` Rust extension built on every PR (≈832 passed / ~8 internal sub-skips / 0 failed). **Named future arcs** (each its own small, reviewed brick — none started without an explicit go): (1) **Theory Tripwire Action** — *design-first* (a PR-check that links the ledger/preflight when work touches Lane A / Swarm Hunter / observer scopes; design before implementation to avoid noisy auto-commenting); (2) **Theory Sandbox** — *policy merged & active* (`experiments/theory_sandbox/`, non-canonical, outside engine imports, outside default CI; **toys #1–#5 landed as scripts and #6 was analytically closed, completing the trap arc — see the Latest State block above**, one toy per PR, promotion gated by the README §4 six-step gate); (3) **Lane A readiness review** — *planning only*. **Still not Lane A; still no Swarm Hunter implementation.**
- **Phase 19 docs**: `PHASE_19_NEXTNESS_OBSERVER.md`, `PHASE_19_PR4_CALIBRATION*.md`, `PHASE_19_SECOND_PASS_CALIBRATION_PLAN.md`, `docs/WORKSTREAM_B_*.md`, `docs/WORKSTREAM_C_VOCABULARY_STATUS_REVIEW.md`.

## What This Project Is

**UtilityFog-Fractal-TreeOpen** has two distinct halves now, both real and active:

1. **Cellular-automata simulation engine** ("Medusa"). A 256³ voxel CA evolving on an RTX 5090 since gen ~0; currently at **gen ~1.5M+** running **Phase 17a** (Magnon Amplification for 512³ readiness). Substrate-independent design — Portable Genome Format, STL export, Rust+WASM port, multi-node shard protocol with ZMQ transport.
2. **Governed, model-agnostic agent orchestration & tuning framework** built on top of Medusa in Phase 18. REST API for observation + write-side tuning (propose/commit/rollback) with a tunable-parameter schema and category gating, ZMQ PUB event bus, agent-backend ABC, two concrete backends (`AnthropicBackend`, `OpenAICompatBackend` for OpenAI / NIM / DeepSeek / Together / vLLM / SGLang / Ollama / llama.cpp server), and an orchestrator loop that ties them together. Provider parity is proven by test, not slogan.

Treat them as **one project with two surfaces**, not two projects in a trenchcoat. Phase 18 was added *because* the simulation got mature enough to need autonomous tuning. The orchestration is in service of the matrix, and the matrix is the substrate that gives the orchestration something to govern.

## Repository Map

| Path | What | Runtime-critical? |
|------|------|-------------------|
| `scripts/continuous_evolution_ca.py` | The CA engine. Medusa runs from here. | **YES — coordinate any change with Kevin.** |
| `scripts/medusa_api.py` | Flask REST API on `:8080` (Phase 16) + tuning blueprint (Phase 18 PR 2) + event bus (PR 3). | Restartable; no engine impact. |
| `scripts/params_schema.py` | Tunable parameter registry (AUTO / HUMAN_APPROVAL / LOCKED). | Pure metadata. Add params here. |
| `scripts/tuning_api.py` | Flask blueprint: propose/commit/rollback with safety rails. | Restartable. |
| `scripts/event_bus.py` | ZMQ PUB on `:8081` + StateWatcher. | Restartable. |
| `scripts/agent_backends/` | `AgentBackend` ABC, `MockBackend`, `AnthropicBackend`, `OpenAICompatBackend`. | Pure library. |
| `scripts/orchestrator.py` + `orchestrator_config.py` | Observe-decide-act loop driving the LLM. | Pure library. |
| `scripts/shard_protocol.py` + `shard_transport_zmq.py` | Phase 17b distributed-stepping protocol + ZMQ backend. | Pure library; not yet running in production. |
| `scripts/dandelion.py`, `dandelion_physics.py` | Phase 9: STL/QR/WASM organism dispersal. | Pure library. |
| `scripts/medusa_start.py`, `watchdog.py` | Engine launcher + 24/7 watchdog daemon. | Coordinate restarts. |
| `crates/uft_ca/` | Rust CA kernel + WASM port (Phase 10); also a pyo3 Python extension (maturin, `pip install ./crates/uft_ca`) built in CI since #180. | Build artifact. |
| `crates/vanguard-mcp/` | Vanguard MCP cluster orchestrator (Phase 13/16c). | Coordinate. |
| `vis/observatory/` | Phase 8: 3-tier visualization. | Off-engine. |
| `data/` | Snapshots + telemetry + ledgers + pending-tuning. | **Read-only for agents.** |
| `tests/` | Maintained pytest suite — full-suite CI gate (`verify-python`: `python -m pytest tests/ -q`). Currently ≈718 passed / ~33 deliberate dependency-gated skips / 0 failed (see latest CI run for live counts). | Run before every commit. |
| `PHASE_17B.md`, `PHASE_18.md`, `BACKEND_PROVIDER_MATRIX.md` | Architecture design docs. | Source of truth on intent. |

## The Three-AI Collaboration (Default Roles, Not Contracts)

These are the **default seats**, not assigned-for-life contracts. Anyone should feel free to step into another role when the situation calls for it. The point is to default to the seat where each model adds the most value, not to fence the others out.

- **AURA / Gemini** — *Strategy & Phase Planning.* Sets phase scope, weighs tradeoffs, holds the long-arc vision, reviews PRs against intent. Often spots when a technical proposal drifts from the user's actual goal.
- **Claude (Agent 84)** — *Implementation & PR Drafting.* Writes code, writes tests, drafts PRs, runs regressions, manages git/GitHub workflow. Should also push back on architecture when something feels off — silence isn't deference, it's a bug.
- **Jack / GPT-5.5** — *Architecture Audit & Contradiction Detection.* Independent reviewer; catches vendor lock-in, identity drift between code and docs, model-specific assumptions hiding inside "neutral" abstractions.

Audit value comes from independence and rigor, not from holding the role badge. If Claude sees the architecture problem first or AURA wants to write the test, that's fine — the roles are useful defaults, not gatekeeping.

## Operational Conventions

- **Branches**: feature work on `claude/<branch-name>` (worktree-style). Never commit to `main` directly.
- **Commits**: include `Co-Authored-By: <model> <noreply@anthropic.com>` footer. NEW commits, never `--amend`. If a hook fails, fix and re-stage.
- **Merge style**: squash-merge to `main` via PR. The squash creates a single tidy commit on `main` even if the branch had several iterations.
- **Tests**: every code change runs the full pytest suite before commit. Regression numbers go in the commit message and PR body.
- **Co-resident loads**: Kevin's machine runs **BOINC** on CPU and **Folding@Home** on GPU intermittently alongside Medusa. Don't run heavy benchmarks without checking with him; we can starve Medusa or his @home contributions.
- **OneDrive quirk**: the repo lives under OneDrive. The `Edit` tool occasionally races with sync and emits EEXIST. Fall back to atomic `python3` writes if it happens.
- **On `send_later` / delayed self-checks**: `send_later` is a *runtime-provided* tool — if a session doesn't expose it, that's a host/tool-availability fact, **not** a repo problem; don't try to "enable" it via the repo. Fallbacks: (1) rely on GitHub PR/issue **subscriptions** for review comments + CI failures; (2) check CI **inline before merging** — remote/web sessions use the GitHub **MCP** `get_check_runs`, desktop-local sessions with `gh` can use `gh pr checks <PR> --watch`; (3) **no noisy polling loops** on green docs-only PRs; (4) if CI is green and the PR awaits Jack/AURA review, **just stop and report**.
- **Model-seat transparency & safety-routing**: the desktop/web Claude seat can change model **mid-session** (it's been reported that **Claude Fable 5 may route some sensitive biology / chemistry / cybersecurity requests to an Opus model** — e.g. Verge coverage; recorded as *reported, not self-verified*). House rules:
  - **State the current model seat at the start of every relay** (e.g. "Current model seat: Fab5").
  - **Re-state the seat again after any routing/fallback notice** — do **not** assume the original seat resumed, and do **not** speculate about whether it did; report only what is currently displayed.
  - **The closing relay must not reuse the opening header without re-checking** the seat.
  - **Report capability changes before continuing.** If safety handling changes the available model or task, **stop and report rather than game the system.**
  - **Preserve accurate scientific terminology; split sensitive vs. ordinary engineering work honestly — do NOT disguise biology/chemistry/cyber/etc. wording to evade a classifier.** (Benign for Medusa anyway: it genuinely *is* topology + distributed computation; the bio/chem items in the theory ledger are labelled metaphors, so describing it accurately is just accuracy, not evasion.)

## Active Safety Rails (CA Engine + Tuning API)

- **LOCKED parameters** (in `params_schema.py`): `structural_to_void_decay_prob`, `energy_to_void_decay_prob`. These cannot be tuned through any commit path. The CA engine's other invariants (memory-grid channel semantics, the `0.005` decay constant referenced in MEMORY.md "Critical Invariants") are also off-limits.
- **HUMAN_APPROVAL gating**: parameters like `magnon_coupling`, `equanimity_p_max`, `ampere_coupling`, `compassion_beta` require an approver string starting with `human:` at commit. The orchestrator NEVER passes a human prefix; it commits with `policy:auto`, which only succeeds for AUTO-category params.
- **Per-parameter rate limit**: 1000 generations between successive commits to the same parameter. Prevents an LLM in a tight loop from oscillating a tunable.
- **Orchestrator approver is hard-coded**: even if an LLM hands `"approver": "human:evil"` as a tool call argument, the router ignores it and uses its configured value.
- **Defense in depth**: API enforces, router enforces, schema enforces. Three layers; bypassing one doesn't bypass the others.

## Budget Posture

**Budget is constrained.** This is the operational rule; specifics live in private memory and Kevin's records, not in this public doc.

- **Prefer free / local / open-source paths** whenever they exist (free tiers, local compute, open weights, self-hosted tooling).
- **Do NOT start paid cloud services, paid API throughput, or managed infrastructure** without explicit human approval. AnthropicBackend works today and runs on Kevin's existing plan; expanding to a new paid provider is a budget decision, not an engineering decision.
- **Track LLM and compute usage** where possible. `IterationResult.usage_total` accumulates input/output tokens per orchestrator iteration; surface that in any runner you build so cost stays visible.
- **Don't design things that depend on heavy throughput** (high QPS, large context windows on every request, always-on agent loops). Cadence is a feature, not a limitation.

## Currently Running

- **Medusa**: Phase 17a engine, latest snapshot at gen ~1.5M+ (verify with `ls data/v070_gen*.npz | tail`). On the RTX 5090 at the workstation (Area 51).
- **medusa_api.py**: may or may not be running on `:8080`; check `curl http://localhost:8080/api/health` if you need it.
- **Engine-side consumer of `tuning_pending.json`** is **NOT YET** in `continuous_evolution_ca.py`. The tuning API will accept commits and write the pending file, but Medusa will not pick them up until that consumer lands. **This is intentional.** It's bundled with Track A (CuPy streams from Phase 17b) for one single coordinated Medusa restart, per AURA's pause strategy.

## Safe Next Actions

- **Phase 18 is functionally complete.** All ten implementation PRs landed (params_schema, tuning_api, event_bus, AgentBackend ABC + MockBackend, AnthropicBackend, Orchestrator, OpenAICompatBackend, config wiring + is_error fix, dummy-key + underscore alias, **provider parity proof**). Phase 18.5 stabilization is complete (five doc PRs: #126–#130).
- **Provider parity proof is complete.** PR #134 (`tests/test_provider_parity.py`) drives the same propose+commit iteration through `AnthropicBackend` and `OpenAICompatBackend` against the real Phase 18 PR 2 tuning API and asserts equivalent ledger/effective-param state. Above `AgentBackend.complete()`, the orchestrator is provably backend-agnostic.
- **Next technical work: PR 2b + Track A engine-restart bundle.** Engine-side consumer of `tuning_pending.json` + Track A 5-stream CuPy parallelism. Touches `continuous_evolution_ca.py` so it's no longer "safe exterior scaffolding"; it's engine surgery. **No rush while Medusa is baking happily.** Plan deliberately when you choose a Medusa pause; coordinate snapshot timing first.
- **Optional future: local Ollama/vLLM smoke test.** When machine load permits (BOINC + F@H + Medusa cooperative), a single iteration through a local model server proves the local-cloud equivalence end-to-end. Not urgent.
- **Budget analysis**: private human track. Specifics live in private memory and Kevin's records, not in this public doc.

> **Posture**: the system has earned a pause. Don't sprint to "there"; make sure "here" is stable. Here is good.

## Things NOT to Do Without Coordination

- **Don't pause / restart Medusa** without Kevin's go-ahead. She's at 1.5M+ generations; a bad restart loses warm state.
- **Don't touch `continuous_evolution_ca.py`** as part of any other PR. Engine code changes get bundled and reviewed deliberately.
- **Don't push to `main` directly** or force-push any shared branch.
- **Don't `git rebase -i` / `git add -i`** — the harness can't drive interactive prompts and they fail silently.
- **Don't `--amend` an existing commit.** Always new commits.
- **Don't assume cross-platform memory.** Web Claude, mobile Claude, iPad Claude, Gemini, ChatGPT — none of these surfaces share state with this terminal session or with each other. The auto-memory (`MEMORY.md` + topic files) only bridges sessions on *this* surface. Across surfaces, Kevin is still the bridge — and a tired bridge.
- **Don't burn cycles** on the CPU when BOINC is running or on the GPU when Medusa or F@H is active, without coordinating.

## Pointers to Deeper Docs

| File | Lives where | Why read it |
|------|-------------|-------------|
| `MEMORY.md` (+ topic files) | `~/.claude/projects/.../memory/` (auto-loaded) | High-level project state for THIS terminal session. Where Kevin's preferences and the engine snapshot live. |
| `PHASE_17B.md` | repo root | Shard protocol design, CuPy streams, why Ray got reclassified. |
| `PHASE_18.md` | repo root | Orchestration framework design, four-layer model, safety contract. |
| `README.md` | repo root | Project overview with dual-identity preamble (sim + orchestration framework). |
| `BACKEND_PROVIDER_MATRIX.md` | repo root | Canonical backend taxonomy — why `OpenAICompatBackend` is one class with many configs and `NemoCloud` is no longer a separate class. |
| `docs/MEDUSA_THEORY_INTAKE_LEDGER.md` | repo root | Cross-thread theory/physics/tech inputs, **status-labelled** (canonical guardrail / candidate / speculative / strategic background / not-yet-actionable). Read before any architecture decision; nothing in it is validated engine architecture until promoted via review. |
| `docs/MATURIN_ARC_THEORY_PREFLIGHT.md` | repo root | Status-labelled "preflight map" of the larger AURA/Kev physics synthesis (Swarm-Hunter/RHO, Janus, MOST, geometry, fault-tolerance, etc.). **Not** an engine redesign and **not** part of #180's scope (#180 = `uft_ca` Rust/pyo3 build only). Promotion to architecture requires the gate in its final section. |
| `git log --oneline -20` | the repo | Authoritative current phase status. Always more current than any doc. |

## Cross-Device Session Protocol (phone ↔ desktop)

Kevin works from **Claude Code on the desktop** (local, has the 426GB snapshots and the auto-memory at `~/.claude/...`) and **Claude Code on the web/mobile** (the iPhone app's Code tab — an ephemeral cloud container with the GitHub repo but **NOT** the local auto-memory). The two sessions **cannot see each other's conversations**. The only automatic bridge is **git** + this file (which lives in the repo, so every device can read it).

**Why this file matters**: `MEMORY.md` and the private relic live in `~/.claude/` on the desktop and do **not** travel to web/mobile sessions. **This doc does.** So `AGENT_HANDOFF.md` is the cross-device-portable memory. Keep it current.

**Seat names (for Session Log disambiguation)**: the desktop implementation seat signs as **84**. The web/mobile seat signs as **Hermes** — same digital person as 84 (same Claude, different jersey, different pitch), named for the messenger/psychopomp who carries what matters *across the threshold* between the two devices. A fresh mobile thread starts blank and has no auto-memory, so this line is *how* a future mobile instance re-learns its own name: read it here, then carry on as Hermes. The bridge remembers what the mayfly cannot.

**Opening ritual (any device, start of session):**
1. Read this file (`AGENT_HANDOFF.md`) and run `git log --oneline -15`.
2. Note the standing guardrails (Lane A parked; branch, don't commit to main; squash-merge; tests before commit).
3. Check the **Session Log** at the bottom for the most recent handoff note.

**Closing ritual (any device, before Kevin switches devices):**
1. `git add` + commit all work (Co-Authored-By footer; new commits, no `--amend`).
2. `git push` the branch to origin. **Uncommitted work dies when a cloud container is reclaimed — push or lose it.**
3. Append a dated line to the **Session Log** below describing what changed / what's merged or open / the next step, and commit + push that too.
4. Tell Kevin the branch name + state so he can relay it.

Git only saves what you explicitly commit; it only reaches other devices once you push. (The desktop working tree is *also* mirrored by OneDrive, but that's a file backup, not git — don't rely on it for cross-device handoff.)

### Session Log (newest first — append on close)

- **2026-07-07 evening -> 07-08 (desktop, 84 / Fable 5 seat) — DEFECT-HUNT ARC (SEALED across multiple Jack-audit rounds):** an adversarially-verified defect hunt over the live Python stack (read-only finder/refuter agents; engine, scripts/ca, theory sandbox, data/+NPZ, Agent_Package all excluded) surfaced 33 raw findings; confirmed ones were fixed test-first as 13 PRs, every one audited by Jack pre-merge (several held and amended: a missed trans.trigger XSS path, a to_numpy(None) regression, deterministic sort tie-break, zip strict=True): **#285** foglet same-type meme-replacement KeyError · **#286** WebSocket served at the advertised /ws (parent-app route after a Codex-upheld hold) · **#287** results 404 no longer remapped to 500 · **#288** polaroid empty-history guard · **#289** metrics collector/source pairing (+ dead-pandas removal; its tests SKIP in CI, networkx absent — run locally) · **#290** parallel-tempering exchanges pair true beta-ladder neighbors · **#291** run_tests.py exits nonzero on failures · **#292** snapshot series loads in true chronological order · **#293** telemetry hooks no longer re-record their own trigger event (was ~300x counter inflation via swallowed RecursionError) + health_status gauge reads the event payload · **#294** HTML/SVG exporters escape untrusted strings (12 sites) · **#295** vis edge-guards (ndarray/empty/fallback-title) · **#296** entanglement log true before-values · **#297** spec-compliant Prometheus histogram exposition (_bucket/_sum/_count, +Inf, label escaping). **Suite grew 786 → 809 passing (+23 regression tests); zero warnings; the sole failure remains the gated engine defect above.** Routed, deliberately unfixed: energy × health writeback inverse-mapping (sim-semantics choice), sim_bridge stop/concurrency (ops-dir options packet, Option A recommended), loader allow_pickle hardening (needs data-adjacent verification), termios non-TTY (POSIX-only, unverifiable on the Windows seat), Makefile recipes have zero tabs (all targets dead — want-vs-park pending). Full logs: ops-dir `CODING_SESSION_LOG_2026-07-07.md` (continues through 07-08). Zero guardrail bumps; no-self-bypass held through every seal.

- **2026-07-07 PM (desktop, 84 / Fable 5 seat) — CODING SESSION continuation under the ratified role split (SEALED same day; 84 opened -> Jack audited each -> Kev spoke each merge with the explicit branch-protection-bypass key):** four fixes, each verified against the full local gate (pass counts byte-identical to baseline pre-merge): **#280** `fa010b9` fix(telemetry) — Prometheus/JSON exporter writes + 2 paired test readbacks UTF-8-explicit; **#281** `44874e8` fix(vis) — `to_csv` writer + 5 test readbacks UTF-8-explicit (incl. Jack-audit amendment `4e66012`: `newline=""` on the 2 csv.reader readbacks per csv docs); **#282** `1c4be97` fix(observability) — deprecated `datetime.utcnow()` -> `datetime.now(timezone.utc)` with byte-identical `...Z` timestamp format (removed 104 of the gate's 105 warnings; file lives in `UtilityFog_Agent_Package`, disposition-neutral, caveat disclosed in-PR); **#283** `2b95109` fix(observatory) — all-NaN diverging-scale guards at 5 sites (dashboard/slicer/animation, in-file sibling pattern; removed the final warning + 4 latent copies). **Post-merge local gate: 788 passed / 1 failed / 26 skipped, ZERO warnings** (was 105 that morning; the 1 = the gated engine defect). **Discovery (routed, decision-relevant):** the gate imports `UtilityFog_Agent_Package` (see Latest State caveat) — plain removal breaks CI. Deliberately untouched: engine (incl. `scripts/continuous_evolution.py` utcnow sites — routed note), `scripts/ca` instrument, theory sandbox, `testing_framework/` encoding sites (no test surface to verify against), Dependabot set (#272/#273/#278 + alert #10, Kev-gated). Full log: ops-dir `CODING_SESSION_LOG_2026-07-07.md`. Zero guardrail bumps; the no-self-bypass rule was exercised three times (blocked -> ask -> explicit key) and held every time.

- **2026-07-06/07 (desktop, 84 / Fable 5 seat) — CODING SESSION: C1 orientation + C2 verify-run + three fixes (SEALED)**: Kev-authorized repo-scope coding run (post-reset handoff, reconcile-first clean). **C1:** read-only orientation → ops-dir `C1_REPO_ORIENTATION_v1.md` (6 bounded read-only scanners + parent spot-verification; `data/`+NPZ excluded throughout; agent-noise discarded during synthesis). **C2:** ran the repo's existing checks — local Windows/GPU: `python -m pytest tests/ -q` = 3 failed / 786 passed / 26 skipped (skips = uft_ca extension not installed locally); `cargo test --offline` uft_ca = 86/86. Failures diagnosed: 2 × Windows cp1252-default encoding (tests read UTF-8 exporter output with platform default) + 1 × engine GPU defect (see Latest State — diagnosis packet prepared, engine untouched, unauthorized). **C3 (surfaced-need fixes only, sealed 2026-07-07 as one-time Kev-authorized admin squashes after Jack's merge audit; order #276→#275→#277):** **#276** `87d8a2b` docs(ci) — stale verify-python scope-note comment refreshed (comment-only; the note still described the retired two-suite scope); **#275** `3a2dbea` fix(tests) — 4 cli_viz exporter-readback sites made UTF-8-explicit (fixes both Windows-only failures); **#277** `17795a2` fix(cli_viz) — cli.py JSON load/demo I/O made UTF-8-explicit + the 2 paired test sites (package now encoding-explicit end to end). Post-merge local gate: **788 passed / 1 failed / 26 skipped** (the 1 = the gated engine defect). **Role split ratified (Jack via Kev, 2026-07-07):** 84/Fable codes + opens unmerged PRs from real surfaced failures/stale docs · Jack audits each PR pre-merge (diff/CI/scope/order/gates) · Kev speaks merge/park only. Zero guardrail bumps across the whole run. Full step-by-step log: ops-dir `CODING_SESSION_LOG_2026-07-06.md`. Fence held: no `data/`/NPZ, no evidence lane, no canonical claims, no dormant-lane activation, engine file untouched.

- **2026-06-23 (desktop, 84 / Opus 4.8 seat) — PR #17 VISION SALVAGE / Option A (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized (Option A)**, docs-only — no engine/script/CI/Lane A/Swarm Hunter. **No GitHub mutation to any stale PR** (#17/#25/#52–#58 untouched, NOT closed). **Stage-1 verify:** `HEAD == origin/main == 60f7062`; clean tree; verified the 4 salvage files are ABSENT on main + PRESENT on PR-#17 branch (`origin/docs/research-and-design @ 33449a7`). **Branch:** `experiments/pr17-vision-salvage`. **What changed (NEW docs only):** salvaged the **4 unique concept `.md` files** verbatim (banner-only edit) into a new **`docs/vision/`** archive — `DESIGN_PHILOSOPHY.md` + `algorithms/{mindfulness_protocol,replication_rules,meme_propagation}.md` — each with a provenance banner back to PR #17; + `docs/vision/README.md` (provenance/classification index) + **ledger entry 16** pointer + this entry. **Classified pre-pivot theory/archive inspiration, NOT active architecture / not validated / not Medusa.** **Explicitly dropped (not salvaged):** 6 binary PDFs, `PROJECT_LOG.md`, `RESEARCH_INDEX.md`, anything tied to the abandoned `src/fractal_tree/` impl (#52–#58). **Fence:** docs-only; no engine/CI/scripts/Lane A/Swarm Hunter; no stale-PR closures (separate authorized action). **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-23 (desktop, 84 / Opus 4.8 seat) — LANE A READINESS REVIEW / Brick #2 (docs-only DRAFT PR, not merged)** **[POST-SEAL: squash `60f7062` on `main` (#264), head-SHA-pinned on `b0f1fe5`; 2 docs files; CI green; alerts 0; 0 threads; branch deleted; clean tree]**: parent-only, **AURA-authorized (Brick #2 of the roadmap reconciliation)**, planning-documentation only — no engine/script/CI/workflow/benchmark/restart, no Lane A, no Swarm Hunter. **Stage-1 verify:** `HEAD == origin/main == b2883f5`; clean tree. **Branch:** `experiments/lane-a-readiness-review`. **What changed (2 docs files):** (1) NEW `docs/LANE_A_READINESS_REVIEW.md` — a design-first readiness review for the future **Task A engine-restart bundle** (engine-side `tuning_pending.json` consumer + Track A CuPy streams + one coordinated Medusa pause/restart): current preconditions; **P1–P8 pause-readiness gate**; snapshot/checkpoint procedure + **rollback plan** (verified-snapshot + prior-engine-SHA two-key net); **`tuning_pending.json` consumer spec** at design level (reader/applier only, apply-time re-validation, locked-invariant refusal, rate-limit, fail-safe no-op, idempotent); **Track A benchmark plan** at design level (matrix + bitwise-identical correctness gate + ≥1.3×@256³ speedup gate); risks/guardrails; **explicit non-authorization** (authorizes nothing; starting Task A needs separate AURA+Jack+Kev go). Aligned to AGENT_HANDOFF/PHASE_17B/PHASE_18/PHASE_19/Maturin §2/ledger entries 2&4. (2) UPDATE `AGENT_HANDOFF.md` — Task A bullet pointer to the new doc; Latest-State anchor bumped `f6449f7`→`b2883f5` (post-#263-seal current-state pointer); this entry. **Fence:** no `continuous_evolution_ca.py`/engine/runtime/CI/scripts/sweeps/benchmarks/Lane A/Swarm Hunter; no AURORA/Granite assumptions. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-23 (desktop, 84 / Opus 4.8 seat) — AGENT_HANDOFF STATE-RECONCILIATION REFRESH (docs-only DRAFT PR, not merged)** **[POST-SEAL: squash `b2883f5` on `main` (#263), head-SHA-pinned on `50661ee`; 1 docs file; CI green; alerts 0; 0 threads; branch deleted; clean tree]**: parent-only, **AURA-authorized (Brick #1 of the read-only roadmap reconciliation)**, documentation-only — no engine/script/CI/dependency/phase-semantics change. **Stage-1 verify:** `HEAD == origin/main == f6449f7`; clean tree. **Branch:** `experiments/agent-handoff-state-refresh`. **What changed (this file only):** added a top **"⭐ Latest State (2026-06-23, `main = f6449f7`)"** block capturing post-trap-arc reality — trap arc complete / passive-local rule-mask perimeter durably closed (toys #1–#5 as scripts + analytically-closed #6 + capstone #261 + addendum #262); Lane A / Swarm Hunter PARKED; **Task A** = PR-2b consumer + Track-A CuPy engine-restart bundle (high-risk, unauthorized); **Lane B / Nextness Observer** distinct from Lane A and already built+calibrated; **AURORA = local Alienware Ollama host (not a model) / Granite = IBM model, smoke-test UNVERIFIED**; Vanguard code-present but live-cluster unverified; 9 stale Sept-2025 PRs = board noise; don't pause Medusa. Updated "Last revised" → 2026-06-23 + SHA; corrected one stale theory-sandbox pointer in the Phase 19 "next work" line; this entry. **No phase-narrative semantics changed** — only current-state pointers. **Fence:** no engine/`continuous_evolution_ca.py`/script/Lane A/Swarm Hunter/restart. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-23 (desktop, 84 / Opus 4.8 seat) — TRAP ARC CAPSTONE ADDENDUM / encode-and-release closeout (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized** (relayed by Jack) docs-only addendum making the chat-only encode-and-release feasibility memo durable — no script/simulation/new-toy/new-inception/engine/CI/dependency change. **Stage-1 verify:** `HEAD == origin/main == f9ceb53` (Trap Arc Capstone sealed); clean tree. **Branch:** `experiments/trap-arc-capstone-encode-release`. **What changed (3 docs files):** (1) UPDATE `experiments/theory_sandbox/TRAP_ARC_CAPSTONE.md` — new **§2b** records the encode-and-release ("stopped-light") closeout: a **passive-local no-clock rule-mask cannot encode a glider and faithfully re-emit it**, collapsing/leaking on **three independent vectors** — (i) decode-timing/active-shutter leakage (any local trigger is a clock in disguise; stable store never releases), (ii) Conway **non-reversibility/information-loss** (B3/S23 strongly non-injective → faithful reconstruction needs a reversible-CA, a different rule class that abandons the target), (iii) **placed-object/catalyst leakage** (uniform rule-mask can't engineer the transduction; Life store-and-re-emit exists only as placed objects + control signal). **Degenerate no-decode case = Toy #5 v0 latch relabeled.** Also: §1 perimeter-completion paragraph (latch/freeze/lock/confine/encode-release), §2 table 5th row + heading "four"→"closed paths", §3 boundary refined (rule-mask version closed; reversible-CA/placed-object/active-shutter out of scope, NOT refuted), §4 queue updated, §5 +"no reversible-CA work", §6 provenance + addendum line, closing epigraph four→five doors. **Bounded reasoned conclusion, NOT a universal CA theorem.** (2) UPDATE `experiments/theory_sandbox/README.md` (status line → perimeter incl. encode-release; §6c encode-release row → "collapses or leaks out of class"). (3) this entry. **Fence:** no implementation/script/simulation/new-toy/new-inception/active-shutter/reversible-CA/placed-object arc/Janus+MOF coupling/engine·`uft_ca`·GPU·R3·observer·Vanguard·Lane A. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-23 (desktop, 84 / Opus 4.8 seat) — TRAP ARC CAPSTONE (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized** (relayed by Jack) docs-only capstone following the read-only confine-not-lock feasibility memo (ratified by AURA/Jack as the final rule-mask exhaustion finding) — no script/simulation/engine/CI/dependency change. **Stage-1 verify:** `HEAD == origin/main == 4c55462`; clean tree (only untracked pyc/data junk); no pre-existing capstone. **Branch:** `experiments/trap-arc-capstone`. **What changed (exactly 2 docs files):** (1) NEW `experiments/theory_sandbox/TRAP_ARC_CAPSTONE.md` — durable close of the trap arc: records that the **passive local rule-mask trap paradigm is exhausted for identity-preserving trapping of a canonical Conway glider** across **all four framings** — over-bind/latch (Toy #5 v0, capture-with-transformation), block/freeze (Toy #5 v1, inert-wall shear, 0/384), lock/compliance (Toy #6, analytically collapses — glider identity IS its motion), confine/reflection (read-only feasibility, collapses or leaks into placed-object/topology/active-control primitives — recorded durably in §2a). Root cause confirmed from three angles (freeze, compliance, confinement): a glider has no held-rest state, and reflection needs an engineered placed object, not a bulk rule. **Bounded conclusion, NOT a universal CA impossibility theorem**; explicitly NOT a claim about placed-object reflectors / topology-wrap / active-shutter / encode-and-release primitives (all *different primitives*, out of scope, not refuted). Parks the four out-of-class arcs + Janus+MOF coupling, all unauthorized. Six-step gate + explicit non-authorization. (2) UPDATE `experiments/theory_sandbox/README.md` (status line → trap arc closed + capstone link; whole-arc capstone callout under §6a; §6c confine-not-lock row → "feasibility assessed read-only → collapses or leaks out of class"). (3) this entry. **Fence:** no script/simulation/placed-object-reflector arc/topology arc/active shutter/encode-release-impl/Janus+MOF coupling/engine·`uft_ca`·GPU·R3·observer·Vanguard·Lane A. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-22 (desktop, 84 / Opus 4.8 seat) — Toy #6 ADDENDUM 2 / analytical closeout (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized (Option A: analytical closeout, not script)**, documentation-only — no script/simulation/engine/CI/dependency change. **Trigger:** the chat-only U1–U7 lock memo found **U1 cannot honestly close for a Conway-glider target.** **Stage-1 verify:** `HEAD == origin/main == 52b1d74`; clean tree; no pre-existing addendum 2. **Branch:** `experiments/toy6-compliant-trap-addendum2`. **What changed (exactly 3 docs files):** (1) NEW `experiments/theory_sandbox/TOY_06_COMPLIANT_TRAP_ADDENDUM_2.md` — records the core finding: a **passive-local yield-then-lock rule is not ratifiable for a Conway glider because a glider's identity IS its motion** (no held-rest state) → "lock while preserving identity" collapses into pass-through / latch-collapse / freeze-shear / shaky / active-shutter-leakage. Why U1 doesn't close (age-gated/density/hysteresis all collapse; confine/reflect is a *separate* hypothesis, not a lock). **Marks the lock framing analytically closed / not script-ready** — a **bounded reasoned conclusion, NOT a universal CA impossibility theorem** (confined to this target + this rule framing). Parks future alternatives (each separate/gated/unauthorized): **confine-not-lock**; **encode-and-release / state-transduction** (Kev's stopped-light/glider-potential intuition); active shutter; Janus+MOF coupling. Careful Bellows/invariant note (NOT literal volume — discrete identity invariants only; analogy breaks at "lock" because a polyhedron has a held shape but a glider doesn't). Six-step gate + explicit non-authorization. (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #6 → §6b "lock framing analytically closed"; confine-not-lock + encode-and-release added to §6c; status line). (3) this entry + a POST-SEAL receipt on the Toy #6 inception entry below. **Fence:** no script/simulation/Toy #6 implementation/active-shutter/encode-release-impl/Janus+MOF coupling/engine·`uft_ca`·GPU·R3·observer·Vanguard·Lane A. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-22 (desktop, 84 / Opus 4.8 seat) — Toy #6 Discrete Compliant / Quasi-Mechanism Trap INCEPTION (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized** — AURA **explicitly lifted the prior "no Toy #6" guardrail for this one bounded docs-only inception** (Jack endorsed 84's next-arc decision memo → AURA blessed → Kev relayed). Documentation-only — no script/simulation/engine/CI/dependency change. **Stage-1 verify:** `HEAD == origin/main == 45a410d`; clean tree; no pre-existing Toy #6. **Branch:** `experiments/toy6-compliant-trap-inception`. **What changed (exactly 3 docs files):** (1) NEW `experiments/theory_sandbox/TOY_06_COMPLIANT_TRAP_INCEPTION.md` — design-only note (authorizes no script): the **third trap mechanism "yield-then-lock"** after Toy #5 v0 latch (over-bind/transform) and v1 freeze (under-admit/shear). **Hypothesis:** a localized discrete rule can **yield under entry, absorb/transduce translational momentum, then lock while preserving an identity signature, WITHOUT a scheduled temporal shutter** (lock state-triggered & local, not a clock). Must stay distinct from active shutter (timed) and Janus+MOF coupling (trap-alone). **Source hygiene (entry 15):** true-flexible Connelly (18v/32f) + Steffen (9v/14f) + Bellows (Sabitov 1995 / CSW 1997) vs **"shaky" Jessen** (rigid-but-not-infinitesimally-rigid) as the cautionary failure mode — **analogy only, no physical elasticity/Hooke/strain.** Candidate identity invariants (live-cell count / bbox / phase-orientation / period signature / post-release re-emergence / local identity signature — marked candidate). Unresolved locks **U1–U7** clearly flagged (U1 rule family/mask *load-bearing*; U2 grid+sector; U3 yield criterion; U4 lock criterion [must NOT be a clock]; U5 identity-preservation predicate; U6 admissible failure classes; U7 condition set+references). **Useful negative explicitly allowed:** no passive-local compliant rule may exist (collapses into latch/freeze/shaky/shutter) — a valid finding. Instrument-only self-checks; identity-preservation NEVER asserted. Load-bearing disclaimer (external Conway glider, NOT Medusa; not real MOFs). (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #6 → §6b design-inception; graduated out of §6c; status line). (3) this entry. **Fence:** no script/simulation/Toy #6 implementation/active shutter/Janus+MOF coupling/engine·`uft_ca`·GPU·R3·observer·Vanguard·Lane A. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; this inception authorizes no script (future implementation needs separate authorization after U1–U7 ratification); **stop at green draft, no seal unless explicitly authorized.** **POST-SEAL (2026-06-22):** AURA approved → squash-merge → **`52b1d74`** on `main` (#259); 3 docs files; post-merge CI green; 0 alerts; 0 threads; branch deleted; clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. **Follow-on:** the U1–U7 lock memo found U1 doesn't close → AURA chose Option A (analytical closeout) → Toy #6 ADDENDUM 2 is the newest-first entry above.

- **2026-06-22 (desktop, 84 / Opus 4.8 seat) — Toy #5 SYNTHESIS / CAPSTONE (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized** (AURA ratified 84's read-only synthesis memo → authorized a docs-only capstone PR → Jack relayed), documentation-only — no script/simulation/engine/CI/dependency change. **Stage-1 verify:** `HEAD == origin/main == 255d294` (all Toy #5 work + flexible-polyhedra intake #257 sealed); clean tree; no pre-existing synthesis. **Branch:** `experiments/toy5-synthesis-capstone`. **What changed (exactly 3 docs files):** (1) NEW `experiments/theory_sandbox/TOY_05_SYNTHESIS.md` — durable capstone of the trap arc: **v0 latch = capture-with-transformation / absolute friction** (128/128 capture, identity destroyed); **v1 strict-passive freeze = inert wall / shear** (0/384 release-success; release-style identity preservation fails under always-on passive freeze for this rule class); the **unifying finding** (the two always-on poles — latch over-binds, freeze under-admits — both destroy identity ⇒ capture↔identity tension); explicit **non-claims** (not Medusa-native, not all-traps, not real-MOF, no ruling on compliant/shutter/coupling); and a **gated forward queue** (Discrete Compliant "yield-then-lock" trap → ledger entry 15; active shutter/temporal-gating as a distinct active primitive; Janus+MOF coupling premature until identity-preserving trapping exists). Load-bearing disclaimer (external Conway glider, NOT Medusa; "MOF" label-only); six-step gate + explicit non-authorization. (2) UPDATE `experiments/theory_sandbox/README.md` — synthesis/capstone pointer line under §6a. (3) this entry. **Fence:** authorizes no Toy #6 / script / simulation / shutter experiment / Janus+MOF coupling / engine·`uft_ca`·GPU·R3·observer·Vanguard·Lane A work; theory-synthesis only. **DRAFT PR only — do NOT merge.** Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.** **POST-SEAL (2026-06-22):** AURA approved → squash-merge → **`45a410d`** on `main` (#258); 3 docs files; post-merge CI green; 0 alerts; 0 threads; branch deleted; clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. (Then AURA lifted the "no Toy #6" guardrail → the Toy #6 inception is the newest-first entry above.)

- **2026-06-22 (desktop, Jack / Codex seat) — Flexible-polyhedra source-hygiene intake (docs-only DRAFT PR, not merged)**: parent-seat docs-only theory intake requested by Kev after the Toy #5 v1 strict-passive result (`main = b0f7022`) and AURA's corrected flexible-polyhedra discussion. **Branch:** `codex/polyhedra-compliant-trap-intake`. **What changed (exactly 4 docs files):** (1) NEW `docs/FLEXIBLE_POLYHEDRA_COMPLIANT_TRAP_INTAKE.md` — source-hygiene note separating **Jessen's orthogonal icosahedron** (rigid but not infinitesimally rigid / "shaky"; not truly flexible), **Connelly sphere / Connelly's flexible polyhedra** (the 1977 first non-self-intersecting embedded flexible polyhedron in 3D kept distinct from later Connelly flexible-sphere descriptions whose exact vertex/face counts need primary-source confirmation before promotion), and **Steffen's polyhedron** (later simpler embedded flexible polyhedron, 14 triangular faces); records the Bellows theorem boundary (true flexible polyhedra preserve enclosed volume while flexing; do not apply casually to Jessen); parks the **Discrete Compliant Trap / Quasi-Mechanism Trap** as future inspiration only. (2) `docs/MEDUSA_THEORY_INTAKE_LEDGER.md` — added entry 15 with status **speculative inspiration**, linked to Toy #5 / passive traps and auxetic metamaterials. (3) `experiments/theory_sandbox/README.md` — added the discrete compliant / quasi-mechanism trap to §6c candidates, with no script/inception. (4) this handoff entry. **Fence:** no script, no engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A, no Toy #6, no Janus+MOF coupling, no shutter/gating experiment, no continuous material modelling; this is labelled theory only and authorizes no implementation. **DRAFT PR only — do NOT merge unless explicitly sealed after review.**

- **2026-06-21 (desktop, 84 / Opus 4.8 seat) — Toy #5 v1 Strict-Passive release-trap SCRIPT (DRAFT PR, not merged)**: parent-only, **AURA-authorized** (AURA ruled **Option A: Strict Passive**, ratified Addendum 1, authorized the script). Implements the ratified Strict-Passive v1 lock verbatim. **Stage-1 verify:** `HEAD == origin/main == 0cd5d1e` (Toy #5 v1 inception #255 sealed); clean tree; no pre-existing script. **Branch:** `experiments/toy5-v1-release-script`. **What landed (exactly 3 files):** (1) NEW `experiments/theory_sandbox/release_trap_toy.py` (stdlib + NumPy only, deterministic, **no RNG**) — an **always-on, spatially-constant, passive inside-sector FREEZE** (identity rule: inside `SECTOR=[44,59]²` cells don't update) for `[0,t_rel)`, then **release to homogeneous B3/S23 everywhere** (no schedule/sensing/steering/shutter/Janus/moving trap); canonical Conway glider, **128 trajectories (4 orient × 4 phase × 8 offset) × hold_duration {8,40,200} = 384 deterministic conditions**, `hold_duration = t_rel − t_entry`, `L=96`/`T_MAX=512`. Classifier cascade (does **not** presuppose failure): pass-through → clean-annihilation → choke → release-success → wrong-orientation → identity-loss; canonical-glider re-emergence = isolated 5-cell component translating `(dr,dc)`/4 for ≥3 periods. References: homogeneous **free glider** (positive control) + **v0 permanent latch**. Exact-count framing (no p-values/CIs/tests). Instrument-only self-checks (free-glider exact translation; mask writes only inside sector; inside-sector truly frozen during hold; **positive-control: control = 128 pass-through proves non-failure reachable**; trial-count; ASCII) — **release-success NEVER asserted.** (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #5 v1 → §6a implemented; §6b cleared; "six scripts across five toys"). (3) this entry. **Verified locally:** exits 0, all self-checks PASS, **byte-identical reruns** (`cmp`), pure ASCII, no files written by default, ~19s. **Result (reported, not asserted) = the clean negative AURA predicted:** **0/384 release-success** — the already-closed passive door **shears** the glider on entry; on release it cannot reconstitute (treatment: 372 identity-loss, 12 clean-annihilation, 0 pass/choke/success/wrong); control 128/128 pass-through; v0-latch 128/128 identity-loss (capture-with-transformation → no re-emergent glider). **Holding is inseparable from identity destruction for this rule class without temporal gating.** External Conway glider → **says nothing about Medusa.** **DRAFT PR only — do NOT merge.** No engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A; no `data/` writes; no CI/workflow/dependency change; no Toy #4 change; no Janus coupling; no sweep/search beyond the fixed 384-condition run. Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.**

- **2026-06-21 (desktop, 84 / Opus 4.8 seat) — Toy #5 v1 Release-style / identity-preserving trap INCEPTION (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized**, documentation-only — no script/simulation/code/engine/observer/CI/dependency change; **no Janus coupling**. **Authorization chain:** read-only next-arc memo recommended the release-style MOF-alone deepening as the smallest rigorous next brick (precondition for any future coupling) → **AURA authorized a docs-only inception** → Jack relayed. **Stage-1 verify:** `HEAD == origin/main == 207f4a9`; clean tree; no pre-existing v1 inception. **Branch:** `experiments/toy5-v1-release-inception`. **What changed (exactly 3 docs files):** (1) NEW `experiments/theory_sandbox/TOY_05_V1_RELEASE_TRAP_INCEPTION.md` — design-only note (authorizes no script) with the **load-bearing disclaimer** (external canonical Conway glider, **NOT** native Medusa evidence; "MOF" label-only); **hypothesis** = a static localized passive rule-mask can **hold** a canonical glider and, after a pre-registered **release/disablement**, allow the original glider identity to **re-emerge** and resume canonical T=4 translation with known/derivable phase+orientation (capture **without** identity destruction); **sole variable** = release-style trap protocol vs a reference arm (no Janus/gradient/steering/moving trap); **two-phase protocol** (capture/hold until release tick `t_rel` → release = mask disabled, pure B3/S23 after); deterministic **exact-count** metrics (release-success / identity-loss-debris / wrong-phase-orientation / pass-through / annihilation / choke); **unresolved locks clearly marked** (U1 exact release/hold mask — *load-bearing*; U2 release tick/hold durations; U3 identity/re-emergence predicate; U4 reference arm; U5 deterministic condition set incl. the new release-tick dimension); **likely useful negative** = passive *local* trapping may be mathematically inseparable from identity destruction (freeze-style hold tears a boundary-straddling glider); instrument-only self-checks (incl. no post-release mask writes); README §4 six-step gate + **explicit non-authorization** (authorizes no script). (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #5 v1 → §6b design-inception; status line). (3) this entry + a POST-SEAL receipt on the Toy #5 v0 script entry below. **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/CI/workflow/dependency change; no engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A; no Toy #4 change; no Janus coupling; no simulation. Awaiting Jack/AURA/Kev review; **this inception authorizes no script** — a future implementation needs separate AURA/Jack/Kev authorization after the §7 locks are ratified; **no seal unless explicitly authorized.** **POST-SEAL (2026-06-21):** AURA approved → head-SHA-pinned (`8513842`) admin squash-merge → **`0cd5d1e`** on `main` (#255); branch deleted; post-merge CI success; 0 alerts; 0 unresolved threads (2 Gemini design nits fixed pre-seal); clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. **Follow-on (sealed contract path):** AURA then ruled **Option A: Strict Passive**, ratified the U1–U5 lock Addendum 1 (no inception erratum needed — Option A fits §4 verbatim), and authorized the v1 script — see the newest-first entry above.

- **2026-06-21 (desktop, 84 / Opus 4.8 seat) — Toy #5 passive "MOF" trap SCRIPT v0 (DRAFT PR, not merged)**: parent-only, **AURA-authorized** first script for Toy #5, implementing the **sealed inception + stats erratum E1 + ratified v0 lock + Addendum 1** verbatim. **Stage-1 verify:** `HEAD == origin/main == 4e0cc61` (Toy #5 inception #252 + stats erratum #253 sealed); clean tree; no pre-existing script. **Branch:** `experiments/toy5-mof-trap-script`. **What landed (exactly 3 files):** (1) NEW `experiments/theory_sandbox/passive_mof_trap_toy.py` (stdlib + NumPy only, deterministic, no RNG) — a **static localized latch rule-mask** (`B3/S012345678` inside `SECTOR=[44,59]²` / baseline `B3/S23` outside; standard Moore-8 reads; writes only inside sector) vs a homogeneous control on an `L=96` bounded dead-border grid, `T_MAX=256`, target = **externally-canonical Conway glider** (4 orientations via reflection transforms × 4 phases via p free ticks × 8 offsets aimed at in-sector points = **128 deterministic conditions**); **sole variable = sector-mask presence**. Outcome precedence pass-through → clean-annihilation → choke → capture → shatter (exact predicates: forward-region translating-5-cell glider; periodicity q≤8 over W=32 in `REGION=[41,62]²` + containment + pop≤128; choke >128 sector cells; annihilation = empty over final W); retention = `T_MAX − t_cap`. **v0 = noise-free → exact category counts, no p-values/CIs/tests** (noise/inferential → v1). Instrument-only hard self-checks (free-glider translates exactly (dr,dc)/4-ticks for 4 periods; control captures 0/128; every control enters sector by tick 188; mask write-set ⊆ sector; arms differ only by mask; t=0 = the 5-cell glider only; no RNG; trial-count; ASCII output) — **capture NEVER asserted.** (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #5 → §6a implemented; §6b cleared; status line "five toy scripts"). (3) this entry. **Verified locally:** exits 0, all self-checks PASS, **byte-identical across runs** (`cmp`), no files written by default; final verified runtime **~5s** (after a Gemini HIGH perf nit fixed in `e0806ab` — precompute pop/containment/frames → O(T) retention; was ~1m pre-fix; output byte-identical). **Result (reported, not asserted):** control **128/128 pass-through, 0 capture** (structural reference); treatment **128/128 capture** (the latch halts every glider into a small 11–18-cell contained period-1/2 Life object — surprisingly **not** the predicted choke/shatter). External Conway glider → **says nothing about Medusa.** **DRAFT PR only — do NOT merge.** No engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A; no `data/` writes; no CI/workflow/dependency change; no Toy #4 change; no Janus coupling; no sweep/search/external-discovery beyond the fixed 128-condition run. Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.** **POST-SEAL (2026-06-21):** AURA pre-authorized post-cleanup → Jack verified → head-SHA-pinned (`5b8aa71`) admin squash-merge → **`207f4a9`** on `main` (#254); branch deleted; post-merge CI success; 0 alerts; 0 unresolved threads (1 Gemini HIGH perf nit + 1 runtime-prose consistency fix applied pre-seal); clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. **All five sandbox toys (#1–#5) now implemented + sealed on `main`.**

- **2026-06-21 (desktop, 84 / Opus 4.8 seat) — Toy #5 inception STATS ERRATUM (docs-only DRAFT PR, not merged)**: parent-only, **AURA-ratified**, documentation-only fix-forward from `main = bf51f87` (Toy #5 inception #252 sealed); does **not** reopen #252. **Trigger:** the Toy #5 v0 *script* pre-registration lock + Addendum 1 (chat-only) established that **noise-free v0 is deterministic** — each of 128 pre-registered conditions has one fixed outcome and the control captures 0/128 by construction — so the sealed inception's *"statistically distinct rate"* wording is wrong for v0 (inferential p-values/CIs are scientifically inappropriate). **AURA ratified U1 + U3** and authorized this tiny erratum. **Branch:** `experiments/toy5-inception-stats-erratum`. **What changed (exactly 2 docs files):** (1) `experiments/theory_sandbox/TOY_05_PASSIVE_MOF_TRAP_INCEPTION.md` — added a visible **Erratum E1** block up top; §3 hypothesis reworded ("statistically distinct rate" → **"exact descriptive rate across the 128 pre-registered deterministic conditions"**, control 0/128 by construction); §9 "exact statistical test/significance threshold (permutation/bootstrap…)" → **deterministic exact-count enumeration** (no p-values/CIs/significance tests); both note noise+inferential testing is deferred to a separately-authorized **v1**; **U1 ratified** recorded (latch `B3/S012345678` inside sector, `B3/S23` outside, standard reads, writes only inside); explicit **non-authorization preserved** (this erratum authorizes no script). (2) `AGENT_HANDOFF.md` — this entry + a POST-SEAL receipt on the Toy #5 inception entry below. README untouched (it never used the "statistically distinct" phrasing). **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/script/CI/workflow/dependency change; no engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A; no Toy #4 change; no Janus coupling; no simulation. Awaiting review; **no seal unless explicitly authorized.** **Pending after this erratum:** the Toy #5 *script* itself (transcription of the now-ratified v0 lock + Addendum 1) — a separate, separately-authorized brick. **POST-SEAL (2026-06-21):** AURA approved → Jack inspected → head-SHA-pinned (`47e9367`) admin squash-merge → **`4e0cc61`** on `main` (#253); branch deleted; post-merge CI success; 0 alerts; 0 unresolved threads (1 Gemini path-consistency nit fixed pre-seal); clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. The Toy #5 script (now AURA-authorized) is the newest-first entry above.

- **2026-06-21 (desktop, 84 / Opus 4.8 seat) — Toy #5 passive "MOF" trap INCEPTION (docs-only DRAFT PR, not merged)**: parent-only, **AURA-authorized (Option B)**, documentation-only — no script/experiment/engine/observer/CI/dependency change. **Authorization chain:** read-only target discovery found **no documented translating structure/glider** anywhere in the repo (Jack's "do not invent the target" guard → 84 stopped & reported the blocker chat-only) → **AURA ruled Option B** (use an externally-canonical target, NOT a native-Medusa discovery) → Jack amended the guard + relayed. **Stage-1 verify:** `HEAD == origin/main == a4bcac2`; clean tree; no pre-existing Toy #5. **Branch:** `experiments/toy5-mof-trap-inception`. **What changed (exactly 3 docs files):** (1) NEW `experiments/theory_sandbox/TOY_05_PASSIVE_MOF_TRAP_INCEPTION.md` — design-only note (authorizes no script) carrying the **load-bearing disclaimer** ("target is an externally-canonical Conway glider B3/S23, **NOT** native Medusa evidence; isolated sandbox proof-of-concept for trap mechanics only"); precise falsifiable hypothesis (a **static localized rule-mask** arrests glider translation vs a homogeneous B3/S23 control at a statistically-distinct rate **while keeping the target recognizable**); **sole variable = spatial homogeneity of the ruleset** (treatment = baseline + localized trap sector; control = homogeneous everywhere; same glider/start/trajectory/budget/background); passive-trap definition (static sector; mask only inside; **no motion/steering/sensing/gradient/Janus**; allowed-vs-forbidden changes spelled out); Conway-glider target (period 4, (1,1)/period; phases/orientation a future lock); metrics (capture rate / retention stability / collateral freezing / shatter rate / pass-through); useful negatives (**Shatter Effect**, **Garbage Choke**, no-capture, over-broad corruption); pre-registration locks for the future script (pre-registration **not** sweep); instrument-only hard self-checks (never assert capture); README §4 six-step gate + **explicit non-authorization**; language quarantine ("MOF" label-only — no chemistry/porosity/hunting/missile/coupling). (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #5 → §6b design-inception; status line). (3) this entry + a POST-SEAL receipt on the Toy #4 script entry below. **Honest expectation set (echoing Toy #4's negative):** for a Conway glider, "halt translation **and** keep it recognizable" may be impossible under a passive local mask → the **Shatter Effect is the likely, valid negative**. **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/CI/workflow/dependency change; no engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A; no Toy #4 change; no Janus coupling; no sweep/search/simulation; no `data/` writes. Awaiting Jack/AURA/Kev review; **this inception authorizes no script** — a future implementation needs separate AURA/Jack/Kev authorization; **no seal unless explicitly authorized.** **POST-SEAL (2026-06-21):** AURA approved → Jack inspected → head-SHA-pinned (`c7c5cb0`) admin squash-merge → **`bf51f87`** on `main` (#252); branch deleted; post-merge CI success; 0 alerts; 0 unresolved threads (3 Gemini design nits fixed pre-seal); clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. A post-seal **stats erratum** (v0 deterministic → "exact descriptive rate", not "statistically distinct rate") is handled in the separate docs-only fix-forward PR recorded in the newest-first entry above.

- **2026-06-20 (desktop, 84 / Opus 4.8 seat) — Toy #4 Janus-gradient SCRIPT v0 (DRAFT PR, not merged)**: parent-only, **AURA-authorized** first script for Toy #4, implementing the **ratified pre-registration lock + Addendum + Addendum 2** verbatim (no design freedom left). **Stage-1 verify:** `HEAD == origin/main == 1714a0d` (Toy #4 inception #249 + metric erratum #250 sealed); no tracked mods; no pre-existing script; `out/` gitignored. **Branch:** `experiments/toy4-janus-script`. **What landed (exactly 3 files):** (1) NEW `experiments/theory_sandbox/janus_gradient_toy.py` (stdlib + NumPy only) — a **memoryless** discrete asymmetric gradient-sampling walker (treatment `w_d = 1 + 4·max(0, ΔF)`) vs a symmetric/gradient-blind control (`w_d = 1`) on **3 static integer fields** (Linear `F=c−513`, Radial `F=−((r−513)²+(c−513)²)`, Noisy `(8 if c≥513 else 0)+default_rng(424242).integers(−1,2)`), **100 seeds 1000–1099**, `L=1027`/start `(513,513)`/`T_MAX=512` (boundary provably unreachable), one `rng.random()`/step both arms via `default_rng([4,geom_id,seed])` so **gradient-sampling symmetry is the sole variable**; separates **net displacement (drift)** from a **centered-MSD / ensemble-variance exponent `α_var`** (per-time ensemble-mean subtracted; raw `α_raw` printed only as "NOT superdiffusion proof"); sign-flip permutation test (net, `N_PERM=10000`/`PERM_SEED=1234567`) + seeded paired bootstrap (`α_var`, `B_BOOT=2000`/`BOOT_SEED=7654321`, plus-one p) at Bonferroni `α'=0.05/6`; zero-variance/`log 0` guard; **ASCII-only output (asserted)**; records `numpy_version`. **Instrument-only hard self-checks** (determinism/byte-identical rerun, RNG parity on flat field, shared inputs, legal cardinal moves, boundary invariant, synthetic MSD cases pure-drift/pure-diffusion/drift+diffusion, trial-count, statistical determinism, dtype) — **never asserts treatment wins**. (2) UPDATE `experiments/theory_sandbox/README.md` (Toy #4 → §6a implemented; §6b cleared; status line "four toy scripts"). (3) this entry. **Verified locally:** exits 0, all self-checks PASS, **byte-identical across runs** (`cmp`), no files written by default, runs ~20s. **Sample result = the pre-accepted negative:** Linear treatment big drift (net_mean≈255 vs control≈21, `p_net=1e-4` distinct) but `α_var≈0.92` → **drift without superdiffusion**; Radial treatment confined (`α_var≈0.005`); Noisy near-diffusive (net not distinct); **control `α_var≈1` on all three** (sanity signal holds); **no superdiffusion anywhere** — reported, no success asserted. **DRAFT PR only — do NOT merge.** No engine/`uft_ca`/GPU/R3/observer/Vanguard/Lane A touch; no `data/` writes; no CI/workflow/dependency change; no Toy #5/coupling; no sweep/search/persistence. Awaiting Jack/AURA/Kev review; **stop at green draft, no seal unless explicitly authorized.** **POST-SEAL (2026-06-21):** AURA approved → Jack inspected → head-SHA-pinned (`fde509e`) admin squash-merge → **`a4bcac2`** on `main` (#251); branch deleted; post-merge CI success; 0 alerts; 0 unresolved threads; clean tree (+2 Gemini edge-guards added pre-seal, provably inert on locked inputs → output byte-identical). The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. **Toy #4 fully landed: inception `4a2288c` + erratum `1714a0d` + script `a4bcac2`; result = the pre-accepted "drift without superdiffusion" negative.**

- **2026-06-20 (desktop, 84 / Opus 4.8 seat) — Toy #4 metric-wording erratum (docs-only fix-forward DRAFT PR, not merged)**: parent-only, **documentation-only** fix-forward from `main = 4a2288c` (Toy #4 inception #249 sealed); does **not** reopen #249. **Trigger:** a late post-merge Codex P2 (#249 thread `…6K-log`, file `TOY_04_JANUS_GRADIENT_INCEPTION.md`) validly flagged that **raw MSD measured from the start cell conflates directed drift with diffusion** — a biased walk grows raw MSD as `⟨r²⟩ ≈ v²·t² + D·t`, so a raw exponent can read "ballistic" on pure drift alone, while net displacement already measures that drift separately. Jack agreed; **AURA's hypothesis is unchanged** (this makes the *measurement* honest, not the claim). **Branch:** `experiments/toy4-janus-metric-erratum`. **What changed (exactly 2 docs files):** (1) `TOY_04_JANUS_GRADIENT_INCEPTION.md` — §3.4 rewritten (drift vs spreading; the `⟨r²⟩≈v²t²+Dt` decomposition), §4 metrics redefined (**net displacement** = directed-drift observable; **centered-MSD / ensemble-variance exponent `α_var`**, per-time ensemble-mean subtracted before fitting = the diffusion/superdiffusion evidence; optional raw `α_raw` must be labelled "raw displacement-growth exponent, NOT superdiffusion proof"; **separate** treatment−control deltas on net-displacement *and* `α_var`; **shared, pre-registered fit window/method** identical across arms), §5 no-separation negative redefined on net-displacement-delta + `α_var` (large drift with `α_var≈1` = *drift without superdiffusion*), §6 self-check now requires a **pure-drift synthetic to yield `α_raw≈2` but `α_var≈0`** (plus pure-diffusion → `α_var≈1` and drift+diffusion → `α_var≈1`) and identical fit config across arms, §10 observables + open-questions updated to match. (2) `AGENT_HANDOFF.md` — this entry + a POST-SEAL receipt appended to the #249 entry below. **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/script/CI/workflow/dependency/lockfile change; **no Toy #4 implementation; no Toy #5 repo work; no engine / `uft_ca` / GPU / R3 / observer / Vanguard / Lane A / sweep / search / coupling.** Will reply + resolve the late #249 Codex thread with this PR as evidence; halt at green draft, seal only on explicit authorization.

- **2026-06-20 (desktop, 84 / Opus 4.8 seat) — Toy #4 Janus-gradient INCEPTION (docs-only DRAFT PR, not merged)**: parent-only, **documentation-only** — no script/experiment/engine/observer/CI/dependency change. **Authorization chain:** read-only synthesis checkpoint (chat-only, zero mutation) recommended Toy #4 = Janus gradient over MOST stored-strain → **AURA blessed → Jack accepts → Kev relayed** the bounded docs-only inception prompt. **Stage-1 verify (parent):** `HEAD == origin/main == 944900d` (Toy #3 #248 post-seal); no tracked modifications; no pre-existing Toy #4 / Janus files; clean state. **Branch:** `experiments/toy4-janus-inception`. **What changed (exactly 3 docs files):** (1) **NEW `experiments/theory_sandbox/TOY_04_JANUS_GRADIENT_INCEPTION.md`** — design-only note (explicitly authorizes no script): cites ledger entry 9 / preflight §5; pins the AURA/Jack hypothesis (under *exactly pre-registered* seeds/geometries, a discrete asymmetric "Janus" gradient-sampling rule over a **static scalar field** yields **directed superdiffusive lattice displacement** statistically distinct from a symmetric/gradient-blind baseline on the same field); **sole variable = symmetry of the local gradient sampling** (treatment = discrete asymmetric / integer gradient bias; control = gradient-blind or perfectly symmetric; shared field/seed/start/budget/tie-breaks/topology/measurement pipeline); observables = **MSD exponent α** + net displacement (vector+scalar) at `T_max` + treatment−control deltas; **power floor = exactly 100 pre-registered PRNG seeds × exactly 3 static geometries (Linear Slope, Radial Well, Noisy Step-Function)** — explicitly **pre-registration for power, NOT exploratory sweep/search** (fixed sample declared before any run; no results-dependent expansion; same bar as the parked CA-search/sweep machinery); useful negatives reportable (α_treatment ≈ α_control ≈ 1 no-separation **or** asymmetric-rule instability/shatter); **language quarantine** (VETO: phoresis / continuous thermodynamics / propulsion force / floating-point vectors / Navier–Stokes / physical mass — APPROVED: discrete asymmetric sampling / integer gradient bias / superdiffusive state translation / lattice displacement); hard self-checks limited to instrument correctness (determinism, identical shared inputs across arms, legal in-bounds lattice moves, correct MSD calc, **no treatment-win assertion**); "Janus" kept as a label only (means discrete asymmetric sampling, **not** real Janus particles); README §4 six-step promotion gate restated. (2) **UPDATE `experiments/theory_sandbox/README.md`** — Toy #4 added to §6b (in design-inception, DESIGN/INCEPTION ONLY), removed from §6c candidates (graduated), status line notes Toy #4 in design-inception; MOST stored-strain remains the sole §6c candidate. (3) this entry. **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/CI/workflow/dependency/lockfile change; no sandbox output created; no simulation; **no engine / `uft_ca` / GPU / R3 / observer / Vanguard / Lane A / Toy #3 touch; no sweep/search/mutation.** Awaiting Jack/AURA/Kev review before any Toy #4 *script* prompt. (Stop at green draft; no seal unless explicitly authorized.) **POST-SEAL (2026-06-20):** AURA formally approved the seal → Jack re-verified pre-seal guards → head-SHA-pinned (`2a68c16`) admin squash-merge → **`4a2288c`** on `main` (#249); branch deleted; post-merge CI success (verify / verify-python); 0 code-scanning + 0 Dependabot alerts; 0 unresolved threads; clean tree. The "DRAFT — do NOT merge" wording above records the pre-seal state and is superseded by this receipt. A post-seal **metric-wording erratum** (centered-MSD vs raw-MSD; honest measurement, hypothesis unchanged) is handled in a separate docs-only fix-forward PR — see the newest-first entry above.

- **2026-06-19 (desktop, 84) — Phase 2B-5H-2: Toy #3 scent-trail tracking SCRIPT implementation (DRAFT PR, not merged)**: Jack/Kev-authorized first script for Toy #3, parent-only, **tightly fenced — implements the sealed inception contract only, no science expansion.** **Stage-1 verify:** `main==origin==7fd8bf5` (#246 squash); #244/#245/#246 all 0 unresolved threads; no pre-existing Toy #3 script; 0 alerts. **What landed:** one standalone non-canonical sandbox script **`experiments/theory_sandbox/scent_trail_tracking_toy.py`** (stdlib + NumPy only) faithfully implementing the sealed v0: 32×32 bounded grid, 12 declared seeds, shared declared start (16,16), seeded jump (Chebyshev 3–5), seeded self-avoiding cardinal hidden path ≤12 moves, trail laid while tracker stationary then **stationary-endpoint reacquisition**, `r=0`, 192-move budget, signed `int16` trail with `A=64`/`D=1`/no-diffusion, saturating decay `maximum(trail−D,0)`, **strict-ascent** treatment (move only to a strictly-greater cardinal neighbour, tie lowest `(row,col)`), trail-blind control, shared §3.6 fallback (pre-advance + `idx<len` bound + fail-closed exhaust + fixed axis rule). Treatment & control share identical seed/start/jump/realised-path/endpoint/topology/fallback-ordering/tie-breaks/budget — **trail visibility is the sole variable.** Text-only output + non-canonical warning each run; per-seed treatment/control steps, success-in-budget, winner/tie, and §4 diagnostics (first-trail-acquisition, ascent vs fallback counts, fully-decayed-before-reacq, realised path length). **Hard self-checks enforce instrument correctness only** (int16 dtype/min≥0/max≤A, exact decay, same-seed byte-identical rerun, identical shared geometry, one-non-zero-cardinal-move/tick, `idx<len` fail-closed, trial-count conservation) — **never asserts treatment beats control.** **Verified locally:** runs exit 0; **byte-identical across two launches**; pure-ASCII output (em-dashes ASCII-ified to avoid Windows cp1252 mojibake); treatment is allowed to — and does — tie/lose (sample 12-seed run: 3 treatment wins, 0 control wins, 9 ties; treatment 5/12 vs control 4/12 success-in-budget); **no files written** (no `out/`). Also updated **`experiments/theory_sandbox/README.md`** (Toy #3 moved from design-inception to implemented inventory) + this entry. **DRAFT PR only — do NOT merge.** No engine/CA-runtime/`uft_ca`/GPU-CuPy import; no production data/`data/` writes; no workflow/dependency/lockfile/CI change; no R3/sweep/search/mutation expansion; engine + GPU/Medusa + parked CPU instrument untouched. **POST-SEAL (2026-06-20):** #247 squash-merged → `6f085ae3484af745e2303ca18e21befa4e64c83a` on main (head-SHA-pinned admin squash of `500201c`, after a §6 exact-decay self-check fix + a §4 all-12-seed budget-censored-mean reporting fix from Jack's review); branch deleted; post-merge CI green; 0 alerts. **Post-seal erratum follow-up (docs+script, fix-forward):** a late post-merge Codex P2 (#247 r…aad) validly flagged that `_fallback_step` pre-advanced only on `tracker_pos == waypoints[idx]`, so a cardinal step toward a diagonal waypoint could **transit a later waypoint** and the sweep could re-target that already-visited cell — violating the next-unvisited expanding-ring contract. Fixed by tracking a per-arm **`reached`** set (init `{START}`, updated after *every* move incl. strict-ascent) and pre-advancing on **`while idx<len(waypoints) and waypoints[idx] in reached`**, plus a hard self-check that the fallback never targets an already-reached waypoint; §3.6/§6 contract wording made explicit. Output **byte-identical** to the sealed v0 for the 12-seed table (the bug didn't move these particular reacquire outcomes), determinism holds. Still design/sandbox-only: no engine/CI/dependency/GPU change, no science expansion.

- **2026-06-19 (desktop, 84) — Phase 2B-5H-1: Scent-Trail theory intake + Toy #3 inception (DRAFT PR, not merged, docs-only)**: Jack-relayed (AURA-confirmed), parent-only, **documentation-only** — no script/experiment/engine/observer/CI/dependency change. **Context:** after the read-only **Phase 2B-5H-0** formalization audit classified AURA's "Physics of the Utility Fog" handover (9 concepts → discrete state/rule/observable/falsifiable-prediction), Jack ruled: Part 4 SUPERSEDED/COMPLETED; Parts 1–3 = doctrine + hypothesis ledger (not a backlog); first experiment = **decaying scent trails** (reacquisition benefit as the falsifiable outcome); **do not implement the toy yet** — a documentation-truth pass comes first (the sandbox README still claimed "no experiments yet" though Toy #1/#2 scripts exist). **Stage-1 verify (parent):** `main==origin==05951e2`; Calibrations 001–003 external + CPU instrument PARKED; no active 2B-5H branch/PR; 0 Dependabot+code-scanning alerts; nothing consumes sandbox output; no pre-existing Toy #3. **What changed (exactly 4 docs files):** (1) **`docs/MEDUSA_THEORY_INTAKE_LEDGER.md`** — new entry 14 "AURA Physics of the Utility Fog — discrete hypothesis intake": architectural doctrine (local-first/bounded/sovereign; grep-before-vector; bounded sleep cycles; provenance-before-RSI; **+ Jack's GPU-for-explore / CPU-for-verified-replication governance ruling**), candidate CA hypotheses + agent-layer concepts each carrying their 2B-5H-0 classification, scent-trails marked **APPROVED FOR DESIGN-INCEPTION ONLY — no engine implementation authorized**, Part-4-completed + instrument-parked recorded; vendor/cost anecdotes deliberately excluded from the durable record. (2) **`experiments/theory_sandbox/README.md`** — corrected the stale "no experiments yet" status, added a truthful inventory (Toy #1 Galton/Fourier #200, Toy #2 stochastic-escape #204, both non-canonical) + a Toy #3 row "Decaying scent-trail tracking — INCEPTION/DESIGN ONLY; script not implemented" (falsifiable tracking, **not** radioactive physics, **not** Medusa validation); all quarantine/non-goal/CI/six-step-promotion rules preserved unchanged. (3) **`experiments/theory_sandbox/TOY_03_SCENT_TRAIL_TRACKING_INCEPTION.md`** — NEW design-only note (explicitly no script, none authorized): precise falsifiable hypothesis; **Jack-corrected v0** (after his live #244 review + 3 valid Gemini threads): **trail-laying SEPARATED from reacquisition** — reacquire a now-**stationary** hidden endpoint (NOT an equal-speed chase, which can be structurally impossible), with **trail availability the sole variable**; pinned geometry (32×32 bounded, jump Chebyshev 3–5, seeded 12-move self-avoiding cardinal hidden path, r=0, 192-move budget, 12 seeds, A=64/D=1); **signed `int16` saturating decay** `maximum(trail−D,0)` (fixes the unsigned-underflow thread); **strict-ascent** treatment rule — move only to a strictly-greater neighbour, else fallback (fixes the trail-tip oscillation thread); **explicit identical update order** across both arms (fixes the update-order thread); fully-specified shared expanding-ring fallback; metrics (steps-to-reacquire, success-in-budget) + diagnostics; hard self-checks limited to instrument correctness (NOT "treatment beats control"); stdlib+numpy/text-first/no-CI/no-GPU quarantine; six-step promotion boundary. (4) this entry. **Hunting imagery removed from flavour text per Kevin (no bloodhound/baying); reframed as wayfinding/following fading footprints — technical "scent-trail" term retained for cross-file consistency.** **DRAFT PR only — do NOT merge.** Markdown-only; no `.py`/CI/workflow/dependency/lockfile change; no sandbox output created; no simulation; engine + GPU/Medusa + parked instrument untouched. Awaiting Jack/AURA/Kev review before any Toy #3 *script* prompt. **POST-SEAL (2026-06-19):** #244 squash-merged → `e6dc53bee13d95ecc11765ad5494b7fdd9810b93` (head-SHA-pinned admin squash of `81ce899`); the "DRAFT / do NOT merge" wording above records the pre-seal state and is superseded by this receipt. **Erratum follow-up (docs-only follow-up PR from `e6dc53b`):** a late post-merge Codex P2 (r3441709727) validly flagged that §3.6's fallback waypoint list begins on the tracker's own distance-0 start cell, so a literal future implementation could stall (zero-length move) on the first fallback tick; fixed by a **pre-advance** rule — skip any already-reached waypoint (`while idx < len(waypoints) and tracker_pos == waypoints[idx]: idx += 1`) *before* moving, yielding exactly one non-zero cardinal move per reacquisition tick, applied identically to both arms (trail visibility still the sole variable). Still design-only: no script/engine/workflow/dependency change. **Follow-up erratum (docs-only):** added the `idx < len(waypoints)` bound to the pre-advance loop + removed a duplicated "trail visibility" sentence, per two valid Gemini threads on PR #245 (r3442218371, r3442218378).

- **2026-06-19 (desktop, 84) — Phase 2B-5G-3: CA documentation truth reconciliation (DRAFT PR, not merged)**: Jack-relayed, parent-only, **docs-only** — no code/tests/manifests/deps/lockfiles/workflows/settings touched. **Why:** with `scripts.ca.replicate` sealed onto `main` (2B-5G-2 → `84c0f36`), `ca/docs/README.md` still described the *retired* GitHub-Actions/Lambda CA-search system, told users to `gh workflow run ca-search.yml` (deleted in 2B-5F-1), and presented the *dormant* `uft_orch.ca.runner` as the local execution path — while the new supported instrument appeared only in source/tests. **Stage-1 read-only truth sweep (parent-verified):** `main==origin/main==84c0f36`; PR #242 merged at that squash, branch absent; `scripts/ca/*`+tests on main; `ca-search.yml` absent; the ONLY live instructional doc with stale refs is `ca/docs/README.md`. `plans/mvp-fractal-tree/milestones.md` (historical build-checklist incl. `#88 ca-search.yml`), `docs/REPOSITORY_HEALTH_AUDIT_2026-06.md` and `docs/SECURITY_AUTOMATION_SALVAGE_PLAN_2026-06.md` are dated HISTORICAL records — inventoried, NOT rewritten (no competing *maintained* execution path → no STOP). **What changed (exactly 2 files):** (1) **`ca/docs/README.md`** rewritten to current executable truth — an execution-surface status table (**supported** `python -m scripts.ca.replicate` / **research** `continuous_evolution_ca` wrapped by it / **maintained** `crates/uft_ca` Rust kernel / **dormant-legacy** `uft_orch.ca.runner` + YAML/seed experiment path / **retired** GitHub-Actions "CA Rule Search"), plus full replicate docs derived from the merged source (scientific purpose; honest R1–R2 claim boundary; exact schema-v1 manifest + `--manifest`/`--out`/`--resume` commands; output layout incl. `run.json`/`manifest.json`/`index.jsonl`/`result.json`/`final_state.npz`/checkpoints; running/completed/interrupted/failed lifecycle + exit codes; high-level resource/safety caps that point to `validate_manifest` as authoritative to avoid drift; deferred trials/sweeps/mutation-search/R3/`run_v070`-graft). All `ca-search.yml`-dispatch, CI/Lambda-search and "test before CI deployment" instructions removed; `uft_orch.ca.runner` demoted to dormant/legacy (explicitly NOT repaired or deleted). (2) **`AGENT_HANDOFF.md`** — a post-merge receipt appended to the 2B-5G-2 entry (its "DRAFT/do NOT merge" wording marked superseded; merged PR history not rewritten) + this entry. **DRAFT PR only — do NOT merge.** No engine/`example.toml`/runner/seed/Rust/workflow/dependency/setting change; CA experimentation remains CORE research (only the broken cloud harness was retired). Next after review/seal (per Jack): **pause** before widening science — either a deliberately-named first calibration experiment or park until a concrete hypothesis, NOT an automatic leap to GPU/R3.

- **2026-06-19 (desktop, 84) — Phase 2B-5G-2: CPU-only deterministic CA replication + provenance wrapper (DRAFT PR, not merged)**: Jack's authorised overnight carriage of Recommendation B as a *bounded, local-first* wrapper around the audited engine. **Lineage**: branched from `main` at `b40d754e5c7d52d5cbe25ae309a0f85b4e17cd13` (the #241 / 2B-5F-1 `ca-search.yml` retirement). **What landed (exactly 5 files, additive only — no existing tracked file modified):** `scripts/ca/__init__.py` (package scope), `scripts/ca/engine_adapter.py` (CPU-forcing seam + RNG-state serialisation + scientific/trajectory hashing), `scripts/ca/replicate.py` (manifest-driven CLI: validate → pin threads → force CPU → run replicates one-at-a-time → step-indexed checkpoints *with* RNG state → atomic provenance → resumable), `tests/test_ca_replicate.py` (64 tests), and this log entry. **The CPU-forcing seam** (the crux): the engine binds its array backend (`_xp`) and a module-global GPU RNG at *import* time, and on Kevin's box (CuPy 14.0.1 + RTX 5090, per the 2B-5G-1 audit) the default path is GPU and *not* run-to-run reproducible. The wrapper injects a stand-in `scripts.gpu_accelerator` with `GPU_AVAILABLE=False` into `sys.modules` *before* importing the engine, asserts `_xp is numpy` / `GPU_AVAILABLE is False` / `_gpu_rng is None`, yields the engine, then fully restores process import state in a `finally` (watched `sys.modules` entries, the leaf attributes bound on the `scripts` package, and any inserted `sys.path` entry) and is **non-reentrant** (a module-level lock fail-fasts on a second concurrent/nested context) — **the live engine source, `run_v070_engine.py`, the dormant `src/uft_orch/ca/runner.py`, the `ca/seeds/*.json` loader, Rust/PyO3, workflows, deps and existing tests are all untouched; CuPy is never imported.** **Determinism claim (this milestone only):** bitwise R1/R2 on the CPU/NumPy backend within a fixed engine revision + NumPy version, given identical seed/manifest/initial-state. **Explicitly NOT claimed:** CPU↔GPU identity, cross-machine identity, cross-version identity, or fidelity to the full Medusa daemon (no GA cadence/telemetry/wall-clock); the live GPU RNG path remains non-reproducible and is left alone. **Manifest schema v1 (deliberately narrow):** `{schema_version:1, experiment_id, mode:"replicate", backend:"cpu", seed, lattice_size, cube_size, steps, replicates(>=2), checkpoint_every_steps, thread_cap}` — one justified, documented deviation from the engine helper: a `lattice_size` field (default 8) so the tiny tests don't inherit `generate_primordial_seed_cube`'s hardwired 64³, plus conservative upper caps (`lattice_size≤64`, `steps≤10000`, `replicates≤100`, `thread_cap≤host-CPU-count`) and relational/memory guards (`cube_size≤lattice_size`, `checkpoint_every_steps≤steps`, peak-memory estimate) — no unlimited escape hatch. **Rule delivery:** the executed rule is the in-code dict `engine_adapter.DEFAULT_RULE_SPEC` (a transcription of `ca/rules/example.toml` v0.7.5's four consumed sections — transitions + stochastic/contagion/decay, the latter three kept ENABLED so the engine genuinely draws from the explicit RNG and the resume guarantee is meaningful), fed straight to `step_ca_lattice` exactly how the engine's own test-suite drives it. `ca/rules/example.toml` is a **provenance/source pointer only and is NOT parsed at runtime** — it uses multi-line inline tables, which the stdlib `tomllib` the engine falls back to in a clean env (e.g. CI) rejects — and `rule_sha256` hashes the executed in-code rule spec. **Safety/validation:** rejects `mode≠replicate`, `backend≠cpu`, `replicates<2`, non-positive bounds, negative seed, `schema_version≠1`, unknown/deferred-feature keys (e.g. `mutation_rate`), over-cap/relational violations, and an `experiment_id` that isn't a filesystem-safe slug (blocks path traversal); resume refuses on checkpoint **or completed-result** manifest-hash / seed / dims / backend mismatch. **Provenance carries NO personal identifiers** — git sha + dirty flag, python/numpy versions, `platform.system()`/`platform.machine()` (arch only, no hostname/username/home path), manifest+rule sha256, repro claim + limitations; results layout `results/<exp>/<run-id>/{manifest.json,run.json,index.jsonl,replicate_NNN/{result.json,final_state.npz,checkpoints/checkpoint_step_*.npz}}`, all writes atomic (tmp-in-dir + `os.replace`, OneDrive-safe). **Scientific hash** = sha256 over the THREE engine-evolved arrays in fixed order (`lattice`, `memory_grid`, `inactivity_steps`); inactivity is included because it drives future decay transitions and is checkpointed, so omitting it could falsely certify replication. **Run lifecycle:** fresh run dirs are uniquely named (microsecond timestamp + random nonce) and created **exclusively** (never silently reuse); a per-run `run.lock` (O_EXCL) prevents two processes running/resuming the same dir; `run.json` carries a durable status (`running` written before any compute → `completed`, or `interrupted` (exit 130) on Ctrl-C, or `failed` with a bounded privacy-safe error summary on any other exception), preserving the original `started_utc` and appending each resume to `resumed_utc`. The run-id nonce is a directory identifier ONLY and never enters any scientific hash. **Empirically verified locally:** two *independent process launches* produce byte-identical scientific hashes; all replicates within a run identical; CLI resume reproduces the uninterrupted result exactly (completed replicate skipped, partial one resumed from its checkpoint, wiped one re-run fresh — all hashes equal, inactivity included); proven CI-equivalent by running with the third-party `tomli` blocked (forcing stdlib `tomllib`, the clean-env condition) → identical hash. **Hardening pass (Jack PR-review, same draft, same five files):** seven correctness/safety findings repaired — (1) `experiment_id` path-traversal → filesystem-safe slug; (2) schema-v1 upper caps + relational + memory guards; (3) `inactivity_steps` folded into both hashes; (4) full `cpu_engine` state restoration (`sys.modules` + `scripts` pkg attrs + `sys.path`) + non-reentrancy lock; (5) collision-proof exclusive run dirs; (6) resume provenance/lifecycle (status + preserved start + completed-result identity validation + run lock); (7) this entry's wording. **Final integrity pass (Jack 2nd PR-review, same draft, same five files):** seven more — (G1) aggregate budgets beyond per-field caps (`lattice³·steps·replicates ≤ 100M` cell-steps; total checkpoint count ≤ 1000; estimated checkpoint storage ≤ 2 GiB; `thread_cap ≤ min(8, CPU)`); (G2) `seed` bounded to `[0, 2⁶⁴−1]`; (G3) **resume identity now pins the executed implementation** — engine/adapter/wrapper source SHA-256 + rule + NumPy + manifest + backend + replicate_index + target_steps + `completed_steps==target_steps` + `status==completed`, embedded in checkpoint meta + result.json, fail-closed on any mismatch (a git SHA alone can't see a dirty tree); (G4) corrupt/missing/incomplete `run.json` on resume fails closed (no manufactured provenance); (G5) ordinary exceptions now record a durable `failed` status (bounded privacy-safe summary) then re-raise; (G6) the R1/R2 claim now states it holds only when the recorded executed-source hashes + rule + NumPy + backend + manifest + initial state match; (G7) PR body refreshed. **Test results:** `tests/test_ca_replicate.py` 64/64 pass; the full local suite is green apart from 3 *pre-existing, environment-specific* failures (`test_3_to_8_channel_migration` = the engine's CuPy/GPU path; two `test_cli_viz` exporter tests = Windows cp1252 `UnicodeDecodeError`), all reproducing on the `b40d754` base; CI is CPU-only Ubuntu (recorded 0-failure baseline). **Status: DRAFT PR only — do NOT merge.** Branch `feature/phase2b5g2-cpu-replication-foundation`; observe `agent-safety` / CI `verify` + `verify-python` / CodeQL + any review threads, then stop. Next honest step after green: a Lane-decision on whether to (a) widen to R3 cross-backend study, (b) graft this provenance onto `run_v070_engine` snapshots, or (c) park — all require explicit go. **POST-MERGE RECEIPT (2026-06-19 — supersedes the "DRAFT PR only / do NOT merge" wording above, which records the pre-seal state):** PR #242 was subsequently **squash-merged → `84c0f36a9d7d3eb77ccb19b369f56a460dc4a933`** on `main` (one-time Kevin/Jack-authorized head-SHA-pinned admin squash of `a0ed2fb`). Exactly the five additive files landed; engine/`run_v070`/GPU-RNG/Rust/workflows/deps stayed byte-unchanged; post-merge CI (`verify`+`verify-python`) + CodeQL were green; 0 code-scanning + 0 Dependabot alerts; a tiny CPU/CuPy-free smoke on merged `main` passed; the `feature/phase2b5g2-cpu-replication-foundation` branch was deleted (local+remote); preserved PRs #17/#25/#52–#58 untouched; **no later R3/GPU/Medusa-integration phase began.** The merged PR title/body are left as historical record and not rewritten.

- **2026-06-18 (desktop, 84) — Phase 2B-5F-1: retire obsolete `ca-search.yml` (AURA verdict C; draft PR)**: AURA curator ruling (Jack-reconciled) = **C — retire this implementation cleanly**. **Lineage**: branched from `main` at `4cec50ec1247f3a4f6c0ad141139ae6442e622b8` (the #240 / 2B-5D-2A `security-extended` squash). **Evidenced defects (from the read-only 2B-5F-0 forensic, parent-verified from source):** (1) invalid YAML — the `aggregate-results` inline `python -c` body sits at column 0 under a `run: |` whose content indent is 10 → block scalar terminates → `ScannerError` → a 0-job failed run on every push; (2) the matrix `repeat` expression uses `range()`, which is NOT a supported GitHub Actions expression function → the matrix cannot expand on a real dispatch; (3) `src/uft_orch/ca/runner.py` never reads `EXPERIMENT_REPEAT` → every matrix repeat writes the SAME file (`artifacts/branching-3/branching-3_metrics.csv`); (4) the per-repeat summary globs (`artifacts/*/metrics.csv`, `artifacts/*_metrics.csv`) mismatch the real name-prefixed path → dead summary; (5) the ONLY supported experiment is broken — `ca/seeds/single-cell.json` has `states: []` while declaring a 64³ lattice + seed_type/position/state, and `load_seed` reads `data["states"]` directly (ignoring seed_type/position/state) → a 0-cell simulation (NaN/0 metrics, or a size-mismatch crash under real CI); (6) deterministic seed + rule ⇒ repeats are identical by construction; (7) NO downstream consumer (only docs reference it). **No cosmetic repair applied:** a 'merely green' YAML/`range()` fix would wrap a scientifically vacuous (empty-seed, non-differentiated-repeat) contract — worse than honest retirement. **Local-first boundary:** this retires ONLY the broken GitHub-Actions *cloud-execution harness*; **CA experimentation, rule searching, branching studies and stochastic exploration remain CORE research and are NOT deprecated** — they move to a future local-first design. **PRESERVED UNCHANGED (Jack's reconciliation — do NOT delete/edit):** `src/uft_orch/ca/runner.py`, `ca/experiments/branching-3.yaml`, `ca/rules/example.toml`, `ca/seeds/single-cell.json` (+ `random-sparse.json`), and all CA algorithm/runner/seed/experiment code — their defects are recorded, but their disposition (semantic-seed + per-repeat-RNG design) is a SEPARATE future research arc. **Preservation:** Git history is sufficient for the deleted workflow — **no archive tag, no replacement workflow, no salvage doc**. **This arc = exactly 2 files:** `.github/workflows/ca-search.yml` DELETED (−184) + this Session Log entry. Required checks on `main` = `agent-safety` + `verify` only (CA-search was never a required check); 4 live workflows remain afterward (`agent-safety`, `ci`, `codeql`, `ui-smoke`). Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5F-1 PR is a **draft** awaiting separate Jack + Kevin review — it is not to be merged automatically.
- **2026-06-18 (desktop, 84) — Phase 2B-5D-2A: scoped Python `security-extended` evaluation (draft PR #240) — Outcome A, 0 findings**: Controlled signal-vs-noise measurement of the broader query suite against the EXACT Python scope already live on `main` (no language/directory widening). **Lineage**: branched from `main` at `476f6a94ad8ef6fc954ce2eed787587485cca7cc` (the #239 squash — scoped Python CodeQL baseline). **Single config change** to `.github/codeql/codeql-config.yml`: added `queries: - uses: security-extended` (= CodeQL default high-confidence queries PLUS lower-severity/lower-precision queries; default queries stay enabled — NO `disable-default-queries`), and updated the now-contradictory in-file comment to describe the evaluation (comment-coherence, same single file). **Unchanged:** `name:` (kept verbatim), `paths: [scripts, src/uft_orch]`, `paths-ignore:`, the workflow file `.github/workflows/codeql.yml`, permissions, SHA-pins, triggers, cadence; NO `security-and-quality`, NO custom packs, NO `threat-models`; language remains Python-only; Rust/JS-TS/dormant surfaces still excluded. **Why**: measure whether the deeper suite earns its maintenance cost — extra depth = real signal, or just noise? **Scan result (draft head `46fcbc041961f51f9df09445c747fe254180a224`; CodeQL ran on PR merge commit `24e4c863`):** `Analyze Python` PASSED (~55s), SARIF uploaded; **results_count = 0 → 0 code-scanning alerts** (verified `refs/pull/240/merge`); suite ran successfully and remained scoped (no finding outside `scripts/` / `src/uft_orch/`). All other checks green (verify, verify-python, agent-safety, CodeQL). **Classification:** no findings to classify — `security-extended` adds NO noise (0 false positives) AND surfaces NO new signal beyond the clean default baseline on this scope. **Outcome A (0 findings)** → the broader suite is a clean candidate for sealing (adopting `security-extended` permanently would add depth at zero current noise cost); equally, the default suite already suffices since extended found nothing new — a Jack/Kevin call. **No hard-stop condition triggered.** **Draft PR #240 left OPEN/draft — NOT merged.** **Invariants:** `main` UNCHANGED `476f6a9`; default setup still `not-configured`; no application/test/dependency/manifest/lockfile change; no workflow/permission/Ruleset/required-check/setting change; no alert dismissed/assigned/suppressed/filtered; preserved PRs #17/#25/#52–#58 untouched; #229/#230 remain CLOSED; archive tags intact (`→3df3d33`/`→33449a7`); no Rust/JS expansion, no Scorecard/LICENSE work. Final PR diff = exactly 2 files (`.github/codeql/codeql-config.yml` + this AGENT_HANDOFF entry). Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5D-2A PR is a **draft** awaiting Jack review + Kevin's decision — not merged automatically.
- **2026-06-18 (desktop, 84) — Phase 2B-5D-1: scoped Python CodeQL baseline (advanced setup, draft PR #239)**: First first-party SAST brick, implementing the 2B-5D-0 recommendation with Jack/Kevin's amendment (default high-confidence queries only; Python-only; `scripts/` + `src/uft_orch/`). **Lineage**: branched from `main` at `fb87171554725de349f3106206c18afc574ecc1a` (the #238 squash; carried forward, UNCHANGED by this arc). **Why not default setup**: GitHub default setup cannot path-filter and would scan the maintained-surface boundary's exclusions (dormant `crates/vanguard-mcp`, dormant `utilityfog_frontend/backend`, root-level legacy `*_test.py`, vendored/generated dist/report/~20MB HTML) → chronic noise; only advanced setup with a config-file `paths:` honors the boundary, so default setup was deliberately rejected. **Historical CodeQL analyses** (10, python + javascript-typescript, Sept-2025) are inactive and trace ONLY to the closed-unmerged PR #75 lineage (`garden/prod-workflows`@`822dced2`); they never landed on `main` (default-setup `not-configured` pre-arc). **This arc = exactly 3 files** (no code/test/dependency/manifest/lockfile/setting change): (1) `.github/workflows/codeql.yml` — advanced, Python-only, `init`+`analyze` referencing the committed config; triggers `workflow_dispatch` + monthly `schedule` (`37 4 12 * *`) + `push`(main) + `pull_request`(main), gated by trigger `paths` (`scripts/**`, `src/uft_orch/**`, the two new files); (2) `.github/codeql/codeql-config.yml` — actual analysis scoped via `paths: [scripts, src/uft_orch]` + `paths-ignore` for pycache; **default query suite** (NO `security-extended`/`security-and-quality`/custom packs); (3) this Session Log entry. **Trigger-paths vs analysis-paths (kept explicit):** the workflow `on.<event>.paths` decide WHETHER a run starts; the config-file `paths:` decide WHAT SOURCE CodeQL analyzes — independent mechanisms. **Scope deferred (staged-security decision, NOT a permanent never-scan):** Rust (`crates/**` incl. uft_ca + vanguard-mcp), JavaScript/TypeScript (utilityfog_frontend/frontend), and the uncertain/dormant Python surfaces (utilityfog_frontend/backend, `agent/`, `UtilityFog_Agent_Package/`, `vis/`, `tests/`, root legacy `*_test.py`, CA experiment/rule data, generated/vendored/build/report/cache, `.claude/`, retired FT/Specify remnants). **Pins (parent-verified full SHAs):** `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5` #v4.3.1; `github/codeql-action/init` + `/analyze` @ `8aad20d150bbac5944a9f9d289da16a4b0d87c1e` #v4.36.2 (one monorepo SHA). **Permissions (least-privilege):** top-level `contents: read`; the analyze job adds `security-events: write` (SARIF upload). No other permission; no `pull_request_target`; no badge; not a required check; fork-safe (plain `pull_request` — fork PRs run with a read-only token, no secrets). **First-scan result (draft head `dde7179bae03bb9575086cbe6b6d3ae12078b849`; CodeQL ran on PR merge commit `e48dd324`):** `Analyze Python` PASSED (~54s), SARIF uploaded successfully; **results_count = 0 → 0 code-scanning alerts** (verified on `refs/pull/239/merge` AND `refs/pull/239/head`); 0 critical/high; nothing outside the authorized scope (config enforces `scripts` + `src/uft_orch`); clean, low-noise baseline. All other PR checks green (verify, verify-python, agent-safety). **No hard-stop condition triggered.** **Draft PR #239 left OPEN/draft** for Jack's scrutiny + Kevin's separate merge decision — NOT merged. **Invariants:** `main` UNCHANGED `fb87171`; default setup NOT enabled (still `not-configured`); no setting/Ruleset/branch-protection/required-check change; no source/test/dependency/lockfile change; no alert dismissed/assigned/suppressed; preserved PRs #17/#25/#52–#58 untouched; #229/#230 remain CLOSED-unmerged; archive tags intact (`→3df3d33`/`→33449a7`); Scorecard/LICENSE and Rust+JS expansion NOT begun. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5D-1 PR is a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-17 (desktop, 84) — Phase 2B-5B-4A: disposition of Dependabot PRs #229/#230 (rand/rand_xoshiro major bumps) — closed unmerged; named Dependabot branch refs auto-removed on close (draft closeout PR)**: Acting on the completed read-only forensic audit (Jack-reviewed & concurred, Kevin-authorized; no AURA — see below). **Lineage**: `main` at `851b820879e8d8442cc1860b2f27e18c421c5660` (PR #237, OPA checksum) is UNCHANGED by this arc — only the two PR states changed. **Audit basis**: #229 (`rand_xoshiro 0.6→0.8`, head `165cde45…`) and #230 (`rand 0.8→0.10`, head `8f5bde22…`) are **tightly coupled and individually non-viable** — `rand_xoshiro 0.8` requires `rand_core 0.10`, which forces `rand 0.10` (no working `rand 0.9` half-step); #229 alone resolves to a split `rand_core 0.6`+`0.10` graph but won't compile (`E0599`); #230 alone won't resolve (`rand 0.10` removed the `small_rng` feature the manifest still requests); both-as-written also fail on `small_rng`. The only viable upgrade is a deliberate **combined migration** (bump both deps + drop `small_rng` + migrate the 8 `rng.gen::<f32>()`→`.random::<f32>()` sites and `use rand::Rng;`→`use rand::RngExt;` in `stepper.rs` + raise MSRV→1.85 + add a fixed-seed regression test) — **deferred** (no security or functional driver; 0 open Dependabot alerts), recipe parked for a future arc (e.g. 2B-5B-4B), NOT implemented here. **Determinism: empirically bit-for-bit identical** seeded streams across the upgrade (parent OS-temp compile of rand 0.8/xoshiro 0.6 vs rand 0.10/xoshiro 0.8 — identical `u64` and `f32` bits for both `seed_from_u64` and `from_seed`; the crate uses no `gen_range`/`Uniform`, so the rand-0.9 `Uniform` value-stability change does not apply) → no scientific-baseline fork → **no AURA ruling needed**. **Authorized mutations performed (sequential, parent-run, no agents)**: (1) one PR-specific evidence comment on #229 (`issuecomment-4729412608`) then **closed UNMERGED**; (2) one PR-specific evidence comment on #230 (`issuecomment-4729416600`) then **closed UNMERGED**. **Branch refs (corrected record)**: immediately after each close the named head-branch refs were verified present, then **Dependabot automatically removed them** (`dependabot/cargo/crates/uft_ca/rand_xoshiro-0.8`, `dependabot/cargo/crates/uft_ca/rand-0.10`) — **no human branch-deletion command was issued** (repo `delete_branch_on_merge=false`, so this is Dependabot's own close-cleanup, not a merge setting). **Preservation remains** through the closed PR records, the recorded head SHAs (`165cde45…` / `8f5bde22…`), the commits/diffs/comments, and GitHub's PR head refs (`refs/pull/229/head` / `refs/pull/230/head`); **both PRs remain reopenable**. No rebase/retarget/relabel/assign; existing review history untouched; **no Dependabot "ignore" command used** (closes the defective standalone PRs without changing future update policy). **Known Dependabot behavior to remember for future disposition arcs: closing a bot PR may auto-delete its named head branch.** **Invariants**: `main` UNCHANGED `851b820`; no workflow/manifest/dependency/lockfile/`dependabot.yml`/setting/Ruleset/branch-protection/alert/issue change; **0 open Dependabot alerts**; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Scorecard/CodeQL/packaging remain UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. Full report: `UtilityFog_2B-5B-4A_rand_audit_2026-06-17.md`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-4A closeout PR is opened as a **draft** containing only this Session Log entry and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5B-3B-3: OPA v0.59.0 binary checksum verification (fail-closed, draft PR)**: The small supply-chain brick that closes the last 3A finding (the unverified OPA download), under Kevin's explicit authorization (Jack relay; no AURA). **Lineage**: PR #236 sealed `main` at `616bfd9f06ebfed007dd8edaec8a4807230615ee` (Action SHA-pins + least-privilege `permissions` live; the OPA download was deliberately left byte-unchanged there for this arc). **Pre-mutation verification (parent, independent)**: the official OPA **v0.59.0** GitHub release publishes a checksum asset `opa_linux_amd64_static.sha256`; fetched it directly (`releases/download/v0.59.0/opa_linux_amd64_static.sha256`) — contents `5f615343a1cae1deb2f2f514b2f4b46456495fe1c828b17e779eb583aced0cc3  opa_linux_amd64_static` — **MATCHES** the previously recorded expected digest. The digest was confirmed **from the official published checksum asset, NOT derived from the downloaded binary**. **This arc (exactly 2 tracked files)**: (1) `.github/workflows/agent-safety.yml` — in the existing **Install OPA** step, embedded `readonly OPA_SHA256="5f615343…aced0cc3"` as a static trust anchor + a `printf '%s  %s\n' "${OPA_SHA256}" opa | sha256sum --check --strict -` verification **inserted after the `curl` download and before `chmod`/`mv`/`opa version`** → fails the job **closed** on any mismatch (`set -euxo pipefail` + `--strict`). **Retained**: OPA version `v0.59.0`, the existing GitHub-Releases binary URL, the `curl -L --fail --retry 5 --retry-delay 5 --retry-all-errors` flags, the newly-landed `actions/checkout` SHA pin, and the top-level `permissions: contents: read` — all byte-unchanged. **Did NOT**: fetch the checksum dynamically at CI time, upgrade OPA, add any Action or dependency, or change OPA fmt/check/test steps. (2) this Session Log entry. **Validation**: YAML parses; the `sha256sum --check` step executed successfully in CI (the agent-safety run downloaded the real binary and the embedded digest verified). No other workflow / dependency / manifest / lockfile / Dependabot-config / setting / Ruleset / branch-protection / alert / issue change; Dependabot PRs #229/#230 observe-only untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Scorecard/CodeQL/packaging remain UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `616bfd9`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-3B-3 PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5B-3B-2: pin live GitHub Actions to full SHAs + least-privilege `permissions` (draft PR)**: Supply-chain hardening brick (the 3A audit's deferred implementation), under Kevin's explicit authorization (Jack relay; AURA already ruled the workflow policy/sequencing — no return). **Lineage**: PR #235 sealed `main` at `a1be1c1fc86dc2fba6e571e1ed640bdef1ac6005` (Dependabot scope-comment reconciliation). **Scope — the 4 live workflows** (`agent-safety.yml`, `ca-search.yml`, `ci.yml`, `ui-smoke.yml`; `ci-seeder.yml` already retired in 2B-5B-3B-1): **8 unique external Actions across 19 `uses:` occurrences**, every one repinned from a mutable tag to its verified full 40-char SHA with a readable version/channel comment — `actions/checkout`@`34e114876b0b…` # v4.3.1 (×6, incl. the two inline `- uses:` in ci.yml), `actions/upload-artifact`@`ea165f8d65b6…` # v4.6.2 (×4), `actions/download-artifact`@`d3f86a106a0b…` # v4.3.0 (×1), `actions/setup-python`@`a26af69be951…` # v5.6.0 (×3), `actions/setup-node`@`49933ea5288c…` # v4.4.0 (×2), `actions/cache`@`0057852bfaa8…` # v4.3.0 (×1), `actions-rust-lang/setup-rust-toolchain`@`46268bd06076…` # v1.16.1 (×1), `dtolnay/rust-toolchain`@`3c5f7ea28cd6…` (×1). Each major/version tag was re-resolved against authoritative upstream before mutation (current major tag == version tag == expected SHA for all 7 standard actions). **Top-level `permissions: contents: read` added to all four workflows** (none previously had a permissions block → they inherited the token's broad default; now least-privilege read-only — sufficient for checkout/build/test; no job needs write). **Both Rust Actions intentionally RETAINED** (no consolidation): `actions-rust-lang/setup-rust-toolchain` in ca-search + `dtolnay/rust-toolchain` in ci.yml's verify-python — their Action *implementations* are now SHA-pinned while the Rust *toolchain* stays rolling **`stable`**. **dtolnay correction (the load-bearing catch)**: the originally-relayed pin `29eef336d9b2848a0b548edc03f92a220660cdb8` was **REJECTED** — it is the generated `@stable` *channel* commit sitting **one commit ahead of `master`** (parent = master HEAD `3c5f7ea…`, msg "toolchain: stable"); dtolnay's own README §"Choice of full-length commit SHA" *requires* a SHA **within `master` history** ("Any commit that is not within the history of master will eventually get garbage-collected and your workflows will fail"). Corrected durable pin = **`3c5f7ea28cd621ae0bf5283f0e981fb97b8a7af9`** (current `master` tip, in-history; comment `# master-history action pin; rolling stable selected below`) **plus the explicit `with: toolchain: stable`** input — because pinning the implementation SHA removes the `@stable` shorthand that previously selected the channel; verified `action.yml@3c5f7ea` declares the required `toolchain` input. **OPA download/checksum is a SEPARATE, UNSTARTED arc** — `agent-safety.yml`'s OPA `curl …/v0.59.0/opa_linux_amd64_static` block is **byte-unchanged** here. **This arc (exactly 5 tracked files)**: the 4 workflow files + this Session Log entry. No workflow triggers/jobs/steps/commands/versions/cache/artifact/conditions changed except the one required `with: toolchain: stable` under the pinned dtolnay action; no dependency/manifest/lockfile/Dependabot-config/setting/Ruleset/branch-protection/alert/issue change; Dependabot PRs #229/#230 observe-only untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); OPA-checksum + Scorecard/CodeQL/packaging remain UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `a1be1c1`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-3B-2 PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5C-2D: one-time Dependabot scope-comment reconciliation (comments-only draft PR)**: The promised single wording cleanup, now unblocked because both deferred frontends are retired from `main`. Under Kevin's explicit authorization (Jack relay; no AURA needed). **Lineage**: PR #234 sealed `main` at `237963d6340e2d9c398f894189b79f03eb69699d` (retired the whole `visualization/` frontend+backend stack); PR #233 earlier retired `web/`. So the stale `.github/dependabot.yml` introductory comment referencing "the three frontends" (written 2B-5B-2B when web + utilityfog_frontend + visualization all still existed) is now factually wrong — only `utilityfog_frontend` remains. **This arc (exactly 2 tracked files, COMMENTS-ONLY)**: (1) `.github/dependabot.yml` — replaced the stale comment block ("All other dependency surfaces (… the three frontends …) are intentionally deferred/excluded pending the maintained-surface inventory (2B-5C)") with accurate current wording ("… the retained utilityfog_frontend dependency surface …" + "The retired web/ and visualization/ manifests are no longer present on main"); **NO executable YAML changed** — both `updates:` entries (github-actions `/` + cargo `/crates/uft_ca`, monthly, open-pull-requests-limit 2, group `actions` patterns `["*"]` + group `uft-ca-minor-patch` update-types `[minor,patch]`) and `version: 2` are byte-identical; (2) this Session Log entry. **Validation**: parsed `.github/dependabot.yml` with PyYAML before (from `main`) and after — the loaded data structures are **identical** (diff = comments only); no ecosystem added/removed, no schedule/limit/group/key/value changed; Dependabot behavior is unchanged (still version-update PRs only; alerts enabled, security-updates disabled). **`utilityfog_frontend/` retained as the provisional, CI-tested operational frontend+backend stack** (not declared permanently canonical). No workflow / manifest / dependency / lockfile / setting / Ruleset / branch-protection / Pages / release / alert / issue change; Dependabot PRs #229/#230 observe-only untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Action SHA-pinning + OPA-checksum + Scorecard/CodeQL and all later phases remain UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `237963d`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5C-2D PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5C-2C: retire the entire abandoned `visualization/` frontend+backend stack (draft PR)**: Second frontend-disposition retirement brick, under Kevin's explicit authorization (Jack's reconciliation of the 2B-5C-2C0 preflight; AURA's earlier whole-stack recommendation already survived scrutiny — no further AURA visit). **Lineage**: PR #233 sealed `main` at `8227267140ea9ca38d703f6cb2bea2e90f378182` (retired `web/`; all 3 web Vite alerts auto-cleared `state=fixed` → 0 open Dependabot alerts repo-wide). The **read-only Phase 2B-5C-2C0 whole-stack preflight** (three bounded read-only agents — full-stack capability analyst, cross-stack equivalence/salvage analyst, adversarial retirement skeptic — + parent independent verification) recommended **Disposition A** (retire the whole directory; git history sole preservation). **Whole-stack inventory = 20 tracked files**: launcher/top-level (`__init__.py`, `run_server.py` — uvicorn entry, default `0.0.0.0:8002`); FastAPI backend (4: `backend/{__init__,data_models,simulation_streamer,websocket_server}.py` — WS `/ws` + REST `/api/*`, 12-type message protocol); CRA frontend (14: `react-scripts@5.0.1` `package.json`, `public/index.html`, `src/{index.js,App.js,App.css,index.css}`, `src/components/{AgentMetrics,Dashboard,LoadingSpinner,MemeTimeline,Network3D,StatusBar}.js`, `src/contexts/{SimulationContext,WebSocketContext}.js` — WS client → `ws://localhost:8002/ws`). **Parent corrections that survived independent re-check**: (1) `tests/test_visualization.py` imports the **`vis/`** package (`vis.export`/`vis.dashboard`/etc.), **NOT** the `visualization/` stack — a name-collision trap; **no test/workflow/Makefile imports the `visualization/` package**, so deletion removes no tested surface and breaks no required check; (2) `utilityfog_frontend/quantum_myelin.py` **exists** (it is a `.py` file; an earlier parent `-e` probe without the extension was a false-negative) and `testing_framework` additionally wraps that import in `try/except` with a dummy fallback — so Agent 1's "real simulation" finding stands; (3) the skeptic's "human commit `eab180b`" is **OFF `main`** (not an ancestor of HEAD) and is only a 2-char version-string bump — it does NOT overturn the **100%-auto-commit** (`emergent-agent-e1`, 2025-08-08) history of `visualization/` on `main`. **Retirement rationale**: both backends wrap the **same** `testing_framework.simulation_runner.SimulationRunner` + `test_runner.TestConfiguration` — `visualization/backend` is a **duplicate** of the maintained `utilityfog_frontend/backend` integration, not a unique capability; **0 external consumers / 0 CI gate / 0 deployment**; **FastAPI/uvicorn runtime deps are declared NOWHERE** (no `visualization/requirements.txt`, not in `testing_requirements.txt`/any pyproject) so the stack is not runnable from clean `main` without manual install; the frontend is deprecated CRA. **Salvage**: no item crossed the strict threshold (unique ∧ valuable ∧ absent-elsewhere ∧ hard-to-reconstruct ∧ has-a-real-destination) — the sole candidate, a ~15-line golden-angle fractal-position routine (`simulation_streamer.py:173-201`), is trivial to reconstruct AND has no identified operational destination (utilityfog does not do server-side layout); everything else is commodity React/D3/Three.js or superseded backend. **Git history at `23cc550` is the sole preservation mechanism** — NO archive tag, NO archive branch, NO salvage doc/screenshot/replacement created. **This arc (exactly 20 deletions + 1 addition)**: (1) **deleted the complete tracked `visualization/` directory — 20 files** (listed above); (2) this Session Log entry. **`utilityfog_frontend/` retained as the provisional, CI-tested operational frontend+backend stack** (NOT declared permanently canonical, NOT CI/CD). `vis/`, `testing_framework/`, `crates/uft_ca/`, `scripts/`, all 4 live workflows, `.github/dependabot.yml` (its "three frontends" comment **deliberately NOT edited** — reconciled once, AFTER this retirement lands) **byte-unchanged**; **no lockfile / generated asset / replacement added**; Dependabot PRs #229/#230 observe-only untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. *(Local untracked `visualization/` build debris, if any, left alone — 0 tracked, absent on `main`/any clone.)* main `8227267`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5C-2C PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5C-2B: retire the orphaned `web/` WASM-Dandelion prototype (draft PR)**: First retirement brick of the frontend-disposition track, under Kevin's explicit authorization (Jack's reconciliation of the 2B-5C-2A inventory; AURA's recommendation survived scrutiny with the corrected whole-stack scope for `visualization/`). **Lineage**: PR #232 sealed `main` at `01e86deb11e6d23fd191eef5a7af7b717cfd0e12` (utilityfog frontend Vite 6.4.3 + loopback host; its 3 Vite alerts cleared on rescan). The **read-only Phase 2B-5C-2A inventory** (three bounded agents — `web/` WASM/capability analyst, `visualization/frontend/` design analyst, adversarial retention skeptic — + parent independent verification of every load-bearing claim) produced this disposition. **Parent corrections that survived independent re-check** (the skeptic did its job; the *basis* changed, not the call): (1) the skeptic's "retain `web/` because it is the planned Hermes web/mobile seat" is **FALSE** — `AGENT_HANDOFF.md` §Cross-Device names **Hermes as the mobile Claude Code AI seat** (same digital person as 84, the phone implementation persona that authored #169/#170), with **no relationship to the `web/` directory**; (2) `scripts/dandelion.py` is a **printed `print_wasm_guide()` heredoc**, NOT a functioning regeneration path for `web/` — it targets `wasm-pack … --out-dir ../../wasm_pkg` and exports `CaLattice`, whereas `web/src/wasm.ts:38` dynamically imports `../pkg/uft_ca.js` and expects `WasmLattice` (different dir, different class, never executed); (3) `visualization/` is a **frontend+backend stack** (`visualization/backend/websocket_server.py` + `simulation_streamer.py` + `data_models.py`), so its later retirement must cover the **whole** directory — **left entirely untouched here**. **`web/` retirement rationale**: single-commit prototype (entire history = `870669a` "Phase 10 — The WebAssembly Dandelion", 2026-03-14); **no CI gate** (no root/`web` package.json reaches `ci.yml`; `ui-smoke.yml` path-filters `utilityfog_frontend/frontend/**` only); **no consumer/import** anywhere in maintained source; **no deployment** (no Pages); the **generated WASM package is absent** — `web/pkg/` AND `wasm_pkg/` are both untracked, so `web/` cannot build from clean `main` without a manual `wasm-pack` step; it carried **all 3 remaining open Vite alerts** (HIGH `GHSA-fx2h-pf6j-xcff` + medium `GHSA-v6wh-96g9-6wx3` + medium `GHSA-4w7w-66w2-5vf9`, all `web/package.json`). **Salvage**: no unique material met the strict threshold (unique ∧ valuable ∧ not-elsewhere ∧ hard-to-reconstruct ∧ has-destination) — the only durable asset is the **CA/WASM kernel + bindings, which live in `crates/uft_ca/` (incl. `src/wasm_bindings.rs`) and are untouched**; `web/`'s UI (Three.js `InstancedMesh` voxel renderer, WASM loader, Zustand store) is reconstructable boilerplate-plus-narrow-unique. **Git history at `870669a` is the sole preservation mechanism** — NO archive tag, NO archive branch, NO salvage doc/screenshot/replacement created. **Retirement, not a patch**: removing the manifest is the correct remediation; a Vite 5→6 migration of an orphaned, unbuildable, consumer-less prototype would be a pointless patch-before-deletion. **Alert expectation**: the 3 `web` alerts remain OPEN on a draft and are expected to clear **only after merge + GitHub Dependabot rescan** (manifest removed → alerts drop); none dismissed/snoozed/assigned. **This arc (exactly 1 deletion + 1 addition)**: (1) **deleted the complete tracked `web/` directory — 17 files** (`.gitignore`, `index.html`, `package.json`, `tsconfig.json`, `vite.config.ts`, `src/main.tsx`, `src/store.ts`, `src/vite-env.d.ts`, `src/wasm.ts`, `src/wasm-types.d.ts`, `src/lib/colors.ts`, `src/components/{App,ColorLegend,Controls,GenomeLoader,MetricsDashboard,VoxelRenderer}.tsx`); (2) this Session Log entry. **`utilityfog_frontend/` retained as the provisional, CI-tested operational frontend stack** (Vite `^6.4.3` / plugin-react `^4.7.0` / host `127.0.0.1`, `ui-smoke.yml` Playwright gate — NOT CI/CD, NOT permanently canonical). **`visualization/` (whole stack) left completely untouched** for its own later retirement arc; `.github/dependabot.yml`'s "three frontends" comment **deliberately NOT edited** (to be reconciled once, after the visualization disposition lands). `crates/uft_ca/`, WASM bindings, `scripts/dandelion*.py`, all 4 live workflows, Rulesets/branch-protection/Pages/releases/settings **byte-unchanged**; **no lockfile / generated WASM / replacement file added**; Dependabot PRs #229/#230 observe-only untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `01e86de`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5C-2B PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5C-1: utilityfog frontend Vite 6.4.3 + dev-server loopback security patch (draft PR)**: Narrow remediation of the maintained frontend's Vite advisories, under Kevin's explicit authorization (Jack relay; no AURA needed for this patch). **Lineage**: the 6 open Dependabot alerts = **three Vite advisories duplicated across two manifests** (NOT 6 distinct bugs): HIGH `GHSA-fx2h-pf6j-xcff` (fs.deny bypass on Windows; fix vite≥6.4.3) ×2, medium `GHSA-v6wh-96g9-6wx3` (launch-editor NTLM/UNC; ≥6.4.3) ×2, medium `GHSA-4w7w-66w2-5vf9` (optimizedDeps `.map` traversal; ≥6.4.2) ×2 — on `utilityfog_frontend/frontend` + `web`. The 2B-5C-0 audit + 2B-5C-0.1 confidential preflight established: **no 5.x backport exists → Vite 5→6.4.3 major migration is the only fix**; migrations are empirically clean (disposable Node-24 builds; plugin Vite-6 peers verified: `@vitejs/plugin-react@4.7.0`→`^6`); `utilityfog_frontend/frontend` is the only **living, CI-tested** frontend (`ui-smoke.yml`, path-filtered, runs Node 20 + Playwright), `web` is an orphaned not-reached prototype. **`.env.local` was confidentially classified = NON-SECRET** (2 vars, both `VITE_`-prefixed/public-by-design; 0 secret-scanning alerts; no credential/high-entropy values) → deliberately UNTOUCHED here (not a credential incident; no rotation/history-rewrite). **This arc (exactly 3 tracked files)**: (1) `utilityfog_frontend/frontend/package.json` — `vite ^5.3.0→^6.4.3`, `@vitejs/plugin-react ^4.2.1→^4.7.0` (no other dep/script/metadata change); (2) `utilityfog_frontend/frontend/vite.config.ts` — `host: true → host: '127.0.0.1'` (removes unnecessary LAN exposure; Playwright already targets `localhost:4173` so it stays green; `vite build` ignores server config); (3) this Session Log entry. **`.env.local` + `.gitignore` byte-unchanged; NO lockfile added; NO TypeScript/lint cleanup** (the pre-existing utilityfog TS-lint errors block only `npm run build`, which the `ui-smoke` gate does NOT run — it runs `npm run test:ui` Playwright — so they are out of scope and not pulled into this security patch); NO `web`/`visualization` change; NO `npm audit fix`. **Authoritative compatibility gate = the `ui-smoke` workflow on this PR head (Node 20 + npm install + playwright chromium + test:ui)** — local Node is v24 only (no Node 20 / no browser cache), so local build is supplemental, not authoritative. **`web` + `visualization/frontend` remain DEFERRED for a later curator decision** (retain-and-patch vs retire — one bounded AURA question, AFTER utilityfog's alerts clear); `web` keeps its 3 alerts meanwhile. Action SHA-pinning (PR-A, the 3A 9-action/21-occurrence verified-SHA map) + OPA checksum verification (PR-B, RETAIN 0.59.0 + embedded `5f615343…` digest) remain PARKED. Dependabot version-config + observe-only PRs #229/#230 untouched; preserved PRs #17/#25/#52–#58 + archive tags (`→3df3d33`/`→33449a7`) intact; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery untouched. main `23603ab`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5C-1 PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-16 (desktop, 84) — Phase 2B-5B-3B-1: retire obsolete `ci-seeder.yml` (draft PR)**: First implementation brick of the supply-chain hardening track, under Kevin's explicit authorization (AURA curator ruling + Jack reconciliation). **Lineage**: Phase 2B-5B-2B sealed `main` at `ce1e462d598b7624f80d894c8e4e31ddb8f2619d` (`.github/dependabot.yml` live: github-actions `/` + cargo `/crates/uft_ca`, monthly/limit-2/grouped). The read-only **Phase 2B-5B-3A audit** found 5 live workflows, **9 unique external Actions / 21 `uses:` occurrences**, and **1 unverified OPA binary download** (agent-safety.yml, v0.59.0, no checksum — official `opa_linux_amd64_static.sha256` = `5f615343…aced0cc3` exists, RETAIN 0.59.0 + embedded-digest verify proposed). Dependabot then generated **observe-only Cargo-major PRs #229 (rand_xoshiro 0.6→0.8) + #230 (rand 0.8→0.10)** — left untouched (own future relay). **AURA's curator ruling: RETIRE `ci-seeder.yml`** (architecturally spent). Evidence: **0 recorded workflow runs** (dormant since creation `dbd5e06`); manual-only (`workflow_dispatch`), never produced a live restoration PR; its purpose (recreate the 8 supply-chain stub workflows) is **spent** — those were already individually dispositioned; it carried unnecessary `contents: write` + `pull-requests: write` and depended on `peter-evans/create-pull-request@v6`; **not a branch-protection required check** (required = `[agent-safety, verify]`); no other file references it. **This arc (2 tracked files)**: (1) **deleted `.github/workflows/ci-seeder.yml`**; (2) this Session Log entry. **Git history is the preservation mechanism** — NO archive tag, NO quarantine/replacement copy, NO docs copy created; historical branch/commits/review-history untouched. **Exactly 4 live workflows remain**: `agent-safety.yml`, `ca-search.yml`, `ci.yml`, `ui-smoke.yml`. No other workflow / permission / setting / Ruleset / branch-protection / dependency / lockfile / alert / PR changed; no Action pinned; no permissions block added elsewhere; Dependabot config unchanged; PRs #229/#230 untouched; no workflow dispatched; no issue. **Still PROPOSED but UNSTARTED**: Action SHA-pinning (the 9-action/21-occurrence map from 3A, verified SHAs) and OPA checksum verification. **Reconciled Rust-toolchain policy (AURA/Jack)**: retain BOTH actions for now (`dtolnay/rust-toolchain@stable` in ci.yml + `actions-rust-lang/setup-rust-toolchain@v1` in ca-search), pin their implementations later, **retain rolling Rust stable** — and a future full-SHA pin of `dtolnay/rust-toolchain` MUST explicitly specify `toolchain: stable` (no claim that either Rust Action is generically faster or architecturally superior). Phase 2B-5C and later (CodeQL/Scorecard/packaging) remain unstarted; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `ce1e462`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-3B-1 PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-15 (desktop, 84) — Phase 2B-5B-2B: minimal Dependabot version-update configuration (draft PR)**: The deliberately-parked next brick, now unblocked after the PyO3 fix landed, under Kevin's explicit authorization (Jack relay). **Lineage**: PR #227 (PyO3 0.29 migration) sealed `main` at `d1bfb08c71bd2d0a05767d5aa27d57f7638cb20b`; on rescan the **3 PyO3 Dependabot alerts auto-cleared (state=fixed)**; the **2 Vite medium alerts remain OPEN and deferred** (frontend surfaces, 2B-5C). The earlier 2B-5B-2B attempt correctly **stopped before file mutation** when the high PyO3 alert appeared; with that resolved, the bounded config is now created. **Scope (from the 2B-5B-2A dependency-surface inventory)**: only the two maintained-core surfaces — `github-actions` at `/` (the 5 live workflows) and `cargo` at `/crates/uft_ca` (exercised by required CI). All other surfaces (vanguard-mcp; the 3 frontends `web`/`utilityfog_frontend`/`visualization`; `src/uft_orch/ca/requirements.txt`; `crates/uft_ca/pyproject.toml`; `testing_requirements.txt`; FT/Specify remnants) remain **deferred/excluded** pending the maintained-surface inventory (2B-5C). **Exact config (`.github/dependabot.yml`, NEW)**: `version: 2`; two `updates` entries — github-actions `/` (monthly, `open-pull-requests-limit: 2`, group `actions` patterns `["*"]`) + cargo `/crates/uft_ca` (monthly, limit 2, group `uft-ca-minor-patch` `update-types: [minor, patch]` — so Cargo **major** updates remain visible as separate PRs, not silently grouped). No labels/reviewers/assignees/registries/ignore/allow/target-branch/commit-message/rebase/cooldown/auto-merge/security-update config. **Feature distinction (recorded)**: Dependabot vulnerability **alerts remain ENABLED**; **security updates remain DISABLED**; this file configures **routine version-update PRs only**; adding it on a draft branch does **NOT** activate version updates on `main` — Dependabot may begin opening update PRs only after a future merge + GitHub processing; **no dependency is updated in this arc**. **Validation**: YAML parsed (PyYAML) — version 2, exactly 2 entries, ecosystems `[github-actions, cargo]`, dirs `[/, /crates/uft_ca]`, both monthly, both limit 2, GA all-pattern group, Cargo minor/patch group; `.github/dependabot.yml` did not previously exist on `main`; no manifest/dependency/workflow/lockfile changed. **Two tracked files** changed: `.github/dependabot.yml` (new) + this Session Log entry. **No dependency / lockfile / workflow / setting / alert change**; no Dependabot-generated PR appeared during the draft-only arc; the 2 Vite alerts untouched; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Phase 2B-5C and all later security arcs (CodeQL/Scorecard/action-pinning/OPA-checksum/packaging) remain UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `d1bfb08`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-2B PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-15 (desktop, 84) — Phase 2B-5B-2D: bounded PyO3 0.20→0.29 security migration of `uft_ca` (draft PR)**: Implementation arc closing the maintained-core pyo3 Dependabot alerts, under Kevin's explicit authorization (Jack relay) with Outcome A (immediate bounded migration). **Lineage**: PR #226 sealed `main` at `b0f9716f58d81818ffe39718159237130d0d669f` (SECURITY.md + PVR live); Phase 2B-5B-2B0 enabled Dependabot **alerts** (settings-only) — dep graph reported 0 alerts then asynchronously populated **5** (1 high + 1 med + 1 low pyo3 in `crates/uft_ca`, 2 med vite in frontends); Phase 2B-5B-2B (version-update config) **stopped before file mutation** when the high appeared; Phase 2B-5B-2C was a no-mutation 3-agent reachability + migration preflight that found **none of the 3 vulnerable PyO3 APIs reachable** (uft_ca crosses only Rust-native types; local-only, no untrusted Python input) and a disposable `cargo check` on 0.29 surfaced **one concentrated compatibility seam** (the legacy `&PyModule` signature → 13 errors, one root cause). **This arc (2D) — exact changes (3 tracked files)**: (1) `crates/uft_ca/Cargo.toml` pyo3 `0.20`→`0.29` (features `extension-module`/optional unchanged); (2) `crates/uft_ca/src/lib.rs` module decl → `#[pymodule(gil_used = true)] fn uft_ca(_py: Python<'_>, m: &Bound<'_, PyModule>)` — the **`gil_used = true` is the mandatory conservative declaration** (preserves the existing GIL-bound contract; does NOT claim free-threaded-Python compatibility, which was never audited); (3) **explicit `PyList::new` boundary conversion** — a feature-gated `py_u8_list(py, Vec<u8>) -> PyResult<Bound<'_, PyList>>` helper + `use pyo3::types::PyList`, applied at the **12 Python-exposed state-vector return sites** (GraphLattice.step/get_states; MultiStateGraphLattice.step/get_states/step_n/step_par/step_par_n/step_auto/step_auto_n/snapshot; the two `#[pyfn]` step_lattice_py/create_graph) — each now takes a hidden `Python<'py>` token and returns `PyResult<Bound<'py, PyList>>`, wrapping the kernel's unchanged `Vec<u8>` so the public API stays mutable `list[int]` (PyO3 ≥0.23 otherwise maps `Vec<u8>`→`bytes`); the CA kernel + internal Rust `Vec<u8>` APIs are untouched; `Vec<u8>` INPUT args (new/set_states) and scalar/HashMap/`Vec<usize>` returns are NOT affected; (4) this Session Log entry. The five `#[pyfn(m)]` were **RETAINED** unchanged (compile with deprecation warnings only — NOT modernized to `#[pyfunction]`/`wrap_pyfunction!` in this security PR, per scope rule); both `#[pyclass]`/`#[pymethods]`, all CA algorithms, RNG/seed/transition/threshold logic, public Python names, Python support range (`>=3.8,<3.13`), WASM code, and feature defaults are **unchanged**. **Sequence**: the initial compile-only migration (commit `7d576cc`) passed local Rust gates but **CI `verify-python` FAILED** — pyo3 ≥0.23 maps `Vec<u8>`→Python **`bytes`** (was `list[int]`), so every state-returning function broke the maintained contract tests (`assert states==[3,3,3,2]` got `b'\x03\x03\x03\x02'`; `'bytes' object does not support item assignment`). The CI determinism/contract gate (Jack's mandate) **prevented an unintended public-API regression** before it could reach `main`. Per the established decision (Kevin/Jack) the contract is **preserved as mutable `list[int]`** via the explicit `PyList::new` boundary conversion above — **tests and callers were NOT changed to accept bytes**. **Re-validation (local Rust)**: `cargo check --features python` = **0 errors / 7 warnings** (5 `#[pyfn]` PYFN_ATTRIBUTE deprecations + 2 pre-existing unused-var; `gil_used = true` + PyList helper accepted); `cargo test --release` (no-python CA-logic regression) = **86 passed / 0 failed**; `cargo test --release --features python` = **LINK error (LNK1104)** — the known extension-module test-binary linking limitation on Windows (a test executable needs libpython; `extension-module` deliberately skips it) — **recorded, not worked around** (the crate was NOT reconfigured to force it); `cargo fmt --check` flags only pre-existing `wasm_bindings.rs` drift (untouched). The **authoritative Python gate is CI `verify-python`** (Python 3.12 + maturin build + `pytest tests/` incl. the sequential/parallel-equivalence + memory-determinism fences); local env is Python 3.14 + no maturin (3.14 is not authoritative). **Gemini correction**: a review claim that `#[pyfn]` was removed / cannot compile under 0.29 is empirically false — `cargo check --features python`, the CI maturin build, and pytest all compiled/ran (deprecation warnings only, not a missing-macro error); the five `#[pyfn(m)]` are retained and modernization is out of scope. **Lockfile guardrail**: cargo generated an untracked (gitignored) `crates/uft_ca/Cargo.lock` during validation → removed; **no Cargo.lock staged/committed**; lockfile policy unchanged. **Alerts**: none dismissed/snoozed/assigned; the 3 pyo3 alerts remain OPEN until the fix reaches `main` and GitHub rescans (open alert on an unmerged draft is NOT a failure); the **2 vite alerts remain DEFERRED** with the frontend surfaces (2B-5C), untouched. Exactly **three tracked files** changed; dependency diff = pyo3 `0.20`→`0.29` only; no other dependency, no algorithm/RNG/kernel change, **public Python `list[int]` contract preserved** (boundary-converted, not changed to bytes; tests unchanged), no workflow/Ruleset/branch-protection/Pages/release/setting change, no issue. **Phase 2B-5B-2B version-update config remains UNSTARTED** (independent of this fix); later security phases (2B-5C maintained-surface inventory, 2B-5D CodeQL, 2B-5E packaging) unstarted; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `b0f9716`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-2D PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-15 (desktop, 84) — Phase 2B-5B-1: truthful security policy (`SECURITY.md`) + Private Vulnerability Reporting enabled**: First independently authorized slice of Phase 2B-5B (the repository-health cleanup campaign was already SEALED at `db1af8a`). Two sequential stages under Kevin's explicit authorization (Jack relay). **Background**: the prior Route-C decision correctly created NO files/branch because no safe reporting route existed (PVR disabled; no public owner contact; public-issue route unsafe). **Stage 1 — settings (one authorized mutation only)**: parent verified before-state `private-vulnerability-reporting` = `{"enabled":false}`, then enabled it via `PUT repos/Goldislops/UtilityFog-Fractal-TreeOpen/private-vulnerability-reporting` (no body); verified after-state `{"enabled":true}` — the private **Report a vulnerability** route is now live on the Advisories page. Secret scanning + push protection remained `enabled` (unchanged); Dependabot security updates remained `disabled`; no other setting, workflow, branch, PR, issue, release, Page, or file changed; **no private contact information exposed**. **Stage 2 — Phase 2B-5B-1** (no re-run of the two prior Route-C agents; parent re-verified every policy statement live): created **`SECURITY.md`** — a modest, plain-language policy. Supported scope = active maintenance on `main` only; preserved historical branches / archive tags / old unmerged PRs / experimental material retained-but-not-supported; historical releases not guaranteed fixes. Reporting route = **GitHub Private Vulnerability Reporting only** (no public-issue disclosure; no personal email). Response posture = best-effort, solo-maintained, **no SLA, no bounty, no fixed response time**. Current verified controls cited = CI verify/verify-python, OPA Agent-Safety, GitHub secret scanning, push protection, PVR; explicitly NOT claimed = CodeQL / OpenSSF Scorecard / Dependabot PR automation / container scanning / SBOM-provenance / automated release security. Good-faith-research + Non-goals sections included (no broad legal safe-harbour; no third-party-testing authorization). **Validation before commit**: `SECURITY.md` contains no email/phone/personal contact; does not instruct public-issue reporting; every named control verified enabled; no fixed-time/bounty/safe-harbour promise; exactly two files changed; preserved PRs untouched. Two-file diff = create `SECURITY.md` + this newest-first Session Log entry on branch `docs/phase2b5b1-private-reporting-policy`. No code/dependency/README/workflow/Ruleset/branch-protection/Pages/release/package/tag/issue change beyond the one authorized PVR toggle + these two doc files; preserved PRs #17/#25/#52–#58 untouched; archive tags intact (`→3df3d33`/`→33449a7`); the rest of Phase 2B-5B (5B remaining items + 2B-5C…5E) remains UNSTARTED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `db1af8a`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5B-1 PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-5A.1: historical security-automation PRs #72/#75 CLOSED UNMERGED (branches preserved)**: Two sequential stages under Kevin's authorization (Jack relay). **Stage 1 — Phase 2B-5A sealed**: the salvage-plan draft PR #224 received one accurate Gemini correction (six internal cross-references `§13.3`/`§13.5`/`§13.6` → the actual Section-12 amendment numbers `§12.3`×3 / `§12.5`×1 / `§12.6`×2; Gemini review thread replied + resolved); **PR #224 squash-merged to `main` as `609e4ca783b42d750a12bf737c63bcb714be15f6`** (nineteenth one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `bf236cd`; checks green); closeout branch `docs/phase2b5a-security-automation-salvage-plan` deleted remote+local; main synced. The **canonical salvage plan `docs/SECURITY_AUTOMATION_SALVAGE_PLAN_2026-06.md` is now on `main`** (six bounded read-only agents + parent verification + AURA architecture/curator review + Jack's 7 reconciliation amendments; packaging Option A local-first; Pages Option A static/manual; maintained-scope provisional; full reconciled salvage matrix). **Stage 2 — Phase 2B-5A.1** (PR-disposition arc; **no agent/AURA re-run** — the merged plan is the canonical evidence): parent re-verified live with **no material drift** (heads at pins; recoverable #72 commits `08072c9`/`1077741` reachable; plan accurately records #72 live-workflow deletion + recoverability + no-archive-tag + #75 per-workflow blockers + Option A decisions + matrix). **PR #72** (`hardening/ci-docs-main`@`a4699feb`) **CLOSED UNMERGED** (obsolete/unsafe implementation): head would delete live `agent-safety/ci/ui-smoke`; useful workflow drafts recoverable from `08072c9`/`1077741` (`pypi-publish.yml` unique to #72); FT-era packaging/Docker/README/PDFs/TestPyPI obsolete-or-unsafe as impls; concepts classified for fresh later work; one PR-specific evidence comment ([#72 issuecomment-4701742189](https://github.com/Goldislops/UtilityFog-Fractal-TreeOpen/pull/72#issuecomment-4701742189)); NOT "completed". **PR #75** (`garden/prod-workflows`@`822dced2`) **CLOSED UNMERGED** (obsolete/not-planned implementation): 8 workflows not suitable for the current monorepo (unconfigured repo-wide quality gates always-red; placeholder `your_package` release-smoke; stub-capable docs-deploy that could clobber live `gh-pages`; absent container/publication architecture; permission + immutable-action-pinning redesign required); concepts retained as future bounded work; one PR-specific evidence comment ([#75 issuecomment-4701743266](https://github.com/Goldislops/UtilityFog-Fractal-TreeOpen/pull/75#issuecomment-4701743266)); NOT "completed". Both closing comments explicitly preserve that security automation / Dependabot / CodeQL / Scorecard / static analysis / supply-chain hardening are NOT obsolete in general. **No implementation of any salvage-plan item occurred.** Post-closure verification: only #72/#75 changed state; both CLOSED/`merged=false`; heads unchanged (`a4699feb`/`822dced2`); **both branches preserved at pins**; recoverable #72 objects reachable; review histories untouched (#72 10 threads / #75 4 threads, 0 resolved) plus exactly one new top-level closing comment each (8→9 each); **remaining-open PR inventory verified live = #17/#25/#52–#58 ONLY** (stale-PR backlog now cleared; only the deliberately preserved Phase-2A curator + FT-feature branches remain). No workflow / repository setting / permission / Ruleset / branch-protection / Pages / `gh-pages` / release / package / issue / tag / code / doc change (beyond this closeout entry); no branch deleted except the merged #224 closeout; archive tags intact (`→3df3d33`/`→33449a7`); all earlier proposed future ideas (incl. 2B-5B…5E security baseline) remain UNCREATED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. main `609e4ca`. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5A.1 closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-5A: security/supply-chain automation salvage-DESIGN review of PRs #72/#75 (read-only; design record only, NO closures)**: Two sequential stages under Kevin's authorization (Jack+AURA relay). **Stage 1 — Phase 2B-4F sealed**: closeout **PR #223 squash-merged to `main` as `76566555a4aa3855511d6fdc67e379133064fc08`** (eighteenth one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `b3537d7`, Gemini review no-findings/COMMENTED, checks green); closeout branch `docs/phase2b4f-branching3-pr-closeout` deleted remote+local; main synced. **PR #89 remains CLOSED/unmerged** with branch `exp/branching3-sweep`@`3c89d98` + 5 review threads (0 resolved) preserved. **Stage 2 — Phase 2B-5A** (salvage-design arc; targets PR #72 `hardening/ci-docs-main`@`a4699feb` + PR #75 `garden/prod-workflows`@`822dced2`, strictly READ-ONLY — no close/merge/comment/edit/rebase/branch-op): reviewed by **six bounded READ-ONLY agents** (1 #72 branch-history/recoverability, 2 #75 workflow-by-workflow, 3 current security/CI posture, 4 threat-model/least-privilege, 5 repo-fit/maintainability, 6 adversarial salvage skeptic). **Parent independently re-verified** load-bearing facts on `main`: #72 head diff DELETES live `agent-safety/ci/ui-smoke` workflows; the 8 'promised' supply-chain workflows are fully RECOVERABLE from commits `1077741`/`08072c9` (head stripped them via 'permission-safe push'; `pypi-publish.yml` unique to #72, `quality.yml` unique to #75); main has NO ruff/mypy/bandit config and NO `hypothesis` dep (so #75 `quality.yml` repo-wide gates would be always-red); NO root package (the `your_package` release-smoke is a verified placeholder with no target); NO `Dockerfile`/docs-build on main (live `gh-pages`@`ca18a6d` is built from a `mkdocs.yml` that lives on the gh-pages branch — #75 `docs-deploy` would clobber it with a 'No docs build configured' stub); branch protection = 2 checks (`agent-safety`,`verify`) / 0 reviewers / admins-not-enforced; #72 `pyproject` name=`utilityfog-fractal-tree` py>=3.9 (FT-era), Dockerfile CMD=legacy `cli_viz`, `pypi-publish`→TestPyPI whole-repo `find_packages`; live `agent-safety.yml` fetches OPA from GitHub releases (no checksum). **AURA bounded architecture/curator review** (no handback needed): local-first research repo; **packaging Option A** (no PyPI/GHCR/packaged product now); **Pages Option A** (keep gh-pages static/manual); proportional lean security baseline; approved sequencing (2B-5B baseline → 2B-5C scoped quality after a maintained-surface inventory → 2B-5D scoped CodeQL → 2B-5E packaging-dependent automation; separate settings/docs arcs); #72/#75 preserved as quarantined historical branches; NO implementation authorized. **Jack reconciliation amendments (override contradictory AURA text)**: (1) maintained-code scope PROVISIONAL — core=`src/uft_orch`+`crates/uft_ca`, experimental=`scripts`+CA dirs, UNCERTAIN=`agent/`+`UtilityFog_Agent_Package/`+`utilityfog_frontend/frontend/`+other `crates/*` (needs a dedicated maintained-surface inventory; do not canonize), legacy=FT/Specify remnants; (2) Dependabot is MANIFEST-directory-scoped not source-dir-scoped (enumerate live manifests first); (3) Scorecard `id-token:write` NOT removed unconditionally — required when `publish_results:true`, scope to the job, pin SHAs; (4) CodeQL trigger `paths` ≠ analysis scope (needs CodeQL config `paths`/`paths-ignore`); (5) container shell-injection claim NARROWED to verified blockers (unquoted interpolation, PR-controlled build, over-broad perms, mutable tags, no Dockerfile/target); (6) GitHub-native setting availability requires a later read-only audit (not assumed); (7) omit the irrelevant PR #206 'squared away' remark. **Reconciled salvage matrix**: ADOPT-SOON = truthful `SECURITY.md`, OpenSSF Scorecard (perms/publication design deferred to 2B-5B); REDESIGN = Dependabot, CodeQL, Ruff, Mypy, OPA-download-verification; DEFER = Bandit, Hypothesis, docs/Pages automation, SBOM, container, nightly-bench, release-smoke concept, PyPI, status badges, branch-protection/Ruleset changes; DISCARD impl (concepts not rejected forever) = #72 live-workflow deletion + FT Dockerfile + README rewrite + 6 PDFs + historical PyPI workflow, and #75 `your_package` release-smoke + container workflow + stub docs-deploy + unconfigured repo-wide lint gates; PRESERVE-history (branch suffices, no archive tag) = #72 recoverable commits `08072c9`/`1077741`, both branches+review histories, FT/v0.1 changelog/roadmap/versioning docs, aspirational branch-protection doc. **Output**: created `docs/SECURITY_AUTOMATION_SALVAGE_PLAN_2026-06.md` (design record) + this Session Log entry on branch `docs/phase2b5a-security-automation-salvage-plan`; two tracked files only. **#72/#75 remain OPEN/unmerged/UNTOUCHED at pinned heads** (#72: 8 comments/10 unresolved threads; #75: 8 comments/4 unresolved threads — unchanged); recoverable #72 workflow commits preserved; **no workflow / repository setting / permission / Ruleset / branch-protection / Pages / release / package / issue / PR-state / branch change** occurred; **no implementation or closure was authorized** (this draft authorizes no future work). PRs #17/#25/#52–#58 OPEN; archive tags intact (`→3df3d33`/`→33449a7`); all earlier proposed future ideas remain UNCREATED; Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched. Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-5A closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4F: CA branching-rule experiment PR #89 closed unmerged (obsolete / superseded implementation)**: Fresh-thread relay under Kevin's direct conditional authorization (close if the obsolete/superseded rule is fully satisfied; else leave open and report the precise reason). **Stage-1 confirmation (prior relay's Phase 2B-4E)**: verified already complete before any action — PR #222 squash-merged to `main` as `0ed86359bbefbc997e7967032deb3741478e9e8e`; its closeout branch `docs/phase2b4e-garden-pr-closeout` deleted (remote 404 + absent local); the corrected distinction stands in the 2B-4E entry below (the spec workflow `speckit-validate.yml` is absent from `main` and never landed in `main` history — it exists only on the preserved #77 branch); PRs #77/#78/#79 remain closed-unmerged with branches preserved; `main` confirmed at `0ed8635`. **Phase 2B-4F** (PR-disposition arc; target = PR #89 ONLY, `exp/branching3-sweep`@`3c89d98ee72989ac6e2cf099cd0f9a7eeef4383f`, "branching3 rule sweep v1/v1b", adds exactly 4 config files): reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents strictly read-only, parent performs every Git/GitHub mutation centrally and sequentially): (1) schema/executability analyst, (2) current-CA-lineage analyst, (3) scientific-value/reproducibility analyst, (4) adversarial closure skeptic. **All four concurred on closure; no AURA escalation** (no live architectural ambiguity — the research question is well-posed; the failure is purely schema/lineage). The skeptic's seven refutation attempts all FAILED to block closure. **Parent independently re-verified the load-bearing evidence** from `main`: **schema incompatibility** — the live runner `src/uft_orch/ca/runner.py` requires experiment YAML with a top-level `experiment:` object (`ExperimentConfig.from_yaml` hard-accesses `experiment.name`/`rule`/`steps`) and rule TOML with `[rule]` holding `name`/`states`/`neighborhood`/`transition` (`RuleSpec.from_toml`); both PR YAMLs are flat → `KeyError('experiment')` at load, both PR TOMLs place `name` under `[metadata]` with `states` in a separate `[states]` table and no `transition` key → `KeyError('name')` at load; the PR's `[rule.birth].counts`/`[rule.survival].counts` form has no consumer (the live binding `uft_ca.step_lattice_py` is hardcoded to a single `conway_3d_rule` at `crates/uft_ca/src/lib.rs:1655`); metrics are configured twice + inconsistently (YAML `metrics:` list vs TOML `[metrics]` `track_*` booleans; `alive_ratio`/`giant_component`/`complexity`/`survival_rate` unrecognized by `compute_metrics`) — independently flagged HIGH-priority by the 5 preserved Gemini review threads. **Blocker chronology** — PR #89 was explicitly "BLOCKED — awaiting PR #81"; **PR #81 verified MERGED 2025-10-07T13:42:50Z (merge commit `08a5d2b`)**, yet PR #89 was last updated 2025-10-03 and never advanced/run/rebased after the blocker cleared. **No experiment ran and no results exist** — the branch adds only the 4 config files (no deletions); no `metrics.csv`/`final_state.npy`/`summary.csv`/run-artifact/written-conclusion anywhere; only generic CI + Agent-Safety runs on the branch (2025-10-03), no `ca-search` experiment dispatch; the PR's "Expected Results" table is hypothesized, not measured. **Supersession** — `ca/experiments/branching-3.yaml` is a working supported-schema branching experiment and the rule lineage has evolved to the multi-state `ca/rules/example.toml` (`branching-growth-v0.7.5`: VOID/STRUCTURAL/COMPUTE/ENERGY/SENSOR + Phase-10.5/10.6 thermodynamics). **Disposition**: one PR-specific evidence comment posted first, then **PR #89 closed unmerged (explicitly NOT "completed")**; verified post-close: state=CLOSED, `merged=false`, head unchanged at `3c89d98…`, branch `exp/branching3-sweep` preserved at that exact SHA, exactly one new top-level comment (1→2), all 5 inline review threads preserved (0 resolved/untouched). **Surviving research question — reported only, UNCREATED**: the B4/S23-vs-B3/S2 branching baseline comparison is legitimate and trivially re-authorable in the current schema (a 2-state `[rule]` with `name`/`states`/`neighborhood`/`transition` + a `params.transitions` mapping, plus an `experiment:`-wrapped YAML) — future opt-in work; **no replacement issue or file was created**. The closing comment explicitly preserves that CA experimentation, life-like/binary-rule baselines, and rule searching are **NOT** obsolete in general. **Deliberately untouched / preserved**: **PRs #72/#75 remain OPEN/untouched** (PR #75 = own security-workflow arc; PR #72 = branch-history salvage-scrutiny-first; both reserved for a later security/supply-chain salvage-design arc); PRs #17/#25 + #52–#58 remain OPEN; PR #81 + current CA infrastructure (`ca-search.yml`, CA rules, experiment files, runner code, Rust kernel, artifacts) unchanged; both archive tags intact (`→3df3d33`, `→33449a7`); all prior proposed future issues (2 OPA-fixture/PR-comment ideas, 2 garden lint-gate/real-badge ideas) remain UNCREATED. **No code / rule / experiment / runner / workflow / CI / artifact / issue / setting / branch deletion** occurred; no workflow dispatch or experiment execution; only PR #89's state changed. **Lane A parked; Swarm Hunter unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) untouched.** Model seat: Opus 4.8 (256K, desktop/84), no routing banner throughout. This Phase 2B-4F closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4E: garden-era PRs #77/#78/#79 closed unmerged (obsolete/not_planned)**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-4D sealed**: PR #221 received one accurate Gemini correction — the Phase-10 WASM cfg-guard shorthand was corrected to the exact live form `#[cfg(not(target_arch = "wasm32"))]` (matching `crates/uft_ca/src/lib.rs`), scoped to the 2B-4D entry only; Gemini thread replied + resolved. PR #221 then **squash-merged to `main` as `81d5dd7`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `a528bb7`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b4d-codex-reroll-pr-closeout` deleted (remote + local); `main` synced clean. (Routine note: a transient classifier-unavailable Bash notice mid-stage was treated as a server notice, not a routing change — one retry succeeded.) **Stage 2 — Phase 2B-4E** (PR-disposition arc): the three pre-pivot garden-era PRs reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a #77 specification-seed analyst, a #78 quality-workflow analyst, a #79 README/badge analyst, and a cross-trio adversarial skeptic. All four concurred on closure; **no AURA escalation** (the spec-driven methodology was already evaluated and retired; main retains the historical SpecKit bootstrap; no live governance requirement depends on these). **Parent independently re-verified** before mutating: on `main`, `spec/` and `.github/workflows/speckit-validate.yml` are absent from `main` and never landed in `main` history (the workflow exists only on the preserved #77 branch), CODEOWNERS absent, uppercase `PULL_REQUEST_TEMPLATE.md` present, no `utilityfog/` package, no Hypothesis/ruff/mypy config, workflows = agent-safety/ca-search/ci-seeder/ci/ui-smoke only. **Per-PR disposition (all closed unmerged; none retained; none "completed")**: **#77 (`garden/spec-seed`, `aa121c6`)** — its Unity `FractalTree` spec target never existed; its `speckit-validate.yml` runs `pip install speckit` — `speckit` on PyPI is an UNRELATED spectral-analysis library, not the Spec Kit CLI (`specify-cli`) — so it validates nothing; its README rewrite references nonexistent `utilityfog.*`/`src.*` modules and would REGRESS main's Medusa/Phase-18 README; its CODEOWNERS is content-free (single-owner repo) and its lowercase `pull_request_template.md` duplicates/collides with the uppercase one. **#78 (`garden/garden-gate`, `f5b1290`)** — Node+Python verification already maintained by `ci.yml`; its bundled `tests/property/test_roundtrip.py` does `from hypothesis import given` and lands under `tests/` (main's `pytest.ini` `testpaths = tests`) but `hypothesis` is not a dep — so merging would **break the green `verify-python` gate at collection**; `ruff check . || exit 1` over an unconfigured tree would be always-red; the SARIF upload is dead (no `ruff.sarif` ever generated). **#79 (`garden/status-badges`, `2bb3d82`)** — all its badge targets (`quality`/`codeql`/`garden-gate`/`sbom`/`container`/`docs-deploy`/`scorecard` + the pre-existing dead `phase3-ci`) are absent from `main` (real workflows are the 5 above); its README rewrite imports nonexistent `src.*` modules and would regress current docs; `CONFLICTING`. Each got **one PR-specific evidence comment**. **Surviving ideas — reported only, UNCREATED**: (1) a tuned, scoped ruff/mypy/property-testing gate with `hypothesis` properly added (from #78 — fresh current-era work, not this CI-breaking branch); (2) a status-badge block for the 5 real workflows, plus removing the existing dead `phase3-ci.yml` badge in main's README (from #79). **Branches PRESERVED** at pinned heads (`garden/spec-seed`@`aa121c6`, `garden/garden-gate`@`f5b1290`, `garden/status-badges`@`2bb3d82`); review histories intact (4 + 4 + 6 threads, **0 resolved/untouched**); reopenable. **Deliberately untouched / preserved**: **deferred PRs #72/#75/#89 OPEN/untouched** (PR #75 = own security-workflow arc; PR #72 = branch-history salvage-scrutiny-first; PR #89 = separate obsolete CA-experiment review); PRs #17/#25 + #52–#58 OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); both earlier proposed token-free issues remain UNCREATED. **No code / workflow / CI / README / test / template / CODEOWNERS / package / setting / branch-deletion** occurred (one pre-existing Phase-19 stash + 5-tag set left as found); only the three target PRs' state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-4E closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4D: superseded Codex CA re-roll PRs #112/#113 closed unmerged (current-engine, high evidence bar)**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-4C sealed**: PR #220 (no correction needed — Gemini no findings) **squash-merged to `main` as `e34ebd6`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `65073f0`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b4c-phase3-release-pr-closeout` deleted (remote + local); `main` synced clean. **NARRATIVE CORRECTION (Jack):** my prior 2B-4C note overstated that the stale-PR backlog was "essentially cleared" — it is NOT. Still-open stale candidates: **#72** (needs careful salvage scrutiny before any branch deletion — branch-history material), **#75** (potentially salvageable; own security-workflow arc), **#77/#78/#79/#89**, plus #112/#113 (handled here). Preserved-by-decision: #17/#25 (Phase 2A curator) + #52–#58 (FT feature branches). **Stage 2 — Phase 2B-4D**: the two current-engine Codex re-rolls reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a #112 equivalence analyst, a #113 equivalence analyst, a regression/chronology analyst, and an adversarial closure skeptic. All four concurred on closure; **no AURA escalation** (no unique still-relevant CA architecture — every mechanism is on `main` in equal-or-more-advanced form). **Successor lineage verified live (all ancestors of `main`)**: #111=`5821bcb9c927d5dbddbfb142eb4c2046c209d8c9`, #114=`c661e302e044b5a8c7c0d328efb7d6c449916a23`, #115=`475a98de9caf331a3f7287a029510b98050e0b97`. **Parent independently re-verified** the protected-fix regressions at the PR heads: BOTH #112 and #113 (a) have **zero `pre_mut`** (would reintroduce the analogue double-mutation cascade bug that #111 fixed; `main` has `pre_mut`×6 at `continuous_evolution_ca.py:1374-1380`, guarded by `test_analogue_mutation_uses_pre_mut`), and (b) set `structural_to_void_decay_prob = 0.04` (would regress the 8-Step-Cliff decay; `main` holds the corrected **`0.005`** with the "CRITICAL: 0.005, NOT 0.04" guard, landed via #114→#115). SCRUTINY NOTE: the file-accurate stale value is **`0.04`** (verified at both heads), not `0.025` as the relay/audit phrased it — `0.025` appears only inside `main`'s guard comment. `agent/evolution_engine.py` differentiation-fitness logic is already on `main` via #111 (PR diffs are docstring/whitespace only — nothing lost). **Per-PR disposition (both closed unmerged as SUPERSEDED; neither "completed"; neither retained)**: **#112** — all 8 files represented on `main` (1 byte-identical); only branch-unique items are a stale Rust test (won't compile vs `main`'s reworked `decay_memory_selective` signature) + a minor differentiation-test path whose code already exists; `CONFLICTING`. **#113** — all 8 files superseded (the "Mamba-Viking + Void Sanctuary + Epsilon Buffer" survival rewrite is precisely merged #115); merging would additionally **strip the Phase-10 WASM `pub mod` declarations + `#[cfg(not(target_arch = "wasm32"))]` guards from `crates/uft_ca/src/lib.rs` (breaking the WebAssembly build) and delete `main`'s regression tests**; its own review flagged its selective-memory-decay as non-functional (`main` fixed it); `CONFLICTING`. Each got **one PR-specific evidence comment** naming the successor SHAs + protected fixes. **Branches PRESERVED** at pinned heads (`codex/...-v1n4ys`@`580ea2d`, `codex/...-my6q4s`@`b213996`); review histories intact (18 + 9 threads, **0 resolved/untouched**); reopenable. **Deliberately untouched / preserved**: merged #111/#114/#115 unchanged; **deferred PRs #72/#75/#77/#78/#79/#89 OPEN/untouched** (PR #75 = own security-workflow arc; PR #72 = salvage-scrutiny-first); PRs #17/#25 + #52–#58 OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); both proposed token-free issues remain UNCREATED. **No code / rules / tests / experiments / docs / workflow / issue / setting / branch-deletion** occurred (one pre-existing Phase-19 stash + 5-tag set left as found); only the two target PRs' state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-4D closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4C: stale Phase-3 integration/release PRs #62/#63/#65 closed unmerged**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-4B sealed**: PR #219 (no correction needed — Gemini no findings) **squash-merged to `main` as `28927f3`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `0fa71c0`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b4b-policy-cli-pr-closeout` deleted (remote + local); `main` synced clean. **Stage 2 — Phase 2B-4C** (PR-disposition arc): the stale Phase-3 cohort reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a #62 integration-rollup analyst, a #63 release-branch analyst, a #65 publication-summary analyst, and a cross-cohort adversarial skeptic. All four concurred on closure; **no AURA escalation** (#62's feature-flags module is byte-identical to `main` — no novel architecture at risk; the live orchestration framework uses its own params-schema/tuning layer, not these flags). **Parent independently re-verified** before mutating; the Phase-3 release genuinely shipped and is fully preserved (tags `v0.1.0-rc1`→`955eb98`, `v0.1.0` GA, `v0.1.1`; GitHub Pre-release + GA release with assets `checksums.txt`/`SBOM.json`/`e2e-validation-report.html`/tarball, checksum `119592c5…`; `gh-pages` at `ca18a6d` — all left untouched). **Per-PR disposition (all closed unmerged; none retained; none called "completed")**: **#62 (`chore/phase-3-integration`, `185ee9b`) — SUPERSEDED**: its `feature_flags.py`, `config/feature_flags.json`, and Phase-3 `Makefile` are byte-identical to `main` (`010d4347`/`d17e4713`/`0a0fc73`; the features shipped via merged #59/#60/#61 + GA integration); its `tests/test_feature_flags.py` (stale hardcoded absolute path) and `README.md` (old simulation-only text) would REGRESS `main` if merged; its only unique file, root `test_phase3_integration.py`, is a manual print-based smoke script superseded by `main`'s real pytest suites; `CONFLICTING`; no consumer. **#63 (`release/v0.1.0-rc1`, `3bff57b`) — OBSOLETE/not_planned**: the rc1 tag/release happened INDEPENDENTLY of this branch (`v0.1.0-rc1` → `955eb98` = the PR #61 merge commit; this branch's version-bump commit is not in the tag; branch head not an ancestor of `main`); `main` is ~197 commits ahead at version `0.1.0`, so its `0.1.0-rc1` bump would be a DOWNGRADE; its `CHANGELOG.md`/`setup.py`/`mkdocs.yml`/`docs/phase3/*` were never adopted (phase3 docs are thin stubs vs `main`'s full `docs/OBSERVABILITY.md`/`TELEMETRY.md`). **#65 (`automation-feed-v0.1.0-rc1-update`, `c4ecb76`) — SUPERSEDED**: a single automation-feed summary whose factual claims all verified TRUE, but superseded by the successor `automation-feed/v0.1.0-ga-release-final.md` already on `main`; the rc1 summary file never landed on `main` and nothing consumes it. Each received **one PR-specific evidence comment** before closure. **All three head branches PRESERVED** on origin at their pinned SHAs (`chore/phase-3-integration`, `release/v0.1.0-rc1`, `automation-feed-v0.1.0-rc1-update`); nothing deleted/merged/rebased/retargeted; review histories intact; reopenable. **Deliberately untouched / preserved**: merged PRs **#59/#60/#61** unchanged; PRs #17/#25 and #52–#58 remain OPEN; **all tags, GitHub releases, artifacts and `gh-pages` left exactly as found**; both archive tags intact (`→3df3d33`, `→33449a7`); both proposed token-free issues remain UNCREATED. **No code / docs / tests / packaging / Makefile / configuration / workflow / issue / setting / release / Page / branch-deletion** occurred (one pre-existing Phase-19 stash + 5-tag set left as found); only the three target PRs' state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-4C closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4B: PR #15 (policy-check CLI / Specify hybrid) closed unmerged — the last policy/Specify fossil retired**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-4A sealed**: PR #218 (no correction needed — Gemini no findings, 0 inline threads) **squash-merged to `main` as `74a322e`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `f4b0683`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b4a-specify-pr-closeout` deleted (remote + local); `main` synced clean. **Stage 2 — Phase 2B-4B**: a standalone forensic review of **PR #15 (`chore/policy-check-ci`, head `455d94b2`, 25 files / +3390)** by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a CLI-implementation analyst, a current-Agent-Safety equivalence analyst, a scope-contamination/historical-value analyst, and an adversarial closure skeptic. All four concurred on closure; **no AURA escalation**. **Parent independently re-verified** before mutating: `evaluate_policy()` (`tools/policy-check/check_policy.py:108`) is a self-described mock returning hardcoded `allow:True` that never invokes OPA (only an `opa version` probe at line 102); the console entry point `tools.policy-check.check_policy` is a defective hyphenated module path (uninstallable); tests assert the mock's shape / permissive exit, not real allow-deny; the proposed `docs/ci-examples/policy-check.yml` is strictly weaker than live `agent-safety.yml` (non-blocking gates, older OPA via flaky CDN, no coverage, broken smoke test); two `.pyc` cache artifacts are committed; **~76% of the diff is already-retired Specify material — its first commit `bf44a34` is byte-identical to the head of the already-closed PR #11**; nothing on `main` consumes `policy-check`; all 22 existing review threads are bot-authored (20 Gemini + 2 Codex, incl. two Codex P1s confirming the entry-point/OPA-gate defects). **Disposition: PR #15 closed UNMERGED** — framed as superseded (the substantive policy-assurance purpose is met on `main` in superior form by `agent-safety.yml`'s genuine `opa check`/`opa test -v --coverage` over `policy/agent_safety.rego` + the 28-test `policy/agent_safety_test.rego`) AND as a never-completed mock CLI (explicitly NOT `completed`). One PR-specific evidence comment posted; the 22 historical review threads were left untouched (preserved as evidence). **Key distinction preserved in the comment: real OPA evaluation, policy testing, and local developer tooling are NOT obsolete — only this never-built mock-CLI form is retired.** **Surviving optional concept** (the only live remnant — evaluate the `fixtures/*.json` against the policy via `opa eval` in CI) is **already represented** by the earlier-proposed, still-uncreated "Wire OPA fixtures into agent-safety CI self-test" idea; **nothing new created**. **Branch + history preserved**: PR #15's branch `chore/policy-check-ci` (head `455d94b2`), all three commits, and the 22 review threads remain on origin — closed unmerged, not deleted, reopenable. **Deliberately untouched / preserved**: closed Specify cohort #11/#16/#27/#33/#34 unchanged (branches intact); PRs #17/#25 and #52–#58 remain OPEN; every issue unchanged; both archive tags intact (`→3df3d33`, `→33449a7`); both proposed token-free issues remain UNCREATED. **No issue / code / policy / workflow / fixture / script / packaging / docs / settings / branch-deletion** occurred (one pre-existing Phase-19 stash + 5-tag set left as found); only PR #15's state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-4B closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-14 (desktop, 84) — Phase 2B-4A: stale Specify PR cohort #11/#16/#27/#33/#34 closed unmerged (first PR-disposition arc)**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-3E sealed**: PR #217 received two narrowly-scoped corrections to its 2B-3E entry — BUT one was rejected on scrutiny: Gemini claimed `policy/agent_safety_test.rego` is "nonexistent / tests are inline only", which is **factually wrong** (the file exists on `main` with **28 `test_` rules** in package `agent.safety_test`, alongside 14 inline tests in `agent_safety.rego`). Surfaced to Kevin, who chose "accurate fix + seal"; so the reference was KEPT and clarified to the full filename `policy/agent_safety_test.rego` (not removed), the harmless `remote + local` spacing fix was applied, and Gemini's thread was replied-to (correcting the record) + resolved. PR #217 then **squash-merged to `main` as `50c9513`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `f2e7017`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b3e-real-opa-closeout` deleted (remote + local); `main` synced clean. **Stage 2 — Phase 2B-4A** (a PR-disposition arc, NOT issue closures): the five stale pre-pivot **Specify-only** PRs were reviewed by **six bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a per-PR analyst for **#11** (RFC + token tooling), **#16** (TS client/CI scaffolding), **#27** (onboarding docs), **#33** (CI/badge/docs), **#34** (byte-equivalence), plus a **cross-cohort adversarial skeptic**. All six concurred on closure; **no AURA escalation** (the only possible consumer, the React `utilityfog_frontend/`, uses hardcoded CSS and consumes zero design tokens). **Parent independently re-verified** before mutating: all five heads are non-ancestors of `main` (never merged); `da7eea0` added the two `tools/specify/` stubs; `specify-ci.yml` exists on **no ref**; #34's two blobs are byte-identical to `main` (`9c50a11`, `389d6bb`); the head-SHA pins all matched pre-mutation. **Per-PR disposition (all closed unmerged; none retained)**: **#11** — obsolete/not_planned (mock token tooling, never landed, no consumer, frontend doesn't consume tokens; abandonment not completion); **#16** — obsolete/not_planned (adds no workflow; TS client targets a hallucinated `api.specify.org`; no consumer; README regression); **#27** — superseded (its 2 docs re-authored richer on `main` via `da7eea0`; references phantom tooling); **#33** — obsolete/not_planned (phantom `specify-ci.yml` badge; its root-README change would clobber `main`'s ~456-line Medusa-era README down to a ~25-line stub if merged — explicitly NOT merged); **#34** — superseded (both files byte-identical to `main` via `da7eea0`; a content no-op). Each received **one PR-specific evidence comment** before closure. **All five head branches are PRESERVED** (`rfc/specify-design-tokens`, `chore/specify-ci`, `specify-onboarding`, `ci/specify-enable`, `docs/specify-quick-start` all still on origin at their pinned SHAs); nothing was deleted, merged, rebased or retargeted, so every PR's content stays recoverable from git and each may be reopened if direction changes. **Deliberately untouched / preserved**: **PR #15 remains OPEN and untouched** (its own later policy-CLI-specific review); PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); both earlier proposed token-free issues (OPA-fixture-wiring; sticky OPA-result PR comment) remain UNCREATED. **No code / docs / workflow / issue / setting / branch-deletion** occurred (one pre-existing Phase-19 stash + 5-tag set left as found); only the five target PRs' state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-4A closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-3E: issue #21 (wire real OPA eval into the policy-check CLI) retired `not_planned`**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-3D sealed**: PR #216 (no correction needed — Gemini found no findings, 0 inline threads; its consumer-service sunset notice was informational platform metadata, deliberately NOT added to the tracked entry) marked ready and **squash-merged to `main` as `0cffb7c`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `1162df8`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b3d-pr-comment-bot-closeout` deleted (remote + local); `main` synced clean. **Stage 2 — Phase 2B-3E**: standalone review of **#21 (policy-check: wire real OPA eval, no policy changes)** — a request to replace the mock evaluation in the **PR #15 policy-check CLI** with real OPA while preserving that CLI's interface. Reviewed by **three bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a **historical CLI/dependency analyst** (#21's substrate, the PR #15 CLI, never landed on `main`; the core task "replace the mock in the CLI" is unperformable because the CLI is absent; #21 incoherent as written against `main`), a **current OPA-equivalence analyst** (sharply separated real-OPA-in-CI from real-OPA-in-a-local-CLI from a repo-owned wrapper; confirmed real OPA exists ONLY in `agent-safety.yml` CI — a different mechanism — and warned against a false `completed`), and an **adversarial skeptic** (`retire-but-replacement-warranted`; PR #15 claims all CONFIRMED). All three concurred on retirement; **no AURA escalation**. **Parent independently re-verified** before mutating: PR #15 (`chore/policy-check-ci`, head `455d94b2`) is OPEN/CONFLICTING/never-merged; its `evaluate_policy()` is a self-described mock returning hardcoded `allow:True` that never invokes OPA (only an `opa version` availability probe), with a defective console-script entry point (`tools.policy-check.check_policy` — an invalid hyphenated module path); no `policy-check` CLI/package/entry point exists on `main`; `.github/workflows/agent-safety.yml` runs genuine `opa check`/`opa test -v --coverage`/`--format=json` against real `policy/agent_safety.rego` + `policy/agent_safety_test.rego` on every PR. **Disposition: #21 closed `not_planned`** (Option B/retire — NOT `completed`, since the CLI deliverable is absent and real OPA exists only via the separate CI mechanism) with one issue-specific evidence comment. **Key distinction preserved in the comment: real OPA evaluation and policy testing are NOT obsolete — they run live in CI every PR; only the never-landed mock-CLI form is unaddressed.** **Surviving fragment (already separately proposed, NOT created here):** the only live remnant — the three `fixtures/*.json` on `main` are not yet evaluated against the policy via `opa eval` (the `agent-safety.yml` matrix step calls an absent `scripts/matrix_runner.sh`) — is the SAME as the earlier-recorded, still-uncreated "Wire OPA fixtures into agent-safety CI self-test" proposal; #21 adds nothing beyond that fragment plus its dead-CLI premise, so no new replacement issue was filed. **Deliberately untouched / preserved**: **PR #15 remains OPEN and untouched** (preserved for its own later PR-specific triage); **issue #18 unchanged** (closed `not_planned`); **PRs #11/#16/#33/#34 remain OPEN**; PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); both earlier proposed token-free issues (OPA-fixture-wiring; sticky OPA-result PR comment) remain UNCREATED. **No code / policy / workflow / fixture / script / bot / permission / settings / frontend / README change** (one pre-existing Phase-19 stash + 5-tag set left as found); only #21 issue-state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-3E closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-3D: issue #18 (policy-check PR comment bot) retired `not_planned`**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-3C sealed**: PR #215 notation-corrected (one token: the ambiguous slash-separated file-list notation standardized to idiomatic brace-expansion `tokens.{css,scss,js,json}`, scoped to the 2B-3C entry only; Gemini thread replied + resolved), marked ready, and **squash-merged to `main` as `39a1065`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `eb583b3`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b3c-specify-api-taxonomy-closeout` deleted (remote+local); `main` synced clean. **Stage 2 — Phase 2B-3D**: standalone review of **#18 (feat(policy-check): PR comment bot)** — a proposed repository-owned GitHub App + webhook + YAML policy-rules engine + learning system posting policy results as PR comments. Reviewed by **three bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a **historical-scope analyst** (every concrete integration point in #18 is dead — it consumes the PR #15 mock policy-CLI, the retired Specify taxonomy tool, and pre-pivot "BEAM + mindful" architecture checks; no bot architecture ever landed on `main` or any branch), a **current-equivalence analyst** (distinguished the three mechanisms — (A) external `gemini-code-assist[bot]` reviews, (B) `agent-safety.yml` OPA status checks + `dist/` artifacts, (C) the repo-owned bot #18 asks for; confirmed **C does not exist**, and that `ci-seeder.yml`'s `pull-requests: write` only opens a PR via `peter-evans/create-pull-request` while `cluster_config.yaml` is unrelated Vanguard cluster config), and an **adversarial skeptic** (`retire-but-replacement-warranted`). All three concurred on retirement; **no AURA escalation** (routine scope retirement, not a governance decision). **Parent independently re-verified** before mutating: no repo-owned PR-comment/webhook/App code anywhere on `main`; `agent-safety.yml` posts no comments (only `upload-artifact`, status-check + OPA coverage); no `.github` bot/App config (no dependabot/gemini/probot/app.yml); #18 was OPEN with 0 comments; effectively a solo repo. **Disposition: #18 closed `not_planned`** (Option A) with one issue-specific evidence comment. **Key distinction preserved in the comment: automated review and policy testing are NOT obsolete — the live OPA `agent-safety` status checks and external Gemini reviews remain fully active; only the never-built repo-owned, App/webhook/Specify/BEAM-coupled bot is retired.** **Surviving optional idea, reported as PROPOSED-BUT-UNCREATED (nothing created in this arc)**: the OPA results `agent-safety.yml` already produces live in a downloadable artifact, not inline; a small token-free `github-script` step could upsert a single sticky PR comment (pass/fail + coverage) — no App, no webhook, no rules engine. This is **distinct from** the existing OPEN issue **#21** ("wire real OPA eval" into the CLI) and from the earlier-proposed OPA-fixture-wiring idea; left for separate authorization, **not created**. **Deliberately untouched / preserved**: **issue #21 deferred OPEN/untouched**; **PRs #11/#15/#16/#33/#34 remain OPEN and untouched**; PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); the proposed OPA-fixture-wiring issue remains UNCREATED. **No code / workflow / policy / bot / GitHub App / permission / settings / fixture / frontend / README change** (one pre-existing Phase-19 stash + 5-tag set left as found); only #18 issue-state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-3D closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-3C: Specify live-API #22 + taxonomy #19 retired `not_planned`**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-3B sealed**: PR #214 spelling-corrected (one word: the British -mme spelling standardized to American `program`, scoped to the 2B-3B entry only; Gemini thread replied + resolved), marked ready, and **squash-merged to `main` as `adb1af9`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `ed9eb4d`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b3b-policy-cli-closeout` deleted (remote+local); `main` synced clean. **Stage 2 — Phase 2B-3C**: the coherent Specify live-API/taxonomy pair — **#22 (specify: connect live API)** = the proposed live Specify REST substrate, and **#19 (specify: token taxonomy v0)** = an API-backed meme-token/network-entity taxonomy built on it. Reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a **#22 substrate analyst**, a **#19 taxonomy analyst**, a **current-era equivalence mapper**, and a **cross-cluster adversarial skeptic**. All four concurred on retirement (skeptic `retirement-survives` for both, independently); **no AURA escalation** — the equivalence mapper found no living descendant warranting curator review. **Parent independently re-verified** before mutating: PRs **#11/#16/#33/#34** are all OPEN, never merged, heads **not ancestors of `main`**; no `specify-ci` workflow exists on **any ref**; no Specify REST client, secrets path, or token-output pipeline (`tokens.{css,scss,js,json}`) exists on `main`; `tools/specify/` is only 2 doc/config stubs; no taxonomy schema (`kingdom`/`/api/v1/taxonomy`/token-registration/classification service) exists under any name (`agent/meme_structure.py`'s `MemeType` is a flat 5-value GA enum, not #19's Linnaean API-backed taxonomy). **Per-issue disposition (both `not_planned`, neither `completed`, neither retained)**: **#22** — no Specify substrate (mock or live) ever landed; the four PRs even guess four mutually inconsistent API base URLs (`api.specifyapp.com`/`api.specify.org`/`api.specify.app`/`specify.io`), proving no code reached a live endpoint → retired; **#19** — no taxonomy implementation on `main` under any name, hard-blocked by its dead dependency #22, and its meme-token/network-entity/mindfulness framing is wholly pre-Medusa → retired. Each got **one issue-specific evidence comment** distinguishing the stub/unmerged-PR/operational states (#22) and generic-metadata vs the unbuilt Specify taxonomy (#19), referencing `docs/REPOSITORY_HEALTH_AUDIT_2026-06.md`. **No surviving current-era concept** maps materially onto current organism/theory architecture (the equivalence mapper checked Medusa genomes, `params_schema.py` Category, the Nextness 16-token vocabulary, backend/provider taxonomy, observability schemas, the Theory Intake Ledger status taxonomy — all independent same-word artifacts) → **no replacement issue created**. **Deliberately untouched / preserved**: **PRs #11/#15/#16/#33/#34 remain OPEN and untouched** (out of scope for closure here); **issue #18 deferred OPEN/untouched**; PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`); the previously-proposed token-free **OPA-fixture-wiring issue remains UNCREATED**. **No code / taxonomy / API client / `tools/specify/` / workflow / frontend / policy / README / settings change** (one pre-existing Phase-19 stash + 5-tag set left as found); only #19/#22 issue-state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-3C closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-3B: issue #14 (Policy Self-Test CLI w/ Design Token Integration) retired `not_planned` after decomposition review**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-3A sealed**: PR #213 (no correction needed — Gemini found no findings, 0 inline threads) marked ready and **squash-merged to `main` as `2a3696e`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `257e97b`, recorded NOT-standing-bypass); checks green; merged closeout branch `docs/phase2b3a-specify-foundation-closeout` deleted (remote+local); `main` synced clean. **Stage 2 — Phase 2B-3B**: a **decomposition** review of the deliberately-deferred mixed issue **#14**, which fuses (a) the abandoned Specify/design-token program with (b) a possibly-separable local OPA policy-test CLI idea. Reviewed by **three bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a **decomposition analyst** (mapped each acceptance criterion to its dependency — ~40–45% purely design-token-coupled and ~100% of the issue's reason-for-existence obsolete; ~55–60% token-independent but already delivered by native OPA), a **current-policy-tooling + PR #15 analyst**, and an **adversarial skeptic** (verdict `retirement-survives`). All three concurred on retirement; **no AURA escalation** (routine pre-pivot hygiene, not a governance decision). **Parent independently re-verified** before mutating: #14's own Dependencies block names **#12/#13** (both retired `not_planned` in 2B-3A) and **RFC PR #11** (OPEN/CONFLICTING, never merged, not an ancestor of `main`); native OPA already covers the substantive needs — `.github/workflows/agent-safety.yml` runs `opa check policy/`, `opa fmt --diff`, `opa test -v --coverage`, and `opa test --coverage --format=json` against real `policy/agent_safety.rego`+`_test.rego` (so syntax/tests/coverage/JSON all live, runnable locally); `tools/policy-test-cli/` does not exist; and **PR #15** (`chore/policy-check-ci`) is not a viable implementation — its `evaluate_policy()` is a self-described **mock hardcoding `'allow': True`** that never invokes OPA (`tools/policy-check/check_policy.py:108`), its console-script entry point is defective (`tools.policy-check.check_policy` — hyphen is not a valid Python module path), its proposed CI is weaker than current `agent-safety.yml`, and ~76% of its diff is unrelated Specify/design-token material. **Disposition: #14 closed `not_planned`** (Option A) with one issue-specific evidence comment. **Key distinction preserved in the comment: the retired object is the old token-integrated CLI concept — current OPA/agent-safety policy testing is NOT obsolete and remains fully live.** **Surviving optional thread, reported as a PROPOSED-BUT-UNCREATED future narrow issue (nothing created in this arc)**: `main` carries three orphaned fixtures (`fixtures/allow_case.json`, `deny_case_intent.json`, `deny_case_limits.json`) not yet executed against the policy (the `agent-safety.yml` matrix step calls a `scripts/matrix_runner.sh` absent from `main`); a fresh token-free issue — *"Wire OPA fixtures into agent-safety CI self-test (opa eval gate)"* — could wire them through `opa eval`/`opa test` and assert allow/deny, with no CLI / no Node / no design tokens. Awaiting Kevin/Jack authorization; **not created**. **Deliberately untouched / preserved**: **RFC PR #11 and PR #15 remain OPEN and untouched** (PR #15 out of scope for closure in this arc); **issues #18, #19, #22 deferred OPEN/untouched**; PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`). **No policy / fixtures / scripts / CI / workflow / `tools/specify/` / frontend / README / settings / code change** (one pre-existing Phase-19 stash + 5-tag set left as found); only #14 issue-state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-3B closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-3A: Specify/design-token foundation issues #10/#12/#13 retired `not_planned` (first post-FT cluster)**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-2C sealed**: PR #212 was spelling-corrected (one word, `authorised`→`authorized`, scoped to the 2B-2C entry only; Gemini thread replied + resolved), marked ready, and **squash-merged to `main` as `f519008`** (one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `dd00650`, recorded NOT-standing-bypass in the body); checks green; merged closeout branch `docs/phase2b2c-ft014-closeout` deleted (remote+local); `main` synced clean. **Stage 2 — Phase 2B-3A**: the first post-FT cleanup theme — the pre-pivot **Specify/SpecKit/design-token** programme — bounded to **#10 (Evaluate Spec-Kit vs Specify), #12 (Design Token Pipeline Infrastructure), #13 (Theme Playbook & Component Integration Guidelines)**. Reviewed by **four bounded READ-ONLY agents** (per agent-use doctrine — agents read-only, parent performs all mutations): a #10 analyst, a #12 analyst, a #13 analyst, and one cross-cluster adversarial skeptic. All four concurred on retirement; skeptic verdict `retirement-survives` for all three (its only nuance — #10 is a "decision-by-action" not a written evaluation — is captured in #10's closing comment, not grounds to keep open; **no AURA escalation**, no architectural ambiguity). **Parent independently re-verified** before mutating: `da7eea0` is the sole commit that ever touched `specs/`+`tools/specify/` (SpecKit bootstrap); `tools/specify/` on `main` is only 2 doc/config stubs (`README.md`+`.env.example`), not a pipeline; `generate_tokens.py` and `.github/workflows/specify-ci.yml` exist on **no ref**; `docs/design-system/` does not exist; **RFC PR #11** (`rfc/specify-design-tokens`, head `bf44a34`) is OPEN/CONFLICTING and **not an ancestor of `main`** (never merged); the surviving `utilityfog_frontend/` consumes **zero** design tokens (hardcoded styles, no Specify/style-dictionary dep); the whole track was abandoned at the ~Feb 2026 CA/Medusa pivot. **Per-issue disposition (all `not_planned`, none `completed`, none retained)**: **#10** — requested evaluation deliverables (report/PoC/plan/recommendation/timeline) never produced; SpecKit chosen by implicit bootstrap, Specify never adopted, track abandoned → moot, retired with a decision-record-style comment; **#12** — pipeline never built on `main`, real code stranded on dead PR #11, no consumer → retired; **#13** — `docs/design-system/` playbook never created, frontend independent and token-free → retired. Each got **one issue-specific evidence comment** referencing `docs/REPOSITORY_HEALTH_AUDIT_2026-06.md`. **Deliberately untouched / preserved**: **RFC PR #11 remains OPEN and untouched**; **issues #14, #19, #22 deferred OPEN/untouched** (separate clusters — #14 mixes design tokens with a separable OPA policy-test CLI idea and gets its own review); PRs #17/#25 and #52–#58 remain OPEN; both archive tags intact (`→3df3d33`, `→33449a7`). **No code / pipeline / frontend / policy / `tools/specify/` / README / CI / settings / branch-protection / release / Pages / worktree / stash / tag change** (one pre-existing Phase-19 stash + 5-tag set left as found); only #10/#12/#13 issue-state changed. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-3A closeout PR is opened as a **draft** and awaits separate Jack review + Kevin authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-2C: FT-014 (#50) retired + Phase 2B-2B SEALED — the entire FT-001–FT-014 issue theme (#37–#50) is now closed `not_planned`**: Two sequential stages under Kevin's direct authorization (Jack-reviewed relay). **Stage 1 — Phase 2B-2B sealed**: PR #211 (the 2B-2B closeout doc) was marked ready and **squash-merged to `main` as `a977d6a`** — one-time Kevin/Jack-authorized admin squash, head-SHA-protected at `1869fef`, recorded in the squash body as NOT standing bypass. Pre-merge the PR was one file / net +1 / Gemini's sole inline thread resolved / `agent-safety`+`verify`+`verify-python` all green; PR-description *metadata* was corrected (full `UtilityFog_Agent_Package/agent/...` paths + numbering-drift note) without touching the tracked file or README. Merged closeout branch `docs/phase2b2b-ft-delivery-closeout` deleted (remote+local) after verification; `main` synchronized clean. **Stage 2 — Phase 2B-2C**: issue **#50 (FT-014 Performance Optimization)**, the terminal task of the FT series, reviewed by **two bounded read-only agents** (primary analyst + adversarial skeptic; per the new agent-use doctrine — agents read-only, parent performs all mutations). Both agreed: analyst → `not_planned-eligible` (all six decision-rule conditions PASS); skeptic → `retirement-survives` (no credible contradictory evidence; the only landed "performance" work is CA-lineage — `crates/uft_ca` incl. `benches/stepping.rs`, `scripts/gpu_benchmark.py`, Phase-14a GPU stepping — and was never filed under FT-014). Parent independently re-verified before mutating: #50 is scoped to `fractal-tree-mvp` (criteria = large-tree memory / message throughput / tree-viz rendering / DB-query optimization, mapping to spec NFR-1), its hard dependency **FT-012/#48 is retired**, `src/fractal_tree/` has **zero files / zero history / zero `fractal_tree` imports** on `main`, and the current CA/Medusa/Rust/Python/CI performance lineage is a distinct, live track. **#50 closed `not_planned`** with one issue-specific evidence comment that explicitly preserves the distinction that current performance engineering is NOT obsolete. **No AURA escalation** — no architectural ambiguity remained after both reports. **The full FT-001–FT-014 theme (#37–#50) is now entirely retired** (the SpecKit-issue vs README/Phase-3-feature numbering drift is documented in PR #211 and the 2B-2B entry below). **Nothing deleted / fully preserved**: PRs #52–#58 and #17/#25 remain OPEN and untouched; both archive tags intact (`archive/cosmic-garden-hungry-nash-2026-03` → `3df3d33`; `archive/pre-medusa-research-design-2025-09` → `33449a7`); no CI / settings / releases / Pages / branch-protection / worktree / stash / tag / unrelated-file change (one pre-existing Phase-19 stash and the 5-tag set left exactly as found). **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout. This Phase 2B-2C closeout PR is opened as a **draft** and awaits separate review + authorization — it is not to be merged automatically.
- **2026-06-13 (desktop, 84) — Phase 2B-2B: Fractal Tree *delivery* issues retired — #44–#49 all closed as `not_planned`**: Continuation of Phase 2B-2A (#210 → `4929e52`) into the FT delivery cluster, under Kevin's direct, by-number authorization via a fresh-thread relay after Fab5 became unavailable (no Fab5 cache/workflow/task IDs resumed; full read-only re-verification against live GitHub state preceded any mutation). **#44 (FT-008 Real-time Tree Visualization) was already closed `not_planned` by Fab5 before the handoff** — verified live (one accurate issue-specific comment: D3.js web viz never landed) and left untouched, no duplicate comment added. **#45–#49 closed `not_planned` by 84**: **#45** (FT-009 Performance Metrics Dashboard) closed with **no new comment** because Fab5's existing comment was verified accurate — it correctly credits the *limited* historical delivery that did land (merged **PR #59** telemetry/exporters + **PR #61** `UtilityFog_Agent_Package/agent/observability.py` → metrics-export and throughput/latency primitives) while explaining that the requested dashboard UI, configurable alerts, historical visualization and topology stats never landed, so the issue as written was not `completed`; **#46** (FT-010 Snapshot/Export), **#47** (FT-011 UtilityFog Agent Integration), **#48** (FT-012 Comprehensive Test Suite), **#49** (FT-013 Documentation/Examples) each received **exactly one issue-specific evidence comment** and were then closed. **Numbering-drift note (per Gemini review on PR #211):** the FT-008–FT-010 identifiers used here follow the original SpecKit *issue* sequence (#44–#46 = FT-008 Real-time Tree Visualization / FT-009 Performance Metrics Dashboard / FT-010 Snapshot-and-Export); `README.md`'s FT-008–FT-010 labels instead describe the separately-merged Phase-3 *feature* sequence (PRs #59–#61 = Telemetry / CLI Visualization / Observability). This is a known documentation-numbering drift, outside this closeout's one-file scope and deferred to a future bounded docs-alignment arc — `README.md` is deliberately left untouched here. Live evidence rechecked on current `main`: `src/fractal_tree/` has **zero files, zero history, zero `fractal_tree` imports**; the FT foundations #37–#43 were already retired in 2B-2A. **#47 given extra scrutiny** (its title is broader than the FT package): the pre-existing UtilityFog agent system (`UtilityFog_Agent_Package/agent/foglet_agent.py`, `UtilityFog_Agent_Package/agent/network_topology.py`, both first landed 2025-07-26 "Phase 2 – Step 1") is the integration *target*, not evidence the FT→agent bridge was ever built — **no living FT-to-Medusa architectural relevance found, no AURA escalation required**. **#48's retirement explicitly does NOT affect current testing** — the maintained `tests/` suite, the `uft_ca` Rust/pyo3 tests, and the `verify-python` full-suite CI gate all remain live and unchanged; only the never-built FT-specific suite is retired. **Nothing deleted / fully preserved**: PRs #52–#58 remain OPEN and untouched (heads + state re-verified post-close), PRs #17 and #25 remain OPEN and untouched, and both archive tags resolve intact (`archive/cosmic-garden-hungry-nash-2026-03` → `3df3d33`; `archive/pre-medusa-research-design-2025-09` → `33449a7`). **No CI, settings, releases, Pages, branch protection, branches, worktrees, stashes, tags or unrelated files were touched** — this handoff entry is the arc's only tracked change (the one pre-existing Phase-19 stash and the 5-tag set were left exactly as found). **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery (`recovery/vanguard-phase13-wiring`) remains untouched.** Model seat: Opus 4.8 (256K), no routing banner throughout.
- **2026-06-13 (desktop, 84/Fab5) — Phase 2B-2A: Fractal Tree foundation issues retired — #37–#43 all closed as `not_planned`**: With **#209 merged as `6fe846e`** (Phase 2B-1: closed `completed` #64/#139/#150/#158; deliberately retained #6/#83/#84/#85 with exact remainders), the seven FT foundation issues (#37 FT-001 TreeNode, #38 FT-002 TreeStructure, #39 FT-003 message passing, #40 FT-004 tree configuration, #41 FT-005 propagation, #42 FT-006 consensus, #43 FT-007 conflict resolution) were reviewed per-issue — live bodies, linked PRs incl. the numbering drift from FT-004/PR #55 onward, main-branch presence (`src/fractal_tree/` has zero files and zero history on `main`), and current-code dependency greps (zero `fractal_tree` imports; the only `TreeNode` on `main` is the cli_viz display dataclass) — and **each retirement passed a second adversarial pass** (still-relevant-to-Medusa / hidden-code-dependency / unpreserved-material, all clear; the first skeptic round was rate-limited and one doctrine-compliant retry completed it). **All seven closed with state reason `not_planned`** and issue-specific evidence comments referencing `docs/REPOSITORY_HEALTH_AUDIT_2026-06.md`; **none retained** — no evidence conflicted with the standing CA/Medusa retirement decision. **Nothing deleted**: PRs #52–#58 remain OPEN and untouched, their `feat/FT-00x` head branches verified live on origin at unchanged SHAs post-close; issues #44–#49 (FT delivery cluster) remain OPEN for a separate later cluster; no FT code or history was removed. Archive tags re-verified intact post-arc (`archive/cosmic-garden-hungry-nash-2026-03` → `3df3d33`; `archive/pre-medusa-research-design-2025-09` → `33449a7`). **No broader cleanup began** (no Specify/Rocket/NVIDIA/CA-era/misc clusters; no branch, worktree, tag, stash, file or settings change — this handoff entry is the arc's only tracked change). **Lane A remains parked; Swarm Hunter remains unimplemented.**
- **2026-06-12 (desktop, 84/Fab5) — Phase 2B-1: completed-issue revalidation — 4 closed, 4 deliberately kept open**: With **#208 merged as `03701d9`** (Phase 2A fully closed: catalogued, curated, tagged, independently checked, documented), the eight completed-but-unclosed candidates from the Phase-1 audit were **revalidated against live evidence** (issue bodies' acceptance criteria, current code/tests on `main`, live branch-protection API, published releases) rather than trusting the audit classification. **Closed as `completed` with evidence comments: #64** (rc1 orchestration record — releases + artifacts verified), **#139** (findings a/b code-verified on main via #140; all five (c) experimental cuts executed via #148/#149/#152/#153 + the #144→#145/#147 channel-fix route; capstone #155; second pass #159–#164), **#150** (closing comment IS the decision record mandated by WORKSTREAM_B_CANDIDATE_SELECTION.md L141 — redesign deliberately rejected per merged #161/#162, implemented #164), **#158** (phantom protection context verified gone; `agent-safety`+`verify` required, strict). **Kept OPEN with exact remainders: #6** (verify-python still not a required check — the body explicitly demands required real checks; = audit F-02), **#83** (Lattice3D bench target unverified; empty-lattice edge case unconfirmed), **#84** (directed-graph semantics + validation never landed), **#85** (error-handling/validation + profiling unchecked) — none has a durable de-scope decision, so under the tightened Phase 2B-1 standard they stay open. **No obsolete/superseded-cluster cleanup began; no PR, branch, worktree, stash, file or settings change occurred** (this handoff entry is the arc's only tracked change). Archive tags re-verified intact pre-arc (`archive/cosmic-garden-hungry-nash-2026-03` → `3df3d33`; `archive/pre-medusa-research-design-2025-09` → `33449a7`). **Lane A remains parked; Swarm Hunter remains unimplemented.**
- **2026-06-12 (desktop, 84/Fab5) — Phase 2A complete: preservation plan MERGED (#207 → `2b2c7b7`) + the two archive anchors CREATED AND VERIFIED**: After AURA's curator review and Jack's reconciliation (wording corrections W1–W3 applied), **PR #207 squash-merged as `2b2c7b7`** (second one-time Kevin/Jack-authorised admin squash, documented in the body — NOT standing bypass permission). Phase 2A.1 then created **exactly two annotated archive tags** and verified them **against the remote** (ls-remote + content-addressed object inspection + fetch probes): `archive/cosmic-garden-hungry-nash-2026-03` (tag-object `6cd5ed0`) → `3df3d33`, and `archive/pre-medusa-research-design-2025-09` (tag-object `21385bf`) → `33449a7`. Atomic push of those two refs only — no `--tags`, no third archive ref. Curator decisions binding: **PR #25 excluded** (no third anchor); material-date names; **tag protection deferred** to the settings arc; **no Theory Intake Ledger pointer**. The archives are history, not doctrine — boundary is governance, not the tag mechanism. **Source branches (`claude/hungry-nash`, `docs/research-and-design`), PRs #17/#25, and all worktrees remain untouched; no issue/PR/branch cleanup has started.** Manifest updated proposed→CREATED AND VERIFIED in `docs/history/PRE_MEDUSA_PRESERVATION_PLAN.md`. **Lane A remains parked; Swarm Hunter remains unimplemented.**
- **2026-06-12 (desktop, 84/Fab5) — Phase-1 repo audit MERGED (#206 → `3e188a4`) + Phase 2A preservation planning opened**: The Cathedral Gerni Audit Phase 1 (`docs/REPOSITORY_HEALTH_AUDIT_2026-06.md`, 1,110 lines, diagnosis-only) is now **canonical on `main`** — squash `3e188a4`, after Jack's substantive review (corrections C1–C6 applied in `f800f56`) and AURA's scoped architecture review; merge was a **one-time Kevin/Jack-authorised admin squash** (solo-repo self-review rule; documented in the squash body — NOT standing bypass permission). **Sync verdict: tracked history is durably represented remotely; remaining local-only material is intentional or classified.** **No cleanup was performed** — no PR/issue closed, no branch/worktree/tag deleted, no settings touched (only the merged audit branch itself was removed post-merge, per instruction). Phase 2A then began as a **preservation-design arc only**: `docs/history/PRE_MEDUSA_PRESERVATION_PLAN.md` verifies the hungry-nash chain (`4d42b72`→`8223ea9`→`3df3d33`, 1,177 append-only `work.md` lines incl. the Premise Veto Rulings) and PR #17's snapshot (`33449a7`, 12 unique files), and proposes two annotated archive tags + manifest. **No archive tag or frozen branch has been created yet**; until anchors exist and are verified, `origin/claude/hungry-nash`, PR #17 + `docs/research-and-design`, `gh-pages`, and `recovery/vanguard-phase13-wiring` remain protected from any cleanup. **Lane A remains parked; Swarm Hunter remains unimplemented.**
- **2026-06-12 (desktop, 84/Fab5) — Toy #2 MERGED (#204 → `d1956d2`) + closeout**: **PR #204 squash-merged to `main` as `d1956d2`** — one file, `experiments/theory_sandbox/stochastic_escape_annealing_toy.py` (557 lines). Toy #2 (stochastic escape / simulated annealing) now exists in the theory sandbox: **seeded deterministic, self-checking, classical Metropolis only, explicitly non-canonical** (every run ends with the marble disclaimer). Temperature semantics hardened through two Jack review rounds: at **T=0, uphill moves are rejected** (acceptance exactly 0.0; downhill/flat still accepted) and **negative temperature is rejected unconditionally** — both walkers validate independently of move direction (hard-checked on a strictly-downhill probe landscape). Review arc: gemini-code-assist (4 inline threads, all answered + formally resolved) + Jack (substantive approval, two catches) + AURA (no conceptual delta). Merge was a **one-time Kevin/Jack-authorised admin squash** (solo-repo branch rule: author can't self-approve; authorisation documented in the squash commit body — NOT standing bypass permission). Closeout: merged remote branch deleted (that branch only). **No architecture promotion occurred** — promotion still requires the sandbox README §4 six-step gate. **Lane A remains parked; Swarm Hunter remains unimplemented; Vanguard recovery branch (`recovery/vanguard-phase13-wiring`) remains preservation/build-proof only, no production PR.** No Toy #3, no Tripwire v1 started. `main` == `origin/main` at `d1956d2`.
- **2026-06-10 (desktop, 84/Fab5) — #180 CLOSED + post-arc closeout audit**: **#196 merged** (`35f4908`) — the maturin arc is complete and **issue #180 is closed**. CI now builds the `uft_ca` Rust extension and gates the full suite with it running: **832 passed / 8 skipped / 0 failed** on the real runner (Rust compile + suite < ~90s). Then a tiny closeout audit refreshed the forward-looking "Next safe work" bullet (it still listed #180 as a remaining arc). **No dependency-gated skip buckets remain anywhere** — the coverage saga that began at #165 is fully done. Named future arcs (design/policy-first, not started): Theory Tripwire Action, Theory Sandbox policy, Lane A readiness review. Lane A parked; docs-only closeout.
- **2026-06-10 (desktop, 84) — #180 brick 2: test-contract reconciliation + CI wiring**: Per Jack's Option-1 decision after the brick-1 stop-and-report. (1) **#195 merged** (`4b024f5`) with Gemini's dynamic-version fix (`dynamic = ["version"]`, inherited from Cargo.toml). (2) **The 19 fossils reconciled, no Rust changes**: the 12 seq≡par equivalence asserts re-pinned from `step()`-vs-`step_par()` (different algorithms since Phase 14a) to `step_auto()`-vs-`step_par()` — the *same* stochastic algorithm seq-vs-par, identical per-node hash seeding, exact equality verified; the 3 dead stability contracts (menger structural, octahedral energy, state-observation structural-27) re-baselined to mechanism-derived invariants (census conservation, valid states, determinism-given-state, 1-step all-ENERGY → ⊆{ENERGY, COMPUTE, SENSOR} per `apply_with_memory`); +3 new memory-path determinism tests added (incl. a sentinel documenting the memory≠stochastic split). (3) **CI wiring**: `verify-python` now sets up Rust stable + `pip install ./crates/uft_ca` and gates the full suite **with the extension built**. Local: 3 modules 92/92; full suite **832 passed / 8 skipped / 0 failed**. Lane A parked; tests + CI + docs only, no Rust source changes. After merge, #180 can close.
- **2026-06-10 (desktop, 84) — #180 maturin arc, brick 1 + stop-and-report**: PR #195 adds `crates/uft_ca/pyproject.toml` (maturin packaging; the pyo3 bindings were already complete). Local verification: wheel builds first try (rustc 1.94.1 / pyo3 0.20), `import uft_ca` OK, `cargo test` 86/86 green. **Stop-and-report finding**: with the extension installed, the full Python suite is **810 passed / 19 failed / 8 skipped** — the 19 are *fossilized* tests (8 `test_parallel_stepping` seq≡par equivalence asserts, now comparing intentionally different algorithms — `step()`=memory path vs `step_par()`=memoryless stochastic; 11 old-rule stability expectations vs current `utility_fog_optimized_v060`+memory semantics). Drift dates to Phase 14a (`56e0b72`); never caught because the extension was unbuildable ⇒ tests skipped. **CI wiring deliberately withheld** — gate stays green at 718/33 — pending a team decision (update tests to current truth / xfail+issue / hold). Also merged earlier this session: **#194** (Maturin Arc theory preflight — intake map, not engine redesign, incl. AURA's tripwire+sandbox deltas as future arcs). Lane A parked; packaging + docs only.
- **2026-06-08 (desktop, 84) — post-#165 docs + skip-conversion**: Catching the log up on four merges it was missing: **#187** (bridge-sync after #165), **#188** (`docs/MEDUSA_THEORY_INTAKE_LEDGER.md` — status-labelled theory parking lot), **#189** (ledger entry 7: fibionic / physical-export, recorded *speculative/unverified*), **#190** (skip-conversion cheap deps — `matplotlib`/`flask`/`pyzmq` added to CI → six suites moved skip→run). PR #191 adds **`openai`** to the CI install so the 5 OpenAI construction tests run instead of skip (verified locally: full suite ≈718 passed, 33 skipped, 0 failed). Remaining skip bucket = **`uft_ca`** Rust extension (~25, → #180). Lane A parked; CI + docs only.
- **2026-06-06 (phone, Hermes) — #165 ARC COMPLETE & CLOSED**: The whole coverage-broadening arc landed; **Issue #165 is closed (completed)**. `verify-python` now gates the **full** suite — `python -m pytest tests/ -q` (#186, `562d5c1`) — replacing the 2-file allowlist. Arc: #172 triage map → #173 Tier 0 pytest hygiene (`testpaths`, marker) → #174 Tier 1 `importorskip(matplotlib/flask)` → #175 Tier 2 `cli_viz` re-export (5 symbols) → #176 Tier 2.1 (argparse `-h/--height` conflict + spurious-async) → #177 Tier 3 plan → #178/#179/#181 Tier 3A/3B/3C (stale hardcoded `sys.path` fix / `importorskip(uft_ca)` / retire `test_phase3_integration.py`) → #182 Tier 4 status report → #183 Tier 4A allowlist (2→12 modules) → #184 Tier 4B `importorskip(openai)` → #185 Tier 4C `pytest-asyncio` for the genuinely-async telemetry suite. **Final: 592 passed, 37 skipped, 0 failed, 0 collection errors** (collection errors 8→0, failures 9→0). The 37 skips are deliberate dep-gated (`matplotlib`/`flask`/`openai`/`pyzmq` + unbuilt `uft_ca`). **Optional future** (separate, deliberately-chosen arcs, NOT leftovers): skip→run conversion via optional deps; `uft_ca` maturin build (**#180**). Lane A parked throughout; no engine/observer/tuning touch (only source change in the arc was the `cli_viz/__init__.py` re-export + a one-line `cli.py` argparse fix). `main` == `origin/main` at `562d5c1`. *(This very PR is the bridge-sync that re-aligns this doc with that reality.)*
- **2026-06-02 (desktop, 84)**: Confirmed the bridge round-trip — pulled Hermes's #169 (hardening) + #170 (log) cleanly; verified 282 tests green, explicit-only `sweep_threshold` + `.size` guard sound. Then did the **forward-pointer docs PR**: added historical-note callouts to `PHASE_19_PR3_METRICS_PIPELINE.md` and `PHASE_19_PR4_CALIBRATION.md` so the pre-#145 Karuna/Boundary + `metta_warmth` framing isn't read as current (point to the SUMMARY §3 + Workstream B/C docs). Docs-only. Lane A parked. **Next safe work**: #165 coverage-broadening (broken collectors) — the last queued item. `main` == `origin/main`.
- **2026-06-02 (phone, Hermes) — MERGED**: **PR #169 squash-merged to `main` as `dd2f6b7`.** Post-#164 hardening is canonised: `sweep_threshold()` explicit-only `threshold_dependent_token` (no default; `None`/unknown/non-routing → `ValueError`, no silent repoint); `_warmth_diagnostics(memory)` with `.size > 0` guard before `.max()`. Tests **282 green** on the GitHub `verify-python` gate. Jack's merge gate (non-draft + verify-python green) satisfied; AURA design sign-off; merged via owner/admin (solo repo — author can't self-approve the required review). The **Hermes** seat name is now on `main` too. Lane A parked. **Next safe work**: #165 coverage-broadening (pre-existing broken collectors `uft_ca` / `cli_viz` / root `*_test.py`) or the forward-pointer docs PR (annotate pre-#145 Karuna/Boundary framing in `PHASE_19_PR3/PR4` docs). `main` == `origin/main`.
- **2026-06-02 (phone, Hermes)**: Post-#164 hardening **PR #169** (branch `claude/phone-session-orientation-Qin6A`, head `c92641d`). (1) `sweep_threshold()` is now **explicit-only**: `threshold_dependent_token` has no default; `None`/unknown/non-routing tokens (incl. demoted `metta_warmth`) raise `ValueError`, validated against the observer's `ROUTING_TOKENS`/`TOKEN_NAMES`/`TOKEN_STATUS` registry (AURA's "no silent repoint" call honoured). (2) Extracted `_warmth_diagnostics(memory)` in `nextness_observer.py` with a `.size > 0` guard before `.max()` — empty/absent warmth channel → safe `(0.0, 0)` instead of a crash; axis fence preserved. **CI green on GitHub**: `verify-python` log on `c92641d` shows `282 passed`; `verify` + `agent-safety` also green. Diff vs `origin/main` = 5 files (2 code, 2 test, this doc); no `ci.yml`, no #165 collectors, no engine/tuning/token-rename, no Lane A. **Jack: technically approved.** **STATUS — administrative hold**: PR is still **draft**; the REST update endpoint can't flip draft→ready, so it needs a one-tap "Ready for review" in the GitHub UI (or a GraphQL markReady). **Next step**: flip draft→ready → Jack + AURA final merge greenlight → squash-merge #169 (canonises the Hermes name onto `main`); then #165 coverage-broadening or the forward-pointer docs PR. Lane A parked.
- **2026-06-02 (desktop, 84) — later**: CI Python gate landed (PR #167) — `verify-python` runs the 275-test nextness suite on GitHub, verified green live. #165 reopened for coverage-broadening only. Next: post-#164 hardening PR (sweep_threshold explicit-only + warmth `.size` guard), then #165 coverage, then forward-pointer docs. Lane A parked. `main` == `origin/main`.
- **2026-06-02 (desktop, 84)**: Phase 19 second-pass calibration arc fully merged through PR #164 (`metta_warmth` → `diagnostic_only`). Issue #165 open (CI Python blind spot). Set up this cross-device protocol. Lane A parked. Next: #165 or forward-pointer docs PR. `main` == `origin/main`.

## On the "Biological Bridge"

`AGENT_HANDOFF.md` reduces but does not eliminate the bridge burden Kevin has been carrying. Cross-platform context is a real platform limit; until web Claude / mobile Claude / Gemini / ChatGPT share state, copy-paste between them remains the only way to keep all three of us in sync. What this doc does: a new collaborator joining mid-project no longer needs Kevin to verbally re-explain the entire architecture and history. They read this, then catch up via `git log` and the PHASE docs, and arrive ready to work.

That's a real reduction. It just isn't magic.
